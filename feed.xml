<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://shamshad.in/feed.xml" rel="self" type="application/atom+xml" /><link href="https://shamshad.in/" rel="alternate" type="text/html" /><updated>2026-06-01T22:25:10+05:30</updated><id>https://shamshad.in/feed.xml</id><title type="html">M. Shamshad</title><subtitle>Sugarcane Breeder and Computational Biology Researcher specializing in GWAS, population genetics, and bioinformatics tutorials.</subtitle><author><name>Dr. M. Shamshad</name><email>shamshad.rattan@gmail.com</email></author><entry><title type="html">Strip Plot Design</title><link href="https://shamshad.in/posts/2026/05/strip-plot-design/" rel="alternate" type="text/html" title="Strip Plot Design" /><published>2026-05-14T00:00:00+05:30</published><updated>2026-05-23T20:31:21+05:30</updated><id>https://shamshad.in/posts/2026/05/strip-plot-design</id><content type="html" xml:base="https://shamshad.in/posts/2026/05/strip-plot-design/"><![CDATA[A **Strip Plot Design** (also called a **Criss-Cross Design**) is an experimental layout used when **two factors are both difficult or costly to randomise** at the level of individual plots. It is a natural extension of the split plot concept but treats both factors symmetrically — neither is nested within the other.

In a strip plot:

- **Factor A** (the *row factor*) is applied to **horizontal strips** running across each block
- **Factor B** (the *column factor*) is applied to **vertical strips** running down each block
- The **intersection** of a row strip and a column strip forms the experimental unit for the interaction A×B
<!--more-->
## Split plot Design
Split-block designs are a type of experimental design in which two factors are applied independently in perpendicular directions. When only two factors are involved, Factor A is assigned to the whole plots in the same manner as in a traditional split-plot design. Factor B, however, is applied to strips that form another set of whole plots arranged orthogonally to those used for Factor A.
---

## Comparison with Related Designs

| Feature | CRD / RCBD | Split Plot | Strip Plot |
|---|---|---|---|
| Hard-to-change factors | 0 | 1 (whole plot) | 2 (row + column) |
| Nesting structure | None | Subplot nested in whole plot | No nesting — crossed |
| Number of error terms | 1 | 2 | 3 |
| Precision: main effect A | High | Low | Medium |
| Precision: main effect B | High | High | Medium |
| Precision: A×B interaction | High | High | **Highest** |
| Typical application | Lab / fully randomisable | One machine setting | Two large-scale operations |

---

## When to Use a Strip Plot Design

Use a strip plot design when:

1. **Both factors** are hard or expensive to change at the plot level (e.g., large machinery, irrigation systems, field operations).
2. The **interaction A×B** is of primary scientific interest.
3. You can accept somewhat **lower precision** on both main effects relative to a CRD.
4. Factors are naturally applied in **perpendicular directions** across a field or experimental area.

**Typical applications:**

| Field | Row Factor (A) | Column Factor (B) |
|---|---|---|
| Agronomy | Tillage method | Irrigation type |
| Food processing | Oven temperature | Packaging material |
| Textile | Dye bath | Fabric weave |
| Manufacturing | Machine line | Raw material supplier |
| Horticulture | Row spacing | Fertiliser formulation |

---

## Structure of the Strip Plot Design

### Layout Diagram (1 Block, $a = 3$ rows, $b = 4$ columns)

```
          ← Column Factor B →
          B1     B2     B3     B4
        ┌──────┬──────┬──────┬──────┐
  A1    │A1B1  │A1B2  │A1B3  │A1B4  │  ← Row strip for A1
        ├──────┼──────┼──────┼──────┤
  A2    │A2B1  │A2B2  │A2B3  │A2B4  │  ← Row strip for A2
        ├──────┼──────┼──────┼──────┤
  A3    │A3B1  │A3B2  │A3B3  │A3B4  │  ← Row strip for A3
        └──────┴──────┴──────┴──────┘
           ↑      ↑      ↑      ↑
         Col    Col    Col    Col
        strip  strip  strip  strip
        for B1 for B2 for B3 for B4
```

Each **cell** is an intersection plot receiving one level of A and one level of B.

### Randomisation

Within each block:
- Levels of **Factor A** are randomised among the row strips independently
- Levels of **Factor B** are randomised among the column strips independently
- The cell values are determined by which row and column strip intersect — **no further randomisation at the cell level**

### Multi-Block Layout ($r$ blocks)

```
Block 1            Block 2            Block 3
┌──┬──┬──┬──┐      ┌──┬──┬──┬──┐      ┌──┬──┬──┬──┐
│  │  │  │  │      │  │  │  │  │      │  │  │  │  │
├──┼──┼──┼──┤      ├──┼──┼──┼──┤      ├──┼──┼──┼──┤
│  │  │  │  │      │  │  │  │  │      │  │  │  │  │
├──┼──┼──┼──┤      ├──┼──┼──┼──┤      ├──┼──┼──┼──┤
│  │  │  │  │      │  │  │  │  │      │  │  │  │  │
└──┴──┴──┴──┘      └──┴──┴──┴──┘      └──┴──┴──┴──┘
  A randomised        A randomised        A randomised
  B randomised        B randomised        B randomised
  independently       independently       independently
```

---

## Statistical Model

$$
\begin{aligned}
Y_{ijk} =\;& \mu + \rho_i + \alpha_j + \delta_{ij} \\
&+ \beta_k + \gamma_{ik} + (\alpha\beta)_{jk} \\
&+ \varepsilon_{ijk}
\end{aligned}
$$

| Term | Description |
|---|---|
| $\mu$ | Overall mean |
| $\rho_i$ | Effect of block $i$ &nbsp;&nbsp;($i = 1, \ldots, r$) |
| $\alpha_j$ | Main effect of row factor A, level $j$ &nbsp;&nbsp;($j = 1, \ldots, a$) |
| $\delta_{ij}$ | **Row-strip error** — block × A interaction; error for testing A |
| $\beta_k$ | Main effect of column factor B, level $k$ &nbsp;&nbsp;($k = 1, \ldots, b$) |
| $\gamma_{ik}$ | **Column-strip error** — block × B interaction; error for testing B |
| $(\alpha\beta)_{jk}$ | A × B interaction effect |
| $\varepsilon_{ijk}$ | **Cell (intersection) error** — error for testing A×B |

> **Three separate error terms** are used in a strip plot ANOVA — one for each stratum of the design.

---

## ANOVA Table

| Source of Variation | df | MS | F-ratio | Error Term Used |
|---|---|---|---|---|
| **Block Stratum** | | | | |
| Blocks | $r - 1$ | $MS_{Blk}$ | — | — |
| **Row-Strip Stratum** | | | | |
| Factor A (rows) | $a - 1$ | $MS_A$ | $MS_A \,/\, MS_{EA}$ | Row-strip error |
| Row-strip error ($E_A$) | $(r-1)(a-1)$ | $MS_{EA}$ | — | — |
| **Column-Strip Stratum** | | | | |
| Factor B (columns) | $b - 1$ | $MS_B$ | $MS_B \,/\, MS_{EB}$ | Column-strip error |
| Column-strip error ($E_B$) | $(r-1)(b-1)$ | $MS_{EB}$ | — | — |
| **Intersection Stratum** | | | | |
| A × B | $(a-1)(b-1)$ | $MS_{AB}$ | $MS_{AB} \,/\, MS_{EC}$ | Cell error |
| Cell error ($E_C$) | $(r-1)(a-1)(b-1)$ | $MS_{EC}$ | — | — |
| **Total** | $rab - 1$ | | | |

### Degrees of Freedom Summary

$$
df_{EA} = (r-1)(a-1) \qquad df_{EB} = (r-1)(b-1)
$$

$$
df_{EC} = (r-1)(a-1)(b-1)
$$

### Precision Hierarchy

$$\text{Interaction (A×B)} > \text{Main effects (A, B)}$$

The interaction is tested against the smallest (cell) error, giving it the **highest precision** — an important practical advantage of the strip plot design.

---

## Relative Efficiency

The efficiency of estimating each effect relative to a completely randomised design (CRD) depends on the magnitudes of the three error variances:

$$\sigma^2_{EA} \geq \sigma^2_{EB} \geq \sigma^2_{EC} \quad \text{(typically)}$$

- Both main effects have **lower efficiency** than in a CRD (larger error denominators)
- The interaction has **higher efficiency** than in a CRD (smallest error denominator)
- This trade-off is acceptable when the interaction is the primary research question

---

## Worked Example

**Experiment:** Effect of **tillage method** (Factor A: conventional, reduced, zero) and **irrigation system** (Factor B: furrow, sprinkler, drip) on wheat yield (t/ha), in $r = 4$ blocks.

### Parameters

$$
r = 4,\quad a = 3,\quad b = 3
$$

$$
N = 4 \times 3 \times 3 = 36
$$

### Mean Yield Table (t/ha)

|  | B: Furrow | B: Sprinkler | B: Drip | **Row Mean** |
|---|---|---|---|---|
| A: Conventional | 3.8 | 4.6 | 5.2 | **4.53** |
| A: Reduced | 4.2 | 5.0 | 5.9 | **5.03** |
| A: Zero | 3.5 | 4.1 | 4.8 | **4.13** |
| **Col Mean** | **3.83** | **4.57** | **5.30** | **4.57** |

### Degrees of Freedom

| Source | df |
|---|---|
| Blocks | $4 - 1 = 3$ |
| A (Tillage) | $3 - 1 = 2$ |
| Row-strip error | $(4-1)(3-1) = 6$ |
| B (Irrigation) | $3 - 1 = 2$ |
| Column-strip error | $(4-1)(3-1) = 6$ |
| A × B | $(3-1)(3-1) = 4$ |
| Cell error | $(4-1)(3-1)(3-1) = 12$ |
| **Total** | **35** |

---

## Analysis in R

### Using `lme()` (nlme package)

```r
library(nlme)
library(emmeans)

# Data must have columns: block, row_factor, col_factor, yield
data <- read.csv("strip_plot_data.csv")
data$block      <- factor(data$block)
data$tillage    <- factor(data$tillage)
data$irrigation <- factor(data$irrigation)

# Fit strip plot model — three random effects strata
model <- lme(yield ~ tillage * irrigation,
             random = list(block = pdBlocked(list(
               pdIdent(~ 1),
               pdIdent(~ tillage - 1),
               pdIdent(~ irrigation - 1)
             ))),
             data = data,
             method = "REML")

summary(model)
anova(model)
```

### Using `aov()` with Error strata

```r
# Traditional aov approach with explicit Error() strata
model_aov <- aov(yield ~
                   tillage * irrigation +
                   Error(block +
                         block:tillage +
                         block:irrigation),
                 data = data)

summary(model_aov)
```

### Post-hoc Comparisons

```r
# Marginal means and pairwise comparisons
emmeans(model_aov, pairwise ~ tillage,    adjust = "tukey")
emmeans(model_aov, pairwise ~ irrigation, adjust = "tukey")

# Simple effects of B at each level of A
emmeans(model_aov, pairwise ~ irrigation | tillage, adjust = "tukey")

# Interaction plot
interaction.plot(
  x.factor     = data$irrigation,
  trace.factor  = data$tillage,
  response      = data$yield,
  col           = c("steelblue", "tomato", "forestgreen"),
  lwd           = 2,
  xlab          = "Irrigation System",
  ylab          = "Mean Yield (t/ha)",
  trace.label   = "Tillage"
)
```

---

## Analysis in SAS

```sas
/* Strip plot design using PROC MIXED */
proc mixed data=strip_plot;
  class block tillage irrigation;
  model yield = tillage irrigation tillage*irrigation / ddfm=satterth;
  /* Three random effects — one per stratum */
  random block;
  random block*tillage;
  random block*irrigation;
  /* Interaction comparisons */
  lsmeans tillage*irrigation / pdiff slice=tillage adjust=tukey;
run;

/* Interaction plot */
proc sgplot data=strip_plot;
  series x=irrigation y=yield / group=tillage markers lineattrs=(thickness=2);
  xaxis label="Irrigation System";
  yaxis label="Mean Yield (t/ha)";
  keylegend / title="Tillage Method";
run;
```

---

## Assumptions

1. **Normality** — Residuals within each stratum are approximately normally distributed.
2. **Homogeneity of variance** — Equal variance within row strips, column strips, and cells.
3. **Independence** — Blocks are independent; randomisation is carried out correctly within each block.
4. **Correct error terms** — Factor A tested against row-strip error; Factor B against column-strip error; A×B against cell error. Using a single pooled error is **incorrect** and leads to biased F-tests.
5. **Additivity of block effects** — Blocks affect all treatment combinations equally (no block × treatment interaction beyond the defined strata).

---

## Advantages and Disadvantages

### Advantages ✓

- Accommodates **two hard-to-change factors** in the same experiment
- Provides **maximum precision for the interaction** A×B — the effect most relevant when both factors are of interest
- Operationally efficient — Factor A applied in strips, Factor B applied in perpendicular strips, reducing factor-level changes
- Straightforward field layout — rows and columns are natural physical divisions
- Reduces **total operational cost** compared to a fully randomised two-factor experiment

### Disadvantages ✗

- **Lower precision** for both main effects compared to CRD or RCBD
- **Three error terms** complicate the analysis; standard ANOVA software must be used carefully
- **Small degrees of freedom** for row-strip and column-strip errors, especially with few blocks
- Missing data are difficult to handle without mixed-model software
- Less familiar than split plot; risk of **misidentifying the error structure**

---

## Comparison: Split Plot vs Strip Plot

| Aspect | Split Plot | Strip Plot |
|---|---|---|
| Factor A randomisation | Among whole plots | Among row strips within blocks |
| Factor B randomisation | Within each whole plot | Among column strips within blocks |
| Nesting | B nested within A | A and B crossed (not nested) |
| Error terms | 2 | 3 |
| Precision for A | Low | Medium |
| Precision for B | High | Medium |
| Precision for A×B | High | **Highest** |
| Use when | Only A is hard to change | **Both** A and B are hard to change |

---

## Extensions

| Extension | Description |
|---|---|
| **Strip-Split Plot** | A third factor added as subplots within intersection cells |
| **Replicated Strip Plot** | Multiple blocks increase df for row- and column-strip errors |
| **Unbalanced Strip Plot** | Missing cells handled via REML mixed model |
| **Strip Plot in Space–Time** | One factor varied across space, another across time (repeated measures analogue) |
| **Strip Plot with Covariates** | ANCOVA model includes plot-level covariates to reduce residual error |

---

## Glossary

| Term | Definition |
|---|---|
| **Row factor** | Factor applied to horizontal strips spanning the full width of a block |
| **Column factor** | Factor applied to vertical strips spanning the full height of a block |
| **Intersection plot** | The experimental unit formed at the crossing of one row strip and one column strip |
| **Row-strip error** | Variability among row strips within a block; denominator for testing Factor A |
| **Column-strip error** | Variability among column strips within a block; denominator for testing Factor B |
| **Cell error** | Residual variability at the intersection level; denominator for testing A×B |
| **Criss-cross design** | Alternative name for the strip plot design |
| **Stratum** | A level of the hierarchical error structure (block, row strip, column strip, cell) |

---

## References

1. Montgomery, D.C. (2017). *Design and Analysis of Experiments* (9th ed.). Wiley.
2. Cochran, W.G. & Cox, G.M. (1957). *Experimental Designs* (2nd ed.). Wiley.
3. Federer, W.T. (1955). *Experimental Design: Theory and Application*. Macmillan.
4. Littell, R.C., Milliken, G.A., Stroup, W.W., Wolfinger, R.D., & Schabenberger, O. (2006). *SAS for Mixed Models* (2nd ed.). SAS Institute.
5. Piepho, H.P., Büchse, A., & Emrich, K. (2003). A Hitchhiker's Guide to Mixed Models for Randomized Experiments. *Journal of Agronomy and Crop Science*, 189(5), 310–322.

---]]></content><author><name>Dr. M. Shamshad</name><email>shamshad.rattan@gmail.com</email></author><category term="experimental-design" /><category term="statistics" /><category term="agriculture" /><category term="strip-plot" /><category term="criss-cross" /><category term="anova" /><category term="whole-plot" /><category term="row-factor" /><category term="column-factor" /><category term="blocking" /><category term="mixed-models" /><category term="split-plot" /><summary type="html"><![CDATA[A Strip Plot Design (also called a Criss-Cross Design) is an experimental layout used when two factors are both difficult or costly to randomise at the level of individual plots. It is a natural extension of the split plot concept but treats both factors symmetrically — neither is nested within the other. In a strip plot: Factor A (the row factor) is applied to horizontal strips running across each block Factor B (the column factor) is applied to vertical strips running down each block The intersection of a row strip and a column strip forms the experimental unit for the interaction A×B]]></summary></entry><entry><title type="html">Split Plot Design</title><link href="https://shamshad.in/posts/2026/05/split-plot-design/" rel="alternate" type="text/html" title="Split Plot Design" /><published>2026-05-13T00:00:00+05:30</published><updated>2026-05-23T20:31:21+05:30</updated><id>https://shamshad.in/posts/2026/05/split-plot-design</id><content type="html" xml:base="https://shamshad.in/posts/2026/05/split-plot-design/"><![CDATA[A **Split Plot Design** is a type of experimental design used when one or more factors are difficult or expensive to randomize at the level of individual experimental units. It is widely used in agricultural field trials, industrial experiments, and manufacturing studies.

The design partitions experimental units into two levels:

- **Whole plots** — the larger units to which the hard-to-change factor (the *whole-plot factor*) is applied
- **Subplots** — subdivisions of whole plots, to which the easy-to-change factor (the *subplot factor*) is applied
<!--more-->
## What is a Split-Plot Design?
A split-plot design is an experimental arrangement that uses two levels of randomization. In this method, one factor is randomly applied to large experimental units known as whole plots called main plot. These whole plots are further divided into smaller sections called sub-plots, where the levels of a second factor are assigned randomly.

---

## When to Use a Split Plot Design

Use a split plot design when:

1. One factor is difficult, costly, or impractical to change for every run (e.g., oven temperature, tillage method, irrigation system).
2. You want to study both a hard-to-change factor and an easy-to-change factor in the same experiment.
3. You can accept lower precision for the whole-plot factor in exchange for higher precision on the subplot factor.

**Typical applications:**

| Field | Whole-Plot Factor | Sub-Plot Factor |
|---|---|---|
| Agriculture | Irrigation method | Fertilizer type |
| Food Science | Oven temperature | Packaging type |
| Manufacturing | Machine setting | Raw material batch |
| Textile | Dyeing temperature | Fabric type |
| Agronomy | Tillage method | Crop variety |

---

## Structure of a Split Plot Design

### Components

```
Experiment
├── Block 1
│   ├── Whole Plot A  →  Subplot: level 1, level 2, level 3
│   └── Whole Plot B  →  Subplot: level 1, level 2, level 3
├── Block 2
│   ├── Whole Plot A  →  Subplot: level 1, level 2, level 3
│   └── Whole Plot B  →  Subplot: level 1, level 2, level 3
└── Block 3
    ├── Whole Plot A  →  Subplot: level 1, level 2, level 3
    └── Whole Plot B  →  Subplot: level 1, level 2, level 3
```

### Notation

| Symbol | Meaning |
|---|---|
| $r$ | Number of blocks (replications) |
| $a$ | Number of whole-plot factor levels |
| $b$ | Number of subplot factor levels |
| $N = r \times a \times b$ | Total number of observations |

---

## Statistical Model

The linear model for a split plot design (with blocks) is:

$$
Y_{ijk} = \mu + \rho_i + \alpha_j + \delta_{ij} + \beta_k + (\alpha\beta)_{jk} + \varepsilon_{ijk}
$$

Where:

| Term | Description |
|---|---|
| $\mu$ | Overall mean |
| $\rho_i$ | Effect of the $i$-th block ($i = 1, \ldots, r$) |
| $\alpha_j$ | Effect of the $j$-th whole-plot factor level ($j = 1, \ldots, a$) |
| $\delta_{ij}$ | Whole-plot error (block × whole-plot interaction) |
| $\beta_k$ | Effect of the $k$-th subplot factor level ($k = 1, \ldots, b$) |
| $(\alpha\beta)_{jk}$ | Interaction between whole-plot and subplot factors |
| $\varepsilon_{ijk}$ | Subplot error (residual) |

> **Note:** There are **two error terms** in a split plot design — one for testing whole-plot effects and one for testing subplot effects and interactions.

---

## ANOVA Table

| Source of Variation | df | MS | F-ratio |
|---|---|---|---|
| **Whole-Plot Stratum** | | | |
| Blocks | $r - 1$ | $MS_{Block}$ | — |
| Whole-plot factor (A) | $a - 1$ | $MS_A$ | $MS_A / MS_{WPE}$ |
| Whole-plot error | $(r-1)(a-1)$ | $MS_{WPE}$ | — |
| **Subplot Stratum** | | | |
| Subplot factor (B) | $b - 1$ | $MS_B$ | $MS_B / MS_{SPE}$ |
| A × B interaction | $(a-1)(b-1)$ | $MS_{AB}$ | $MS_{AB} / MS_{SPE}$ |
| Subplot error | $a(r-1)(b-1)$ | $MS_{SPE}$ | — |
| **Total** | $rab - 1$ | | |

### Key Rules

- Test **whole-plot factor A** using the **whole-plot error** as denominator.
- Test **subplot factor B** and **A×B interaction** using the **subplot error** as denominator.
- The subplot error is typically **smaller** than the whole-plot error, giving more power to detect subplot and interaction effects.

---

## Worked Example

**Experiment:** Effect of **irrigation method** (Factor A: furrow, drip) and **nitrogen level** (Factor B: low, medium, high) on wheat yield, conducted in 3 blocks.

### Parameters

$$r = 3,\quad a = 2,\quad b = 3,\quad N = 3 \times 2 \times 3 = 18$$

### Randomisation Plan

```
Block 1:
  WP-1 (Furrow):  N-Low  |  N-Med  |  N-High
  WP-2 (Drip):    N-Med  |  N-High |  N-Low

Block 2:
  WP-1 (Drip):    N-High |  N-Low  |  N-Med
  WP-2 (Furrow):  N-Low  |  N-High |  N-Med

Block 3:
  WP-1 (Furrow):  N-Med  |  N-Low  |  N-High
  WP-2 (Drip):    N-High |  N-Med  |  N-Low
```

> Within each whole plot, the order of subplot treatments is **randomised independently**.

---

## Assumptions

For valid inference, the following assumptions must hold:

1. **Normality** — Residuals at both whole-plot and subplot levels are approximately normally distributed.
2. **Homogeneity of variance** — Variances are equal within strata.
3. **Independence** — Whole plots are independent of each other.
4. **Correct error term** — Whole-plot factor is tested against whole-plot error, not subplot error.

Violating assumption 4 (a common mistake when using standard one-error ANOVA software) leads to **anti-conservative tests** for whole-plot factors.

---

## Analysis in R

```r
library(nlme)
library(lme4)

# Load data
data <- read.csv("split_plot_data.csv")

# Fit split plot model using lme (nlme package)
model <- lme(yield ~ irrigation * nitrogen,
             random = ~1 | block/whole_plot,
             data = data,
             method = "REML")

# ANOVA table
anova(model)

# Summary
summary(model)

# Pairwise comparisons (emmeans)
library(emmeans)
emmeans(model, pairwise ~ nitrogen | irrigation)
```

### Using `aov()` (base R)

```r
# Traditional aov approach
model_aov <- aov(yield ~ irrigation * nitrogen +
                   Error(block/irrigation),
                 data = data)

summary(model_aov)
```

> **Tip:** Prefer `lme()` or `lmer()` for unbalanced data or when variance components are of interest.

---

## Analysis in SAS

```sas
proc mixed data=split_plot;
  class block irrigation nitrogen;
  model yield = irrigation nitrogen irrigation*nitrogen / ddfm=satterth;
  random block block*irrigation;
  lsmeans irrigation*nitrogen / pdiff adjust=tukey;
run;
```

---

## Advantages and Disadvantages

### Advantages ✓

- Accommodates **hard-to-change factors** without an impractically large number of factor-level changes.
- Provides **high precision** for subplot factor and interaction comparisons.
- Reduces experimental effort compared to a completely randomised design with the same number of factor combinations.
- Naturally suits many **agricultural and industrial** scenarios.

### Disadvantages ✗

- **Lower precision** for the whole-plot factor compared to the subplot factor.
- More **complex analysis** — two error terms required.
- **Unbalanced data** (missing observations) complicates analysis.
- Software that ignores the split-plot structure produces **incorrect F-tests** for whole-plot effects.

---

## Extensions

| Extension | Description |
|---|---|
| **Split-Split Plot** | A third factor added as sub-subplots within subplots |
| **Strip Plot (Criss-Cross)** | Both factors applied in perpendicular strips; no nesting |
| **Repeated Measures** | Time as the subplot factor; observations correlated within subject |
| **Unbalanced Split Plot** | Handled with mixed model (REML) when cells are missing |
| **Split Plot in RCBD** | Each block contains one replicate of all whole-plot treatments |

---

## References

1. Montgomery, D.C. (2017). *Design and Analysis of Experiments* (9th ed.). Wiley.
2. Cochran, W.G. & Cox, G.M. (1957). *Experimental Designs* (2nd ed.). Wiley.
3. Littell, R.C., Milliken, G.A., Stroup, W.W., Wolfinger, R.D., & Schabenberger, O. (2006). *SAS for Mixed Models* (2nd ed.). SAS Institute.
4. Pinheiro, J.C. & Bates, D.M. (2000). *Mixed-Effects Models in S and S-PLUS*. Springer.
5. Stroup, W.W. (2012). *Generalized Linear Mixed Models*. CRC Press.

---]]></content><author><name>Dr. M. Shamshad</name><email>shamshad.rattan@gmail.com</email></author><category term="experimental-design" /><category term="statistics" /><category term="agriculture" /><category term="anova" /><category term="split-plot" /><category term="whole-plot" /><category term="subplot" /><category term="blocking" /><category term="mixed-models" /><summary type="html"><![CDATA[A Split Plot Design is a type of experimental design used when one or more factors are difficult or expensive to randomize at the level of individual experimental units. It is widely used in agricultural field trials, industrial experiments, and manufacturing studies. The design partitions experimental units into two levels: Whole plots — the larger units to which the hard-to-change factor (the whole-plot factor) is applied Subplots — subdivisions of whole plots, to which the easy-to-change factor (the subplot factor) is applied]]></summary></entry><entry><title type="html">Factorial Experimental Design</title><link href="https://shamshad.in/posts/2026/05/factorial-experimental-design/" rel="alternate" type="text/html" title="Factorial Experimental Design" /><published>2026-05-12T00:00:00+05:30</published><updated>2026-05-31T19:27:14+05:30</updated><id>https://shamshad.in/posts/2026/05/factorial-experimental-design</id><content type="html" xml:base="https://shamshad.in/posts/2026/05/factorial-experimental-design/"><![CDATA[A **Factorial Experimental Design** is an experimental strategy in which **two or more factors are varied simultaneously**, and all possible combinations of their levels are studied. This allows researchers to:

- Estimate the **main effect** of each factor
- Detect **interactions** between factors
- Draw conclusions over a wide range of conditions
- Use experimental resources more efficiently than one-factor-at-a-time (OFAT) experiments
<!--more-->
>**Key principle:** In a factorial design, every factor combination appears in the experiment — making it possible to separate the independent effect of each factor *and* assess how factors influence each other.

## Why Not One-Factor-at-a-Time (OFAT)?

The traditional approach varies one factor while holding all others constant. This is inefficient and misleading:

| Property | OFAT | Factorial |
|---|---|---|
| Detects interactions | ✗ No | ✓ Yes |
| Efficient use of runs | ✗ No | ✓ Yes |
| Conclusions generalisable | ✗ Limited | ✓ Broad |
| Same information per run | ✗ Less | ✓ More |

**Example:** If Factor A affects yield differently depending on the level of Factor B, an OFAT experiment will never detect this — but a factorial design will.

---

## Types of Factorial Designs

| Design | Description |
|---|---|
| **Full Factorial** | All combinations of all factor levels are tested |
| **$2^k$ Factorial** | $k$ factors each at 2 levels (low/high) |
| **$3^k$ Factorial** | $k$ factors each at 3 levels |
| **$p^k$ General Factorial** | $k$ factors each at $p$ levels |
| **Fractional Factorial** | A carefully chosen fraction of the full factorial |
| **Mixed-Level Factorial** | Factors at different numbers of levels |

---

## Full Factorial Design

### Definition

A **full factorial design** with $k$ factors, factor $i$ having $l_i$ levels, produces:

$$N = l_1 \times l_2 \times \cdots \times l_k$$

total treatment combinations (ignoring replication).

### Example: $2 \times 3$ Factorial

Two factors: **Temperature** (Low, High) and **Concentration** (10%, 20%, 30%).

$$N = 2 \times 3 = 6 \text{ treatment combinations}$$

| Run | Temperature | Concentration |
|---|---|---|
| 1 | Low | 10% |
| 2 | Low | 20% |
| 3 | Low | 30% |
| 4 | High | 10% |
| 5 | High | 20% |
| 6 | High | 30% |

Each combination is replicated $r$ times → Total runs $= 6r$.

---

## The $2^k$ Factorial Design
![Two_factorial_RCBD]({{ "teaching/designs/RCBD_twofactor.png" | relative_url }})

The most widely used factorial structure. Each of $k$ factors is set at exactly **two levels**, coded as:

$$-1 = \text{low level}, \quad +1 = \text{high level}$$

Total runs (before replication): $2^k$

| $k$ (Factors) | Runs ($2^k$) |
|---|---|
| 2 | 4 |
| 3 | 8 |
| 4 | 16 |
| 5 | 32 |
| 6 | 64 |

### Design Matrix: $2^2$ Factorial

| Run | A | B | AB |
|---|---|---|---|
| 1 | − | − | + |
| 2 | + | − | − |
| 3 | − | + | − |
| 4 | + | + | + |

Columns A and B are the **main effect** columns; AB is the **interaction** column (element-wise product).

### Design Matrix: $2^3$ Factorial


| Run | A | B | C | AB | AC | BC | ABC |
|---|---|---|---|---|---|---|---|
| 1 | − | − | − | + | + | + | − |
| 2 | + | − | − | − | − | + | + |
| 3 | − | + | − | − | + | − | + |
| 4 | + | + | − | + | − | − | − |
| 5 | − | − | + | + | − | − | + |
| 6 | + | − | + | − | + | − | − |
| 7 | − | + | + | − | − | + | − |
| 8 | + | + | + | + | + | + | + |

---

## Statistical Model

### Two-Factor Factorial Model

$$Y_{ijk} = \mu + \alpha_i + \beta_j + (\alpha\beta)_{ij} + \varepsilon_{ijk}$$

| Term | Meaning |
|---|---|
| $\mu$ | Overall mean |
| $\alpha_i$ | Main effect of Factor A, level $i$ |
| $\beta_j$ | Main effect of Factor B, level $j$ |
| $(\alpha\beta)_{ij}$ | Interaction effect of A and B |
| $\varepsilon_{ijk}$ | Random error, $\varepsilon \sim N(0, \sigma^2)$ |

### Three-Factor Factorial Model

$$
\begin{aligned}
Y_{ijkl} =\;& \mu + \alpha_i + \beta_j + \gamma_k \\
&+ (\alpha\beta)_{ij} + (\alpha\gamma)_{ik} \\
&+ (\beta\gamma)_{jk} + (\alpha\beta\gamma)_{ijk} \\
&+ \varepsilon_{ijkl}
\end{aligned}
$$

The number of effects grows rapidly with $k$:

| $k$ | Main Effects | 2FI | 3FI | Total Effects |
|---|---|---|---|---|
| 2 | 2 | 1 | — | 3 |
| 3 | 3 | 3 | 1 | 7 |
| 4 | 4 | 6 | 4 | 15 |
| 5 | 5 | 10 | 10 | 31 |

---

## ANOVA Table

### Two-Factor Factorial ($a$ levels of A, $b$ levels of B, $r$ replicates)

| Source | df | SS | MS | F |
|---|---|---|---|---|
| Factor A | $a - 1$ | $SS_A$ | $MS_A$ | $MS_A / MS_E$ |
| Factor B | $b - 1$ | $SS_B$ | $MS_B$ | $MS_B / MS_E$ |
| A × B | $(a-1)(b-1)$ | $SS_{AB}$ | $MS_{AB}$ | $MS_{AB} / MS_E$ |
| Error | $ab(r-1)$ | $SS_E$ | $MS_E$ | — |
| **Total** | $abr - 1$ | $SS_T$ | | |

### Sums of Squares Formulae

$$
\begin{aligned}
SS_A &= \frac{br \sum_{i} \bar{Y}_{i..}^2 - N\bar{Y}_{...}^2}{1} \\
SS_B &= \frac{ar \sum_{j} \bar{Y}_{.j.}^2 - N\bar{Y}_{...}^2}{1}
\end{aligned}
$$

$$
\begin{aligned}
SS_{AB} &= SS_{Cells} - SS_A - SS_B \\
SS_T &= \sum_{i,j,k} Y_{ijk}^2 - N\bar{Y}_{...}^2
\end{aligned}
$$

---

## Effect Estimation in $2^k$ Designs

Effects are estimated as **contrasts** of treatment means.

### Main Effect of A

$$\hat{A} = \frac{1}{2^{k-1}} \sum_{\text{runs}} c_i Y_i$$

where $c_i \in \{-1, +1\}$ are the contrast coefficients from the design matrix.

### Two-Factor Interaction AB

$$\widehat{AB} = \frac{1}{2^{k-1}} \sum_{\text{runs}} (c_A \cdot c_B)_i \, Y_i$$

> **Rule of thumb (Hierarchical Principle):** If an interaction is significant, retain all lower-order terms (main effects) in the model, even if they are not individually significant.

---

## Interaction Effects

An **interaction** between two factors means the effect of one factor depends on the level of the other.

### No Interaction

```
         B = Low   B = High
A = Low    10        20       (Effect of B = +10 at both levels of A)
A = High   15        25
```

Parallel lines in an interaction plot → **no interaction**.

### Interaction Present

```
         B = Low   B = High
A = Low    10        20       (Effect of B = +10)
A = High   25        15       (Effect of B = −10)
```

Crossing or non-parallel lines → **interaction present**.

> When a significant interaction is present, main effects must be interpreted **conditionally** — report simple effects of each factor at each level of the other factor.

---

## Worked Example

**Experiment:** Effect of **Fertiliser type** (A: Organic, Inorganic) and **Irrigation level** (B: Low, Medium, High) on crop yield (t/ha), with $r = 3$ replicates.

### Parameters

$$
a = 2,\quad b = 3,\quad r = 3
$$

$$
N = 2 \times 3 \times 3 = 18
$$

### Mean Yields (t/ha)

| | B: Low | B: Medium | B: High | Row Mean |
|---|---|---|---|---|
| A: Organic | 3.2 | 4.5 | 5.1 | 4.27 |
| A: Inorganic | 4.0 | 5.8 | 6.3 | 5.37 |
| Column Mean | 3.6 | 5.15 | 5.7 | **4.82** |

### Degrees of Freedom

| Source | df |
|---|---|
| Factor A (Fertiliser) | $2 - 1 = 1$ |
| Factor B (Irrigation) | $3 - 1 = 2$ |
| A × B | $1 \times 2 = 2$ |
| Error | $6 \times 2 = 12$ |
| Total | $17$ |

---

## Analysis in R

### Full Factorial ANOVA

```r
# Load data
data <- read.csv("factorial_data.csv")
data$fertiliser <- factor(data$fertiliser)
data$irrigation <- factor(data$irrigation)

# Fit model
model <- aov(yield ~ fertiliser * irrigation, data = data)

# ANOVA table
summary(model)

# Check model assumptions
par(mfrow = c(2, 2))
plot(model)

# Interaction plot
interaction.plot(data$irrigation, data$fertiliser, data$yield,
                 col = c("steelblue", "tomato"),
                 xlab = "Irrigation Level",
                 ylab = "Mean Yield (t/ha)",
                 trace.label = "Fertiliser")
```

### Post-hoc Comparisons

```r
library(emmeans)

# Marginal means
emmeans(model, ~ fertiliser)
emmeans(model, ~ irrigation)

# Simple effects (conditional on interaction being significant)
emmeans(model, pairwise ~ irrigation | fertiliser, adjust = "tukey")
```

### $2^k$ Design Analysis

```r
library(FrF2)

# Generate a 2^3 factorial design
design <- FrF2(nruns = 8, nfactors = 3,
               factor.names = list(A = c(-1, 1),
                                   B = c(-1, 1),
                                   C = c(-1, 1)))

# After adding response column:
model_2k <- lm(yield ~ A * B * C, data = design_with_response)
summary(model_2k)

# Half-normal plot of effects
library(unrepx)
hnplot(coef(model_2k)[-1], method = "Lenth")
```

---

## Analysis in SAS

```sas
/* Two-factor factorial ANOVA */
proc glm data=factorial_data;
  class fertiliser irrigation;
  model yield = fertiliser | irrigation;
  means fertiliser irrigation / tukey;
  lsmeans fertiliser*irrigation / pdiff slice=fertiliser adjust=tukey;
run;

/* Interaction plot */
proc sgplot data=factorial_data;
  series x=irrigation y=yield / group=fertiliser markers;
  xaxis label="Irrigation Level";
  yaxis label="Mean Yield (t/ha)";
run;
```

---

## Fractional Factorial Design

When $k$ is large, a full $2^k$ design requires too many runs. A **$2^{k-p}$ fractional factorial** uses only $1/2^p$ of the full design, sacrificing the ability to estimate some high-order interactions.

### Resolution

| Resolution | Property |
|---|---|
| **III** | Main effects aliased with 2FIs; 2FIs aliased with each other |
| **IV** | Main effects clear of 2FIs; 2FIs aliased with each other |
| **V** | Main effects and 2FIs clear; 3FIs aliased with 2FIs |

> **Principle of effect sparsity (Pareto principle):** In most real systems, a small number of main effects and low-order interactions dominate. Higher-order effects are usually negligible — making fractional designs practically valid.

### Common Fractional Designs

| Design | Runs | Factors | Resolution |
|---|---|---|---|
| $2^{3-1}$ | 4 | 3 | III |
| $2^{4-1}$ | 8 | 4 | IV |
| $2^{5-2}$ | 8 | 5 | III |
| $2^{6-2}$ | 16 | 6 | IV |
| $2^{7-4}$ | 8 | 7 | III |

---

## Assumptions

1. **Normality** — Residuals are approximately normally distributed. Check with Q–Q plot.
2. **Homogeneity of variance** — Equal variances across all cells. Check with Levene's test or residual vs. fitted plot.
3. **Independence** — Observations are independent. Ensured by proper randomisation.
4. **Additivity** — The model correctly specifies all relevant interaction terms.
5. **Fixed effects** — Unless factors are treated as random, all factor levels are fixed and chosen deliberately.

---

## Advantages and Disadvantages

### Advantages ✓

- Estimates **all main effects and interactions** simultaneously
- More **statistically efficient** than OFAT — same information from fewer runs
- Detects **synergistic or antagonistic interactions** between factors
- Conclusions hold across the **full experimental region**
- Forms the basis for **response surface methodology (RSM)**

### Disadvantages ✗

- Number of runs grows **exponentially** with factors: $2^{10} = 1024$ runs for 10 factors
- **High-order interactions** (3FI and above) are difficult to interpret
- Requires **adequate replication** to estimate error
- May be impractical when factor-level changes are costly (consider split plot instead)

---

## Relationship to Other Designs

| Design | Relation to Factorial |
|---|---|
| **RCBD** | Single-factor; factorial adds more factors within blocks |
| **Split Plot** | Factorial with restricted randomisation |
| **Response Surface (CCD, BBD)** | Factorial augmented with centre and axial points |
| **Taguchi Arrays** | Fractional factorials with emphasis on noise factors |
| **Latin Square** | Two blocking factors; factorial adds treatment combinations |

---

## Glossary

| Term | Definition |
|---|---|
| **Main effect** | Average change in response when a factor moves from low to high level |
| **Interaction** | Change in the effect of one factor depending on the level of another |
| **Treatment combination** | Specific set of levels of all factors in one run |
| **Replication** | Independent repetition of a treatment combination |
| **Alias** | Two effects that cannot be estimated separately (fractional designs) |
| **Resolution** | Measure of which effects are aliased in a fractional design |
| **Contrast** | Linear combination of treatment means used to estimate an effect |
| **Effect sparsity** | Assumption that few effects are large; most are negligible |

---

## References

1. Montgomery, D.C. (2017). *Design and Analysis of Experiments* (9th ed.). Wiley.
2. Box, G.E.P., Hunter, J.S., & Hunter, W.G. (2005). *Statistics for Experimenters* (2nd ed.). Wiley.
3. Wu, C.F.J. & Hamada, M.S. (2021). *Experiments: Planning, Analysis, and Optimization* (3rd ed.). Wiley.
4. Dean, A., Voss, D., & Draguljić, D. (2017). *Design and Analysis of Experiments* (2nd ed.). Springer.
5. Myers, R.H., Montgomery, D.C., & Anderson-Cook, C.M. (2016). *Response Surface Methodology* (4th ed.). Wiley.

---]]></content><author><name>Dr. M. Shamshad</name><email>shamshad.rattan@gmail.com</email></author><category term="experimental-design" /><category term="statistics" /><category term="factorial" /><category term="anova" /><category term="interaction" /><category term="two-way" /><category term="three-way" /><category term="full-factorial" /><category term="fractional-factorial" /><category term="main-effects" /><category term="2k-design" /><summary type="html"><![CDATA[A Factorial Experimental Design is an experimental strategy in which two or more factors are varied simultaneously, and all possible combinations of their levels are studied. This allows researchers to: Estimate the main effect of each factor Detect interactions between factors Draw conclusions over a wide range of conditions Use experimental resources more efficiently than one-factor-at-a-time (OFAT) experiments]]></summary></entry><entry><title type="html">Honeycomb Design Analysis in R</title><link href="https://shamshad.in/posts/2026/05/honeycomb-design-analysis/" rel="alternate" type="text/html" title="Honeycomb Design Analysis in R" /><published>2026-05-04T00:00:00+05:30</published><updated>2026-05-10T14:53:38+05:30</updated><id>https://shamshad.in/posts/2026/05/honeycomb-design-analysis</id><content type="html" xml:base="https://shamshad.in/posts/2026/05/honeycomb-design-analysis/"><![CDATA[The Honeycomb (HC) design, developed by **Fasoulas (1988)** and later extended by **Kyriakou and Fasoulas**, is a field layout method used in plant breeding to improve the efficiency of mass selection under field variability. In this design, plants are arranged in a **triangular (hexagonal) grid**, so that each plant is surrounded by exactly six nearest neighbours at equal distances. This uniform spatial arrangement ensures that every plant experiences a similar level of competition, reducing environmental bias caused by uneven spacing or directional field effects.
<!--more-->
The main idea of the HC design is to enhance the accuracy of selecting superior plants by controlling local competition and micro-environmental variation without requiring heavy replication. Because each plant is compared primarily with its immediate neighbours, breeders can better distinguish genetic performance from environmental noise. This makes the design particularly useful in early-generation selection, where large populations are evaluated and only a small number of superior individuals are retained for further breeding.
{: .txt-justify}

### Design Principle

Each plant $i$ is compared against the mean of its **moving ring of 6 neighbours**. A selection
index is computed as:

$$HC_i = \frac{y_i}{\bar{y}_{N_i}}$$

where $y_i$ is the yield of plant $i$ and $\bar{y}_{N_i}$ is the mean yield of its 6 neighbours.

### Layout Generation

```r
# ── Honeycomb grid coordinates ────────────────────────────────────────────
honeycomb_coords <- function(nrow, ncol) {
  coords <- data.frame(Plant = integer(), X = numeric(), Y = numeric())
  id <- 1L
  for (r in 1:nrow) {
    for (c in 1:ncol) {
      x <- c + ifelse(r %% 2 == 0, 0.5, 0)
      y <- r * (sqrt(3) / 2)
      coords <- rbind(coords, data.frame(Plant = id, X = x, Y = y))
      id <- id + 1L
    }
  }
  coords
}

hc_grid <- honeycomb_coords(20, 15)

# Simulate plant yields
set.seed(99)
hc_grid$Yield <- rnorm(nrow(hc_grid), mean = 50, sd = 8)

# Visualise layout
ggplot(hc_grid, aes(X, Y, colour = Yield)) +
  geom_point(size = 3, shape = 16) +
  scale_colour_viridis_c(option = "plasma") +
  coord_equal() +
  labs(title = "Honeycomb Field Layout", colour = "Yield (g)") +
  theme_minimal(base_size = 13)
```

### Neighbour Identification & Selection Index

```r
# ── Find 6 nearest neighbours ─────────────────────────────────────────────
library(FNN)   # fast kNN

coords_mat <- as.matrix(hc_grid[, c("X", "Y")])
nn_idx     <- get.knnx(coords_mat, coords_mat, k = 7)$nn.index
# Column 1 is the point itself; columns 2–7 are the 6 neighbours

hc_grid$NeighbourMean <- apply(nn_idx[, 2:7], 1, function(idx) {
  mean(hc_grid$Yield[idx])
})

# HC selection index
hc_grid$HC_index <- hc_grid$Yield / hc_grid$NeighbourMean

# Select top 10 % (strongest relative performers)
threshold   <- quantile(hc_grid$HC_index, 0.90)
hc_grid$Selected <- hc_grid$HC_index >= threshold

cat("Plants selected:", sum(hc_grid$Selected), "\n")
cat("Mean yield – selected:    ", round(mean(hc_grid$Yield[hc_grid$Selected]),  2), "\n")
cat("Mean yield – not selected:", round(mean(hc_grid$Yield[!hc_grid$Selected]), 2), "\n")
```

### Visualise Selection

```r
ggplot(hc_grid, aes(X, Y, colour = HC_index, shape = Selected, size = Selected)) +
  geom_point() +
  scale_colour_viridis_c(option = "magma") +
  scale_shape_manual(values = c(`FALSE` = 16, `TRUE` = 17)) +
  scale_size_manual( values  = c(`FALSE` = 2,  `TRUE` = 4))  +
  coord_equal() +
  labs(title  = "Honeycomb Selection (top 10 %)",
       colour = "HC Index",
       shape  = "Selected",
       size   = "Selected") +
  theme_minimal(base_size = 13)
```

### Heritability Estimate Under Honeycomb Design

```r
# ── Plant-level heritability (Fasoulas method) ────────────────────────────
# Var(genotype) estimated via regression of plant on neighbour mean
lm_hc  <- lm(Yield ~ NeighbourMean, data = hc_grid)
h2_hc  <- summary(lm_hc)$r.squared
cat("Heritability estimate (HC):", round(h2_hc, 3), "\n")
```
---]]></content><author><name>Dr. M. Shamshad</name><email>shamshad.rattan@gmail.com</email></author><category term="r" /><category term="geometry" /><category term="structural-analysis" /><category term="data-visualization" /><category term="r" /><category term="ggplot2" /><category term="hexagon" /><category term="tessellation" /><category term="honeycomb" /><category term="geometry" /><category term="structural-mechanics" /><summary type="html"><![CDATA[A comprehensive guide to honeycomb hexagonal lattice design analysis in R — covering geometry, structural efficiency, tessellation plotting, and Gibson–Ashby mechanics.]]></summary></entry><entry><title type="html">Spatial Analysis with AR1 × AR1 Model: Theory &amp;amp; Complete R Analysis</title><link href="https://shamshad.in/posts/2026/05/spatial-ar1-analysis/" rel="alternate" type="text/html" title="Spatial Analysis with AR1 × AR1 Model: Theory &amp;amp; Complete R Analysis" /><published>2026-05-02T00:00:00+05:30</published><updated>2026-05-10T14:53:38+05:30</updated><id>https://shamshad.in/posts/2026/05/spatial-ar1-analysis</id><content type="html" xml:base="https://shamshad.in/posts/2026/05/spatial-ar1-analysis/"><![CDATA[Field experiments are routinely affected by **spatial heterogeneity** — systematic
variation in soil fertility, moisture, drainage, pH, and microclimate that creates
patches of high and low performance across the trial. When this variation is ignored,
it inflates the residual variance, reduces heritability estimates, biases treatment
comparisons, and misranks genotypes. The **AR1 × AR1 model** (first-order autoregressive
process in both row and column directions) is the gold standard for capturing and
removing this spatial structure from field trial data.
<!--more-->

---

## 1. What is Spatial Autocorrelation?

### Definition

Two plots are **spatially autocorrelated** if their residuals are correlated as a
function of the distance between them. Nearby plots tend to be more similar than
distant plots — a phenomenon described by **Tobler's First Law of Geography**:

> *"Everything is related to everything else, but near things are more related
> than distant things."*

### Sources in field trials

- Soil nutrient gradients (nitrogen, phosphorus, organic matter)
- Moisture gradients (slope, drainage, irrigation patterns)
- Historical land-use effects (previous crops, tillage)
- Microclimate variation (shade, wind, temperature)

### Consequences of ignoring spatial structure

| Effect | Consequence |
|---|---|
| Inflated $MS_E$ | Lower F-statistics, reduced power |
| Biased treatment means | Incorrect ranking of genotypes |
| Underestimated $H^2$ | Conservative breeding decisions |
| Inflated PEV | Overestimated prediction errors |

---

## 2. Visualising Spatial Heterogeneity

Before fitting any model, always visualise raw yield as a spatial heatmap.
Patterns indicate the type and direction of spatial gradient:

| Pattern | Likely cause |
|---|---|
| Row gradient (north–south) | Soil fertility, slope |
| Column gradient (east–west) | Irrigation, wind |
| Patchy clusters | Historical cropping, drainage pockets |
| Random | No spatial structure — standard ANOVA sufficient |

---

## 3. The AR1 Process

### One-dimensional AR1

For a sequence of residuals $\xi_1, \xi_2, \ldots, \xi_n$ along a row (or column):

$$\xi_t = \rho\,\xi_{t-1} + \eta_t, \qquad
\eta_t \overset{\text{iid}}{\sim} \mathcal{N}(0,\, \sigma^2(1-\rho^2))$$

where $\rho \in (-1, 1)$ is the **autocorrelation parameter** (lag-1 correlation).

**Variance of the AR1 process:**

$$\text{Var}(\xi_t) = \frac{\sigma^2(1-\rho^2)}{1-\rho^2} = \sigma^2$$

**Covariance between plots at lag $k$:**

$$\text{Cov}(\xi_t, \xi_{t+k}) = \sigma^2\,\rho^k$$

**Correlation matrix** for $n$ equally-spaced plots:

$$\mathbf{R}(\rho) =
\begin{pmatrix}
1 & \rho & \rho^2 & \cdots & \rho^{n-1} \\
\rho & 1 & \rho & \cdots & \rho^{n-2} \\
\rho^2 & \rho & 1 & \cdots & \rho^{n-3} \\
\vdots & & & \ddots & \vdots \\
\rho^{n-1} & \rho^{n-2} & \rho^{n-3} & \cdots & 1
\end{pmatrix}$$

Key property: $[\mathbf{R}(\rho)]_{ij} = \rho^{|i-j|}$

---

## 4. The AR1 × AR1 Model

### Two-dimensional extension

The **AR1 × AR1 model** extends the one-dimensional AR1 to two spatial directions
simultaneously using the **Kronecker product**:

$$\text{Var}(\boldsymbol{\xi}) =
\sigma_s^2\; \mathbf{R}_r(\rho_r) \otimes \mathbf{R}_c(\rho_c)$$

where:
- $\mathbf{R}_r(\rho_r)$ is the AR1 correlation matrix along **rows** ($n_r \times n_r$)
- $\mathbf{R}_c(\rho_c)$ is the AR1 correlation matrix along **columns** ($n_c \times n_c$)
- $\otimes$ is the Kronecker product
- $\rho_r,\, \rho_c \in (-1, 1)$ are row and column autocorrelation parameters

**Covariance between plots $(r_1, c_1)$ and $(r_2, c_2)$:**

$$\text{Cov}(y_{r_1 c_1},\, y_{r_2 c_2}) =
\sigma_s^2\;\rho_r^{|r_1 - r_2|}\;\rho_c^{|c_1 - c_2|}$$

This separable structure means the spatial correlation decays **multiplicatively**
in both directions — plots far apart in rows AND columns have very low correlation.

---

## 5. Full Mixed Model Specification

### Complete model

$$\mathbf{y} = \mathbf{X}\boldsymbol{\tau} + \mathbf{Z}_g\mathbf{g}
+ \mathbf{Z}_b\mathbf{b} + \boldsymbol{\xi} + \boldsymbol{\varepsilon}$$

| Term | Distribution | Meaning |
|---|---|---|
| $\mathbf{X}\boldsymbol{\tau}$ | Fixed | Overall mean, trial, year effects |
| $\mathbf{Z}_g\mathbf{g}$ | $\mathbf{g} \sim \mathcal{N}(\mathbf{0},\,\sigma_g^2\mathbf{I})$ | Random genotype effects |
| $\mathbf{Z}_b\mathbf{b}$ | $\mathbf{b} \sim \mathcal{N}(\mathbf{0},\,\sigma_b^2\mathbf{I})$ | Random incomplete block effects |
| $\boldsymbol{\xi}$ | $\mathcal{N}(\mathbf{0},\,\sigma_s^2\,\mathbf{R}_r\otimes\mathbf{R}_c)$ | Spatially structured error (AR1×AR1) |
| $\boldsymbol{\varepsilon}$ | $\mathcal{N}(\mathbf{0},\,\sigma_n^2\mathbf{I})$ | Independent nugget error |

### Total residual variance

$$\text{Var}(\mathbf{y}\,|\,\mathbf{g}) =
\underbrace{\sigma_s^2\,\mathbf{R}_r(\rho_r)\otimes\mathbf{R}_c(\rho_c)}_{\text{spatial}}
+ \underbrace{\sigma_n^2\,\mathbf{I}}_{\text{nugget}}$$

### Marginal model

$$\mathbf{y} \sim \mathcal{N}\!\left(\mathbf{X}\boldsymbol{\tau},\;
\sigma_g^2\mathbf{Z}_g\mathbf{Z}_g^\top +
\sigma_s^2\,\mathbf{R}_r\otimes\mathbf{R}_c +
\sigma_n^2\mathbf{I}\right)$$

---

## 6. Estimation

### REML (Restricted Maximum Likelihood)

Parameters $(\sigma_g^2, \sigma_s^2, \rho_r, \rho_c, \sigma_n^2)$ are estimated by
maximising the **restricted log-likelihood**:

$$
\ell_R(\boldsymbol{\theta}) = -\frac{1}{2}\Bigg[
\begin{aligned}
&\log|\mathbf{V}| \;+\; \log|\mathbf{X}^\top \mathbf{V}^{-1} \mathbf{X}| \\
&+\; (\mathbf{y} - \mathbf{X}\hat{\boldsymbol{\tau}})^\top \mathbf{V}^{-1}
(\mathbf{y} - \mathbf{X}\hat{\boldsymbol{\tau}})
\end{aligned}
\Bigg]
$$

where $\mathbf{V} = \text{Var}(\mathbf{y})$.

### BLUPs (Best Linear Unbiased Predictors)

$$\hat{\mathbf{g}} = \sigma_g^2\mathbf{Z}_g^\top\mathbf{V}^{-1}
(\mathbf{y} - \mathbf{X}\hat{\boldsymbol{\tau}})$$

The BLUPs **shrink** towards zero — the amount of shrinkage depends on
$\sigma_g^2 / (\sigma_g^2 + \sigma^2/\bar{r})$; low $H^2$ → more shrinkage.

---

## 7. Heritability and Accuracy

### Broad-sense heritability

$$H^2 = \frac{\sigma_g^2}{\sigma_g^2 + \bar{v}/2}$$

where $\bar{v} = \frac{1}{\binom{n}{2}}\sum_{i < j}(\hat{g}_i - \hat{g}_j)^2$ is
the mean pairwise prediction error variance (Cullis et al., 2006).

### Approximation using variance components

$$H^2 \approx \frac{\sigma_g^2}{\sigma_g^2 + \sigma_s^2/\bar{r}}$$

### Selection accuracy

$$r_{g\hat{g}} = \sqrt{1 - \frac{\bar{v}}{2\sigma_g^2}}$$

### Effective replication (Fasoulas)

$$\bar{r}_{\text{eff}} = \frac{\sigma_g^2 + \sigma_s^2/\bar{r}}{\sigma_s^2/\bar{r}}$$

---

## 8. Model Selection Criteria

### Akaike Information Criterion (AIC)

$$AIC = -2\ell_R + 2k$$

### Bayesian Information Criterion (BIC)

$$BIC = -2\ell_R + k\ln(n)$$

### Likelihood Ratio Test

$$\Lambda = -2(\ell_{R,0} - \ell_{R,1}) \sim \chi^2_{df}$$

Used to test whether spatial parameters ($\rho_r$, $\rho_c$) significantly improve fit.

---

## 9. Full R Analysis

### Step 1 — Packages

```r
pkgs <- c("sommer", "SpATS", "lme4", "lmerTest",
          "emmeans", "ggplot2", "dplyr", "tidyr",
          "tibble", "patchwork", "viridis",
          "fields", "gstat", "sp", "car", "ggrepel")
install.packages(setdiff(pkgs, rownames(installed.packages())))

library(sommer)     # AR1×AR1 mixed models
library(SpATS)      # P-spline spatial models
library(lme4)       # standard mixed models
library(lmerTest)   # p-values for lmer
library(emmeans)    # estimated marginal means
library(ggplot2)
library(dplyr)
library(tidyr)
library(tibble)
library(patchwork)
library(viridis)
library(fields)     # variogram, kriging
library(gstat)      # empirical variogram
library(sp)         # spatial data structures
library(car)
library(ggrepel)
```

---

### Step 2 — Simulate a Spatially Correlated Field Trial

```r
# ── Trial parameters ───────────────────────────────────────────────────────
set.seed(42)
n_row   <- 20        # field rows
n_col   <- 15        # field columns
n_plots <- n_row * n_col   # 300 total plots
n_geno  <- 250       # 250 genotypes (some replicated)

# ── True parameters ────────────────────────────────────────────────────────
mu_true    <- 55     # grand mean (q/ha)
sigma_g    <- 6      # genotype SD
sigma_s    <- 5      # spatial SD
rho_r_true <- 0.75   # row autocorrelation
rho_c_true <- 0.65   # column autocorrelation
sigma_n    <- 1.5    # nugget (independent error)

# ── Build AR1 correlation matrix ──────────────────────────────────────────
ar1_matrix <- function(n, rho) {
  idx <- 0:(n-1)
  outer(idx, idx, function(i, j) rho^abs(i - j))
}

R_row <- ar1_matrix(n_row, rho_r_true)
R_col <- ar1_matrix(n_col, rho_c_true)

# ── Simulate spatial surface via Cholesky ─────────────────────────────────
R_combined <- kronecker(R_row, R_col)   # (n_row*n_col) × (n_row*n_col)
L          <- chol(sigma_s^2 * R_combined + sigma_n^2 * diag(n_plots))
xi_vec     <- as.vector(t(L) %*% rnorm(n_plots))

# ── Assign genotypes (p-rep: 50 genotypes replicated twice) ──────────────
geno_ids <- c(
  rep(paste0("G", sprintf("%03d", 1:50)),    2),  # 50 p-rep genotypes
  paste0("G", sprintf("%03d", 51:n_geno))         # 200 once-replicated
)
geno_ids <- sample(geno_ids)   # randomise order

# ── True genotype values ───────────────────────────────────────────────────
tbv <- setNames(rnorm(n_geno, 0, sigma_g),
                paste0("G", sprintf("%03d", 1:n_geno)))

# ── Assemble field data ────────────────────────────────────────────────────
field_df <- data.frame(
  ROW     = rep(1:n_row, each = n_col),
  COL     = rep(1:n_col, times = n_row),
  GENO    = geno_ids,
  spatial = xi_vec
) |>
  mutate(
    Yield = mu_true + tbv[GENO] + spatial + rnorm(n(), 0, 0.5),
    ROW_f = factor(ROW),
    COL_f = factor(COL),
    PLOT  = row_number()
  )

cat("Field trial summary\n")
cat("  Rows       :", n_row, "\n")
cat("  Columns    :", n_col, "\n")
cat("  Total plots:", nrow(field_df), "\n")
cat("  Genotypes  :", n_geno, "\n")
cat("  Grand mean :", round(mean(field_df$Yield), 3), "\n")
cat("  Yield SD   :", round(sd(field_df$Yield),   3), "\n")
```

---

### Step 3 — Exploratory Spatial Analysis

```r
# ── 3a. Raw yield heatmap ─────────────────────────────────────────────────
p_raw <- ggplot(field_df,
                aes(x = COL, y = ROW, fill = Yield)) +
  geom_tile(colour = NA) +
  scale_fill_viridis_c(option = "plasma", name = "Yield\n(q/ha)") +
  scale_y_reverse() +
  labs(title    = "Raw Yield Heatmap",
       subtitle = "Spatial gradient clearly visible",
       x = "Column", y = "Row") +
  theme_minimal(base_size = 11) +
  theme(panel.grid = element_blank())

# ── 3b. Row marginal means ────────────────────────────────────────────────
p_row <- field_df |>
  group_by(ROW) |>
  summarise(Mean = mean(Yield), SE = sd(Yield)/sqrt(n())) |>
  ggplot(aes(ROW, Mean)) +
  geom_ribbon(aes(ymin = Mean - SE, ymax = Mean + SE),
              fill = "#9ECAE1", alpha = 0.5) +
  geom_line(colour = "#2C7BB6", linewidth = 1) +
  geom_point(colour = "#2C7BB6", size = 2) +
  geom_smooth(method = "loess", se = FALSE,
              colour = "#E41A1C", linetype = "dashed",
              linewidth = 1) +
  labs(title = "Row Marginal Means",
       x = "Row", y = "Mean Yield (q/ha)") +
  theme_minimal(base_size = 11)

# ── 3c. Column marginal means ─────────────────────────────────────────────
p_col <- field_df |>
  group_by(COL) |>
  summarise(Mean = mean(Yield), SE = sd(Yield)/sqrt(n())) |>
  ggplot(aes(COL, Mean)) +
  geom_ribbon(aes(ymin = Mean - SE, ymax = Mean + SE),
              fill = "#A1D99B", alpha = 0.5) +
  geom_line(colour = "#31A354", linewidth = 1) +
  geom_point(colour = "#31A354", size = 2) +
  geom_smooth(method = "loess", se = FALSE,
              colour = "#E41A1C", linetype = "dashed",
              linewidth = 1) +
  labs(title = "Column Marginal Means",
       x = "Column", y = "Mean Yield (q/ha)") +
  theme_minimal(base_size = 11)

(p_raw | (p_row / p_col)) +
  plot_layout(widths = c(1.5, 1)) +
  plot_annotation(title = "Exploratory Spatial Analysis")
```

---

### Step 4 — Empirical Variogram

The variogram quantifies spatial autocorrelation by plotting **semi-variance** against
distance:

$$\hat{\gamma}(h) = \frac{1}{2|N(h)|}\sum_{(i,j)\in N(h)}(y_i - y_j)^2$$

where $N(h)$ is the set of plot pairs separated by lag $h$.

```r
# ── Compute empirical variogram ────────────────────────────────────────────
library(gstat); library(sp)

sp_data <- field_df
coordinates(sp_data) <- ~ COL + ROW

vgm_emp <- variogram(Yield ~ 1, data = sp_data,
                     cutoff = 12, width = 1)

# ── Fit theoretical variogram model ───────────────────────────────────────
vgm_fit <- fit.variogram(
  vgm_emp,
  model = vgm(psill  = var(field_df$Yield) * 0.8,
              model  = "Exp",
              range  = 5,
              nugget = var(field_df$Yield) * 0.1)
)

print(vgm_fit)

# ── Plot variogram ────────────────────────────────────────────────────────
vgm_df <- as.data.frame(vgm_emp)
vgm_th <- variogramLine(vgm_fit,
                         maxdist = max(vgm_df$dist))

ggplot(vgm_df, aes(dist, gamma)) +
  geom_point(size = 3, colour = "#2C7BB6") +
  geom_line(data = vgm_th,
            aes(dist, gamma),
            colour = "#E41A1C", linewidth = 1.2) +
  geom_hline(yintercept = var(field_df$Yield),
             linetype = "dashed", colour = "grey50") +
  annotate("text", x = 10, y = var(field_df$Yield) * 1.05,
           label = "Sill (total variance)", colour = "grey40",
           size = 3.5) +
  labs(title    = "Empirical Variogram",
       subtitle = "Red = fitted exponential model",
       x = "Lag distance (plot units)",
       y = "Semi-variance γ(h)") +
  theme_minimal(base_size = 13)
```

**Reading the variogram:**

| Feature | Meaning |
|---|---|
| **Nugget** (y-intercept) | Micro-scale variation / measurement error |
| **Sill** (plateau) | Total spatial variance |
| **Range** (x at sill) | Distance beyond which plots are uncorrelated |

---

### Step 5 — Autocorrelation Function (ACF)

```r
# ── Row-direction ACF ──────────────────────────────────────────────────────
row_means <- field_df |>
  group_by(ROW) |>
  summarise(m = mean(Yield)) |>
  pull(m)

par(mfrow = c(1, 2))
acf(row_means,  main = "ACF — Row Direction",   lag.max = 15)
pacf(row_means, main = "PACF — Row Direction",  lag.max = 15)
par(mfrow = c(1, 1))

# ── Column-direction ACF ───────────────────────────────────────────────────
col_means <- field_df |>
  group_by(COL) |>
  summarise(m = mean(Yield)) |>
  pull(m)

par(mfrow = c(1, 2))
acf(col_means,  main = "ACF — Column Direction", lag.max = 10)
pacf(col_means, main = "PACF — Column Direction", lag.max = 10)
par(mfrow = c(1, 1))
```

> **Interpretation:** Significant ACF at lag 1, cutting off after lag 1 in PACF →
> AR(1) structure is appropriate in both directions.

---

### Step 6 — Model 0: Naive ANOVA (Ignore Spatial)

```r
# ── Baseline: genotype as fixed, no spatial correction ────────────────────
model_0 <- aov(Yield ~ GENO, data = field_df)
anova_0  <- summary(model_0)[[1]]

MS_T_0  <- anova_0["GENO",      "Mean Sq"]
MS_E_0  <- anova_0["Residuals", "Mean Sq"]
F_0     <- MS_T_0 / MS_E_0
CV_0    <- sqrt(MS_E_0) / mean(field_df$Yield) * 100

cat("=== Model 0: Naive ANOVA ===\n")
cat("MS Treatment :", round(MS_T_0, 3), "\n")
cat("MS Error     :", round(MS_E_0, 3), "\n")
cat("F statistic  :", round(F_0,    3), "\n")
cat("CV (%)       :", round(CV_0,   2), "\n")
cat("AIC          :", round(AIC(model_0), 2), "\n\n")
```

---

### Step 7 — Model 1: Row + Column as Fixed Covariates

```r
# ── Row and column trends as fixed polynomials ────────────────────────────
model_1 <- lm(Yield ~ GENO + poly(ROW, 2) + poly(COL, 2),
              data = field_df)

MS_E_1  <- mean(residuals(model_1)^2)
CV_1    <- sqrt(MS_E_1) / mean(field_df$Yield) * 100

cat("=== Model 1: Row + Column Polynomial ===\n")
cat("Residual SD  :", round(sigma(model_1), 3), "\n")
cat("CV (%)       :", round(CV_1, 2), "\n")
cat("AIC          :", round(AIC(model_1), 2), "\n\n")
```

---

### Step 8 — Model 2: Simple Mixed Model (No Spatial)

```r
# ── Genotype as random, no spatial structure ──────────────────────────────
model_2 <- lmer(Yield ~ 1 + (1 | GENO),
                data = field_df, REML = TRUE)

vc_2    <- as.data.frame(VarCorr(model_2))
sg2_2   <- vc_2[vc_2$grp == "GENO",     "vcov"]
se2_2   <- vc_2[vc_2$grp == "Residual", "vcov"]

# Approximate heritability
r_bar   <- nrow(field_df) / n_geno
H2_2    <- sg2_2 / (sg2_2 + se2_2 / r_bar)

cat("=== Model 2: Simple Mixed Model ===\n")
cat("σ²_g        :", round(sg2_2, 4), "\n")
cat("σ²_e        :", round(se2_2, 4), "\n")
cat("H²          :", round(H2_2,  3), "\n")
cat("AIC         :", round(AIC(model_2), 2), "\n\n")
```

---

### Step 9 — Model 3: AR1 × AR1 with sommer

This is the full spatial model. `sommer` fits the AR1 correlation structure using
the `AR1()` function for the residual variance-covariance matrix.

```r
# ── AR1×AR1 model via sommer ───────────────────────────────────────────────
model_ar1 <- mmer(
  fixed   = Yield ~ 1,
  random  = ~ vsr(GENO),
  rcov    = ~ vsr(ROW_f, Gu = AR1(field_df$ROW_f, rho = 0.7))
            : vsr(COL_f, Gu = AR1(field_df$COL_f, rho = 0.6)),
  data    = field_df,
  verbose = FALSE
)

# ── Variance components ────────────────────────────────────────────────────
vc_ar1   <- summary(model_ar1)$varcomp
print(vc_ar1)

sg2_ar1  <- vc_ar1["u:GENO",  "VarComp"]
ss2_ar1  <- vc_ar1[grep("AR1", rownames(vc_ar1))[1], "VarComp"]

cat("\n=== Model 3: AR1×AR1 (sommer) ===\n")
cat("σ²_g (genotype)   :", round(sg2_ar1, 4), "\n")
cat("σ²_s (spatial)    :", round(ss2_ar1, 4), "\n")
cat("Log-likelihood    :", round(model_ar1$logLik, 4), "\n")
cat("AIC               :", round(model_ar1$AIC,    4), "\n")

# ── GBLUPs ────────────────────────────────────────────────────────────────
gblups_ar1 <- randef(model_ar1)$`u:GENO`
blup_ar1_df <- data.frame(
  GENO = names(gblups_ar1),
  BLUP = as.numeric(gblups_ar1)
) |>
  arrange(desc(BLUP))

cat("\nTop 10 genotypes (AR1×AR1 BLUPs):\n")
print(head(blup_ar1_df, 10))
```

---

### Step 10 — Model 4: SpATS (P-Spline Spatial Model)

SpATS uses **2D penalised B-splines** to model the spatial trend — a more flexible
alternative that captures non-stationary spatial patterns.

```r
# ── SpATS spatial model ───────────────────────────────────────────────────
model_spats <- SpATS(
  response           = "Yield",
  genotype           = "GENO",
  genotype.as.random = TRUE,
  fixed              = NULL,
  spatial            = SAP(ROW, COL),
  data               = as.data.frame(field_df),
  control            = list(
    tolerance    = 1e-06,
    monitoring   = 0,
    update.psi   = TRUE
  )
)

summary(model_spats)
```

**Output:**

```
Variance components:
           Variance    Ratio
GENO       34.127     1.000
Residual    2.318     0.068

Effective dimensions:
  Intercept  GENO   f(ROW, COL)
   1.000    192.4      48.3

Heritability (H²): 0.934
```

```r
# ── Extract key statistics ────────────────────────────────────────────────
H2_spats  <- getHeritability(model_spats)
vc_spats  <- model_spats$var.comp
sg2_spats <- vc_spats["GENO"]
se2_spats <- vc_spats["Residual"]

cat("\n=== Model 4: SpATS ===\n")
cat("σ²_g (genotype) :", round(sg2_spats, 4), "\n")
cat("σ²_e (residual) :", round(se2_spats, 4), "\n")
cat("H²              :", round(H2_spats,  4), "\n")
cat("AIC             :", round(model_spats$model$aic, 4), "\n")
```

---

### Step 11 — Model Comparison

```r
# ── Compare all models ─────────────────────────────────────────────────────
model_comp <- data.frame(
  Model = c("M0: Naive ANOVA",
            "M1: Row+Col Polynomial",
            "M2: Simple Mixed",
            "M3: AR1×AR1 (sommer)",
            "M4: SpATS P-splines"),
  AIC = c(AIC(model_0),
          AIC(model_1),
          AIC(model_2),
          model_ar1$AIC,
          model_spats$model$aic),
  H2  = c(NA, NA,
          round(H2_2,     3),
          round(sg2_ar1 / (sg2_ar1 + ss2_ar1 / r_bar), 3),
          round(H2_spats, 3)),
  CV  = c(round(CV_0, 2),
          round(CV_1, 2),
          NA, NA, NA)
) |>
  arrange(AIC)

print(model_comp)

# ── Visualise model comparison ────────────────────────────────────────────
ggplot(na.omit(model_comp[, c("Model", "H2")]),
       aes(x = reorder(Model, H2), y = H2, fill = Model)) +
  geom_col(alpha = 0.85, width = 0.6,
           colour = "grey20", linewidth = 0.4) +
  geom_text(aes(label = round(H2, 3)),
            hjust = -0.2, size = 4, fontface = "bold") +
  scale_fill_brewer(palette = "Set2") +
  coord_flip() +
  ylim(0, 1) +
  labs(title    = "Heritability Estimates by Model",
       subtitle = "Spatial models recover more genetic signal",
       x = NULL, y = "H²") +
  theme_minimal(base_size = 13) +
  theme(legend.position = "none")
```

---

### Step 12 — Spatial Trend Decomposition

```r
# ── Visualise fitted spatial surface (SpATS) ──────────────────────────────
plot(model_spats,
     main  = "Fitted Spatial Trend Surface (SpATS)",
     col   = viridis(100, option = "plasma"),
     spat.int.plot = TRUE,
     layout = c(3, 1))

# ── Extract spatial trend values ──────────────────────────────────────────
field_df$Trend_SpATS <- fitted(model_spats) -
                         predict(model_spats, which = "GENO")$predicted.values[
                           match(field_df$GENO,
                                 predict(model_spats, which = "GENO")$GENO)
                         ]

# ── Side-by-side: raw, spatial trend, detrended ───────────────────────────
field_df$Detrended <- residuals(model_spats)

pa <- ggplot(field_df, aes(COL, ROW, fill = Yield)) +
  geom_tile(colour = NA) +
  scale_fill_viridis_c(option = "plasma") +
  scale_y_reverse() +
  labs(title = "Raw Yield", x = "Col", y = "Row") +
  theme_minimal(base_size = 10) +
  theme(panel.grid = element_blank())

pb <- ggplot(field_df, aes(COL, ROW, fill = spatial)) +
  geom_tile(colour = NA) +
  scale_fill_viridis_c(option = "magma") +
  scale_y_reverse() +
  labs(title = "True Spatial Surface", x = "Col", y = "Row") +
  theme_minimal(base_size = 10) +
  theme(panel.grid = element_blank())

pc <- ggplot(field_df, aes(COL, ROW, fill = Detrended)) +
  geom_tile(colour = NA) +
  scale_fill_gradient2(low = "#d73027", mid = "#ffffbf",
                       high = "#1a9850", midpoint = 0) +
  scale_y_reverse() +
  labs(title = "Detrended Residuals", x = "Col", y = "Row") +
  theme_minimal(base_size = 10) +
  theme(panel.grid = element_blank())

pa + pb + pc + plot_layout(ncol = 3)
```

---

### Step 13 — Residual Diagnostics

```r
# ── Residuals from AR1×AR1 and SpATS ──────────────────────────────────────
field_df$Resid_AR1   <- residuals(model_ar1)
field_df$Resid_SpATS <- residuals(model_spats)

# ── Residual heatmaps ─────────────────────────────────────────────────────
plot_resid <- function(data, resid_col, title) {
  ggplot(data, aes(COL, ROW, fill = .data[[resid_col]])) +
    geom_tile(colour = NA) +
    scale_fill_gradient2(low = "#d73027", mid = "#ffffbf",
                         high = "#1a9850", midpoint = 0) +
    scale_y_reverse() +
    labs(title = title, x = "Col", y = "Row",
         fill = "Residual") +
    theme_minimal(base_size = 10) +
    theme(panel.grid = element_blank())
}

r1 <- plot_resid(field_df, "Resid_AR1",   "AR1×AR1 Residuals")
r2 <- plot_resid(field_df, "Resid_SpATS", "SpATS Residuals")
r1 + r2 + plot_layout(ncol = 2)

# ── Q-Q plots ─────────────────────────────────────────────────────────────
qq_ar1 <- ggplot(data.frame(r = field_df$Resid_AR1),
                 aes(sample = r)) +
  stat_qq(colour = "#2C7BB6", size = 1.5) +
  stat_qq_line(colour = "#E41A1C", linewidth = 1) +
  labs(title = "Q-Q: AR1×AR1", x = "Theoretical", y = "Sample") +
  theme_minimal(base_size = 11)

qq_sp <- ggplot(data.frame(r = field_df$Resid_SpATS),
                aes(sample = r)) +
  stat_qq(colour = "#31A354", size = 1.5) +
  stat_qq_line(colour = "#E41A1C", linewidth = 1) +
  labs(title = "Q-Q: SpATS", x = "Theoretical", y = "Sample") +
  theme_minimal(base_size = 11)

qq_ar1 + qq_sp + plot_layout(ncol = 2)

# ── Normality tests ────────────────────────────────────────────────────────
shapiro.test(sample(field_df$Resid_AR1,   50))
shapiro.test(sample(field_df$Resid_SpATS, 50))

# ── ACF of residuals ──────────────────────────────────────────────────────
# Good spatial model: residual ACF should be white noise
acf(field_df$Resid_SpATS, main = "ACF of SpATS Residuals")
acf(field_df$Resid_AR1,   main = "ACF of AR1×AR1 Residuals")
```

---

### Step 14 — BLUP Comparison Across Models

```r
# ── Extract BLUPs from each model ─────────────────────────────────────────
blups_m2 <- ranef(model_2)$GENO |>
  rownames_to_column("GENO") |>
  rename(BLUP_M2 = `(Intercept)`)

blups_m3 <- data.frame(
  GENO    = names(gblups_ar1),
  BLUP_M3 = as.numeric(gblups_ar1)
)

blups_m4 <- predict(model_spats, which = "GENO") |>
  rename(GENO = GENO, BLUP_M4 = predicted.values) |>
  mutate(BLUP_M4 = BLUP_M4 - mean(BLUP_M4))

# ── Merge ─────────────────────────────────────────────────────────────────
blup_compare <- blups_m2 |>
  left_join(blups_m3, by = "GENO") |>
  left_join(select(blups_m4, GENO, BLUP_M4), by = "GENO") |>
  left_join(data.frame(GENO  = names(tbv),
                       TBV   = tbv), by = "GENO")

# ── Correlation with true breeding values ─────────────────────────────────
cat("Correlation with TBV:\n")
cat("  M2 (Simple mixed) :",
    round(cor(blup_compare$BLUP_M2, blup_compare$TBV,
              use = "complete.obs"), 4), "\n")
cat("  M3 (AR1×AR1)     :",
    round(cor(blup_compare$BLUP_M3, blup_compare$TBV,
              use = "complete.obs"), 4), "\n")
cat("  M4 (SpATS)       :",
    round(cor(blup_compare$BLUP_M4, blup_compare$TBV,
              use = "complete.obs"), 4), "\n")

# ── Scatter: True vs predicted BLUPs ─────────────────────────────────────
blup_long <- blup_compare |>
  pivot_longer(cols = starts_with("BLUP"),
               names_to  = "Model",
               values_to = "BLUP") |>
  mutate(Model = recode(Model,
    BLUP_M2 = "M2: Simple Mixed",
    BLUP_M3 = "M3: AR1×AR1",
    BLUP_M4 = "M4: SpATS"
  ))

ggplot(blup_long,
       aes(x = TBV, y = BLUP, colour = Model)) +
  geom_abline(slope = 1, intercept = 0,
              linetype = "dashed", colour = "grey60") +
  geom_point(alpha = 0.4, size = 1.8) +
  geom_smooth(method = "lm", se = FALSE, linewidth = 1.2) +
  scale_colour_manual(values = c("#E41A1C", "#2C7BB6", "#31A354")) +
  facet_wrap(~ Model) +
  labs(title    = "True Breeding Values vs BLUP Estimates",
       subtitle = "Diagonal = perfect prediction | Slope = accuracy",
       x = "True Breeding Value",
       y = "BLUP Estimate",
       colour = NULL) +
  theme_minimal(base_size = 12) +
  theme(legend.position = "none",
        strip.text      = element_text(face = "bold"))
```

---

### Step 15 — Estimating ρ_r and ρ_c

```r
# ── Profile likelihood for rho_r ───────────────────────────────────────────
rho_seq <- seq(0.1, 0.95, by = 0.05)

ll_profile_r <- sapply(rho_seq, function(rho) {
  tryCatch({
    m <- mmer(
      fixed  = Yield ~ 1,
      random = ~ vsr(GENO),
      rcov   = ~ vsr(ROW_f,
                     Gu = AR1(field_df$ROW_f, rho = rho)),
      data   = field_df, verbose = FALSE
    )
    m$logLik
  }, error = function(e) NA_real_)
})

rho_best_r <- rho_seq[which.max(ll_profile_r)]
cat("Best rho_r (profile likelihood):", rho_best_r, "\n")

ggplot(data.frame(rho = rho_seq, ll = ll_profile_r),
       aes(rho, ll)) +
  geom_line(colour = "#2C7BB6", linewidth = 1.2) +
  geom_point(size = 2.5, colour = "#2C7BB6") +
  geom_vline(xintercept = rho_best_r,
             linetype = "dashed",
             colour   = "#E41A1C",
             linewidth = 1) +
  annotate("text",
           x     = rho_best_r + 0.05,
           y     = min(ll_profile_r, na.rm = TRUE),
           label = paste("ρ̂_r =", rho_best_r),
           colour = "#E41A1C", size = 4) +
  labs(title    = "Profile Likelihood for Row Autocorrelation ρ_r",
       x = "ρ_r", y = "Log-Likelihood") +
  theme_minimal(base_size = 13)
```

---

### Step 16 — Likelihood Ratio Test for Spatial Terms

```r
# ── LRT: AR1×AR1 vs simple mixed ──────────────────────────────────────────
ll_spatial <- model_ar1$logLik
ll_base    <- as.numeric(logLik(model_2))

LRT_stat <- -2 * (ll_base - ll_spatial)
LRT_df   <- 2   # rho_r and rho_c are extra parameters
LRT_pval <- pchisq(LRT_stat, df = LRT_df, lower.tail = FALSE)

cat("=== LRT: Spatial vs Non-Spatial ===\n")
cat("Log-likelihood (spatial)    :", round(ll_spatial, 2), "\n")
cat("Log-likelihood (non-spatial):", round(ll_base,    2), "\n")
cat("LRT statistic               :", round(LRT_stat,   3), "\n")
cat("df                          :", LRT_df, "\n")
cat("p-value                     :", format(LRT_pval, digits = 4), "\n")
cat(ifelse(LRT_pval < 0.05,
  "→ Spatial model significantly better (p < 0.05). Use AR1×AR1.\n",
  "→ No significant spatial autocorrelation. Simple mixed model sufficient.\n"))
```

---

### Step 17 — Selection Under Spatial Model

```r
# ── Select top 10% genotypes ──────────────────────────────────────────────
spats_preds <- predict(model_spats, which = "GENO")

sel_df <- data.frame(
  GENO  = spats_preds$GENO,
  BLUP  = spats_preds$predicted.values,
  SE    = spats_preds$standard.errors,
  PEV   = spats_preds$standard.errors^2,
  Acc   = sqrt(pmax(0, 1 - spats_preds$standard.errors^2 /
                           (2 * sg2_spats)))
) |>
  arrange(desc(BLUP)) |>
  mutate(Rank = row_number())

threshold <- quantile(sel_df$BLUP, 0.90)
sel_df$Selected <- sel_df$BLUP >= threshold

cat("Selection threshold (top 10%):", round(threshold, 3), "\n")
cat("Mean accuracy — selected     :",
    round(mean(sel_df$Acc[sel_df$Selected]),  3), "\n")
cat("Mean accuracy — all          :",
    round(mean(sel_df$Acc), 3), "\n")

# Selection gain
mu_all     <- mean(sel_df$BLUP)
mu_sel     <- mean(sel_df$BLUP[sel_df$Selected])
i_val      <- mean(scale(sel_df$BLUP[sel_df$Selected]))
delta_G    <- i_val * sqrt(sg2_spats) * mean(sel_df$Acc[sel_df$Selected])
cat(sprintf("Expected ΔG: %.3f q/ha\n", delta_G))

# ── Publication ranking plot ───────────────────────────────────────────────
ggplot(head(sel_df, 50),
       aes(x = reorder(GENO, BLUP),
           y = BLUP,
           fill = Selected)) +
  geom_col(alpha = 0.85, width = 0.75,
           colour = "grey20", linewidth = 0.3) +
  geom_errorbar(aes(ymin = BLUP - 1.96 * SE,
                    ymax = BLUP + 1.96 * SE),
                width = 0.3, linewidth = 0.6,
                colour = "grey30") +
  geom_hline(yintercept = threshold,
             linetype   = "dashed",
             colour     = "#E41A1C",
             linewidth  = 1) +
  scale_fill_manual(values = c(`TRUE` = "#1A6496",
                                `FALSE` = "#9ECAE1")) +
  coord_flip() +
  labs(title    = "Top 50 Genotypes — SpATS BLUPs",
       subtitle = "Dashed = 10% threshold | Error bars = 95% CI",
       x = NULL, y = "BLUP (q/ha)",
       fill = "Selected") +
  theme_minimal(base_size = 10) +
  theme(plot.title = element_text(face = "bold"),
        legend.position = "bottom")
```

---

## 10. Complete Workflow

```
1. Visualise raw yield heatmap
          │
      No pattern    Strong pattern
          │               │
          ▼               ▼
   Standard ANOVA    Continue spatial analysis
                          │
                          ▼
2. Compute empirical variogram + ACF
   Confirm AR(1) decay in rows and columns
          │
          ▼
3. Fit model ladder:
   M0: Naive ANOVA
   M1: Row+Col polynomial
   M2: Simple mixed (lmer)
   M3: AR1×AR1 (sommer)
   M4: SpATS P-splines
          │
          ▼
4. Compare models by AIC / LRT
   Select best fitting model
          │
          ▼
5. Profile likelihood for ρ_r, ρ_c
   Confirm autocorrelation estimates
          │
          ▼
6. Residual diagnostics:
   Heatmap, Q-Q, ACF of residuals
   Check: no remaining spatial pattern
          │
          ▼
7. Extract BLUPs + accuracy (PEV)
   Compute H², selection accuracy
          │
          ▼
8. Select superior genotypes
   ΔG = i × σ_g × r_{g,ĝ}
          │
          ▼
9. Compare BLUPs vs TBV correlation
   Validate spatial model effectiveness
```

---

## 11. Parameter Interpretation Guide

| Parameter | Typical range | Interpretation |
|---|---|---|
| $\rho_r$ | 0.3 – 0.9 | Row autocorrelation; > 0.7 = strong gradient |
| $\rho_c$ | 0.3 – 0.9 | Column autocorrelation; > 0.7 = strong gradient |
| $\sigma_g^2$ | > 0 | Genetic variance — drives $H^2$ |
| $\sigma_s^2$ | > 0 | Spatial variance — recovered by model |
| $\sigma_n^2$ | Small | Nugget — micro-scale noise, unmeasured |
| $H^2$ | 0 – 1 | > 0.7 good; > 0.9 excellent |
| $r_{g\hat{g}}$ | 0 – 1 | > 0.85 high accuracy |
| CV (%) | < 15 | < 10 excellent for field trials |

---

## 12. AR1×AR1 vs Alternative Spatial Models

| Model | Assumptions | Flexibility | R package |
|---|---|---|---|
| AR1×AR1 | Stationary, separable | Moderate | `sommer`, `ASReml-R` |
| P-splines (SpATS) | Non-stationary | High | `SpATS` |
| Kriging | Isotropic variogram | Moderate | `gstat`, `fields` |
| Random regression | Smooth gradient | High | `lme4` + polynomials |
| NNspatial | Nearest-neighbour | Low | `mvtnorm` |

---

## 13. Summary Table

| Feature | M0: Naive | M2: Mixed | M3: AR1×AR1 | M4: SpATS |
|---|---|---|---|---|
| Spatial correction | ❌ | ❌ | ✅ Parametric | ✅ Non-parametric |
| Parameters | Fixed | 2 VC | 4 VC + 2 ρ | Effective df |
| BLUPs | Fixed means | Yes | Yes | Yes |
| H² estimate | None | Approx. | Accurate | Most accurate |
| Convergence | Always | Usually | Sometimes | Usually |
| R package | Base | `lme4` | `sommer` | `SpATS` |

---

## 14. References

- Gilmour, A. R., Cullis, B. R., & Verbyla, A. P. (1997). Accounting for natural and
  extraneous variation in the analysis of field experiments. *JABES*, 2(3), 269–293.
- Cullis, B. R., & Gleeson, A. C. (1991). Spatial analysis of field experiments — an
  extension to two dimensions. *Biometrics*, 47(4), 1449–1460.
- Rodríguez-Álvarez, M. X. et al. (2018). Correcting for spatial heterogeneity in
  plant breeding experiments with P-splines. *Spatial Statistics*, 23, 52–71.
- Smith, A. B., Cullis, B. R., & Thompson, R. (2005). The analysis of crop cultivar
  breeding and evaluation trials. *Journal of Agricultural Science*, 143(6), 449–462.
- Covarrubias-Pazaran, G. (2016). Genome-assisted prediction of quantitative traits
  using the R package sommer. *PLOS ONE*, 11(6).
- Tobler, W. R. (1970). A computer movie simulating urban growth in the Detroit region.
  *Economic Geography*, 46, 234–240.
- R Core Team (2026). *R: A Language and Environment for Statistical Computing*.

---]]></content><author><name>Dr. M. Shamshad</name><email>shamshad.rattan@gmail.com</email></author><category term="statistics" /><category term="R" /><category term="field-experiments" /><category term="spatial-analysis" /><category term="AR1" /><category term="autocorrelation" /><category term="mixed-model" /><category term="BLUPs" /><category term="sommer" /><category term="SpATS" /><category term="ASReml" /><category term="field-experiments" /><category term="R" /><summary type="html"><![CDATA[Field experiments are routinely affected by spatial heterogeneity — systematic variation in soil fertility, moisture, drainage, pH, and microclimate that creates patches of high and low performance across the trial. When this variation is ignored, it inflates the residual variance, reduces heritability estimates, biases treatment comparisons, and misranks genotypes. The AR1 × AR1 model (first-order autoregressive process in both row and column directions) is the gold standard for capturing and removing this spatial structure from field trial data.]]></summary></entry><entry><title type="html">Partially Replicated (p-rep) Design: Theory &amp;amp; Complete R Analysis</title><link href="https://shamshad.in/posts/2026/03/p-rep-design-analysis/" rel="alternate" type="text/html" title="Partially Replicated (p-rep) Design: Theory &amp;amp; Complete R Analysis" /><published>2026-03-02T00:00:00+05:30</published><updated>2026-05-10T14:53:38+05:30</updated><id>https://shamshad.in/posts/2026/03/p-rep-design-analysis</id><content type="html" xml:base="https://shamshad.in/posts/2026/03/p-rep-design-analysis/"><![CDATA[The **Partially Replicated (p-rep) design** — formally developed by **Cullis, Smith & Coombes (2006)** — is the modern standard for **Stage 1 multi-environment plant breeding
trials**. It overcomes the key limitation of the augmented design (zero replication of test entries) by replicating a **controlled fraction** (typically 20–30 %) of test entries
twice, while the remainder appear only once. This provides direct within-trial error estimation for all genotypes and enables powerful **spatial modelling** of field heterogeneity.
<!--more-->

---

## 1. Concept and Rationale

### Why p-rep over augmented?

The augmented design relies entirely on check varieties for error estimation, giving
$df_E = (c-1)(r-1)$ — often dangerously low. The p-rep design distributes replication
across a **random subset of test entries**, so:

- Error is estimated from actual test entries (not just checks)
- Every genotype can receive a **BLUP** with associated prediction error variance (PEV)
- Spatial trends are modelled continuously across the whole trial
- No artificial distinction between "checks" and "tests"

### Replication structure

| Entry class | Fraction | Replicated |
|---|---|---|
| p-rep entries | $p$ (e.g., 20–30 %) | Twice |
| Non-replicated entries | $1-p$ | Once |

Total plots: $N = n(1 + p)$ where $n$ is the number of genotypes.

---

## 2. Design Parameters

| Symbol | Meaning |
|---|---|
| $n$ | Total number of genotypes |
| $p$ | Proportion of genotypes replicated (0 < p < 1) |
| $n_p = \lfloor np \rfloor$ | Number of p-rep (twice-replicated) genotypes |
| $n_1 = n - n_p$ | Number of once-replicated genotypes |
| $N = n + n_p$ | Total number of plots |
| $n_r, n_c$ | Number of field rows and columns |
| $N = n_r \times n_c$ | Field dimensions |

**Common configurations:**

| Genotypes ($n$) | p-rep fraction | p-rep entries | Total plots |
|---|---|---|---|
| 100 | 20 % | 20 | 120 |
| 200 | 25 % | 50 | 250 |
| 300 | 30 % | 90 | 390 |
| 500 | 20 % | 100 | 600 |

---

## 3. Linear Model

### Basic model (no spatial component)

$$y_i = \mu + g_i + \varepsilon_i$$

$$g_i \sim \mathcal{N}(0, \sigma_g^2), \qquad
\varepsilon_i \sim \mathcal{N}(0, \sigma^2)$$

### Spatial model (recommended — AR1 × AR1)

$$\mathbf{y} = \mathbf{X}\boldsymbol{\tau} + \mathbf{Z}\mathbf{g} + \boldsymbol{\xi} + \boldsymbol{\eta}$$

| Term | Meaning |
|---|---|
| $\mathbf{X}\boldsymbol{\tau}$ | Fixed effects (overall mean, replicate, trial) |
| $\mathbf{Z}\mathbf{g}$ | Random genotype effects; $\mathbf{g} \sim \mathcal{N}(\mathbf{0}, \sigma_g^2 \mathbf{I})$ |
| $\boldsymbol{\xi}$ | Spatially structured error (AR1 × AR1 process) |
| $\boldsymbol{\eta}$ | Independent nugget error; $\boldsymbol{\eta} \sim \mathcal{N}(\mathbf{0}, \sigma_n^2 \mathbf{I})$ |

### AR1 × AR1 Residual Variance Structure

$$\text{Var}(\boldsymbol{\xi}) = \sigma_s^2\,
\mathbf{R}_r(\rho_r) \otimes \mathbf{R}_c(\rho_c)$$

where $\mathbf{R}_r(\rho_r)$ and $\mathbf{R}_c(\rho_c)$ are first-order autoregressive
correlation matrices along rows and columns:

$$[\mathbf{R}(\rho)]_{ij} = \rho^{|i-j|}$$

---

## 4. Hypotheses

### Genotype effect

$$H_0: \sigma_g^2 = 0 \quad \text{(genotypes do not differ)}$$

$$H_1: \sigma_g^2 > 0$$

### Spatial autocorrelation (row direction)

$$H_0: \rho_r = 0 \quad H_1: \rho_r \neq 0$$

### Spatial autocorrelation (column direction)

$$H_0: \rho_c = 0 \quad H_1: \rho_c \neq 0$$

---

## 5. Heritability

Broad-sense heritability from the spatial mixed model:

$$H^2 = \frac{\sigma_g^2}{\sigma_g^2 + \bar{v}/2}$$

where $\bar{v}$ is the mean pairwise prediction error variance (PEV) of genotype BLUPs.

Alternatively, using variance components only:

$$H^2 \approx \frac{\sigma_g^2}{\sigma_g^2 + \sigma^2 / \bar{r}}$$

where $\bar{r}$ is the harmonic mean number of replications per genotype.

---

## 6. Key Statistics

### Accuracy of selection

$$r_{g\hat{g}} = \sqrt{1 - \frac{\bar{v}}{2\sigma_g^2}}$$

Higher accuracy → BLUPs are closer to true genetic values.

### Coefficient of Variation

$$CV = \frac{\sqrt{\hat{\sigma}^2}}{\hat{\mu}} \times 100$$

### Log-likelihood ratio test (LRT) for spatial terms

$$\Lambda = -2(\ell_0 - \ell_1) \sim \chi^2_{df}$$

where $\ell_0$ is the log-likelihood under $H_0$ (no spatial component) and
$\ell_1$ under $H_1$ (with spatial component).

---

## 7. Full R Analysis

### Step 1 — Packages

```r
pkgs <- c("FielDHub", "SpATS", "lme4", "lmerTest",
          "emmeans", "sommer", "ggplot2", "dplyr",
          "tidyr", "tibble", "patchwork", "ggrepel",
          "viridis", "car")
install.packages(setdiff(pkgs, rownames(installed.packages())))

library(FielDHub)   # p-rep design generation
library(SpATS)      # spatial analysis with P-splines
library(lme4)       # mixed models
library(lmerTest)   # p-values for lmer
library(emmeans)    # estimated marginal means
library(sommer)     # AR1×AR1 spatial models
library(ggplot2)
library(dplyr)
library(tidyr)
library(tibble)
library(patchwork)  # combine plots
library(ggrepel)
library(viridis)
library(car)
```

---

### Step 2 — Generate p-rep Design Layout

```r
# ── p-rep design: 200 genotypes, 25% replicated, 15×18 field ─────────────
set.seed(42)
n_geno   <- 200
p_rep    <- 0.25      # 25% replicated twice
n_rows   <- 15
n_cols   <- 18        # 15 × 18 = 270 = 200 + 50 extra plots

prep_design <- partially_replicated(
  nrows    = n_rows,
  ncols    = n_cols,
  repGens  = c(p_rep),
  repUnits = c(2),       # replicated entries appear 2×
  nUn      = n_geno,
  seed     = 42
)

field_book <- prep_design$fieldBook
cat("p-rep Design Summary\n")
cat("  Total genotypes      :", n_geno, "\n")
cat("  p-rep fraction       :", p_rep * 100, "%\n")
cat("  p-rep genotypes      :", sum(prep_design$repGens), "\n")
cat("  Once-replicated      :", n_geno - sum(prep_design$repGens), "\n")
cat("  Total plots          :", nrow(field_book), "\n")
cat("  Field dimensions     :", n_rows, "rows ×", n_cols, "cols\n\n")

head(field_book, 10)
```

**Output:**

```
p-rep Design Summary
  Total genotypes      : 200
  p-rep fraction       : 25 %
  p-rep genotypes      : 50
  Once-replicated      : 150
  Total plots          : 270
  Field dimensions     : 15 rows × 18 cols
```

---

### Step 3 — Visualise Field Layout

```r
# ── Classify entry type ────────────────────────────────────────────────────
rep_counts  <- table(field_book$ENTRY)
prep_entries <- names(rep_counts[rep_counts == 2])
once_entries <- names(rep_counts[rep_counts == 1])

field_book <- field_book |>
  mutate(
    EntryType = case_when(
      ENTRY %in% prep_entries ~ "p-rep (×2)",
      TRUE                    ~ "Once (×1)"
    )
  )

# ── Layout tile map ────────────────────────────────────────────────────────
ggplot(field_book,
       aes(x = COL, y = ROW, fill = EntryType)) +
  geom_tile(colour = "white", linewidth = 0.4, alpha = 0.85) +
  scale_fill_manual(
    values = c("p-rep (×2)" = "#E41A1C",
               "Once (×1)"  = "#4292C6")
  ) +
  scale_y_reverse() +
  labs(title    = "p-rep Design Field Layout",
       subtitle  = paste0(n_rows, " rows × ", n_cols,
                          " cols | Red = p-rep entries (×2) | Blue = once (×1)"),
       x = "Column", y = "Row",
       fill = "Entry type") +
  theme_minimal(base_size = 12) +
  theme(panel.grid   = element_blank(),
        legend.position = "bottom")
```

---

### Step 4 — Simulate Spatially Correlated Phenotypic Data

```r
# ── Simulate with AR1×AR1 spatial trend + genotype effects ────────────────
set.seed(123)

# Genotype true breeding values
n_unique   <- length(unique(field_book$ENTRY))
geno_names <- unique(field_book$ENTRY)
tbv        <- setNames(rnorm(n_unique, 0, 6), geno_names)

# AR1 spatial surface
ar1_surface <- function(nr, nc, rho_r = 0.7, rho_c = 0.6, sigma = 4) {
  mat <- matrix(0, nr, nc)
  for (r in 1:nr) for (c in 1:nc) {
    spatial_r <- if (r > 1) rho_r * mat[r-1, c] else 0
    spatial_c <- if (c > 1) rho_c * mat[r, c-1] else 0
    mat[r, c] <- (spatial_r + spatial_c) / 2 +
                  rnorm(1, 0, sigma * sqrt(1 - rho_r^2))
  }
  mat
}

spatial_mat <- ar1_surface(n_rows, n_cols,
                           rho_r = 0.75, rho_c = 0.65, sigma = 5)

field_book <- field_book |>
  mutate(
    spatial_eff = spatial_mat[cbind(ROW, COL)],
    Yield       = 55 + tbv[ENTRY] + spatial_eff + rnorm(n(), 0, 1.5)
  )

cat("Yield summary:\n")
print(summary(field_book$Yield))
```

---

### Step 5 — Exploratory Data Analysis

```r
# ── Overall distribution ───────────────────────────────────────────────────
p1 <- ggplot(field_book, aes(x = Yield)) +
  geom_histogram(bins = 30, fill = "#4292C6",
                 colour = "white", alpha = 0.8) +
  geom_vline(xintercept = mean(field_book$Yield),
             colour = "#E41A1C", linetype = "dashed",
             linewidth = 1) +
  labs(title = "Yield Distribution",
       x = "Yield (q/ha)", y = "Count") +
  theme_minimal(base_size = 12)

# ── Spatial heatmap of raw yield ──────────────────────────────────────────
p2 <- ggplot(field_book,
             aes(x = COL, y = ROW, fill = Yield)) +
  geom_tile(colour = NA) +
  scale_fill_viridis_c(option = "plasma") +
  scale_y_reverse() +
  labs(title = "Raw Yield Spatial Pattern",
       x = "Column", y = "Row",
       fill = "Yield\n(q/ha)") +
  theme_minimal(base_size = 12) +
  theme(panel.grid = element_blank())

p1 + p2 + plot_layout(ncol = 2)

# ── Row and column marginal means ─────────────────────────────────────────
p3 <- field_book |>
  group_by(ROW) |>
  summarise(Mean = mean(Yield)) |>
  ggplot(aes(ROW, Mean)) +
  geom_col(fill = "#4292C6", alpha = 0.8) +
  geom_smooth(method = "loess", se = FALSE,
              colour = "#E41A1C", linewidth = 1) +
  labs(title = "Row Marginal Means",
       x = "Row", y = "Mean Yield") +
  theme_minimal(base_size = 11)

p4 <- field_book |>
  group_by(COL) |>
  summarise(Mean = mean(Yield)) |>
  ggplot(aes(COL, Mean)) +
  geom_col(fill = "#74C476", alpha = 0.8) +
  geom_smooth(method = "loess", se = FALSE,
              colour = "#E41A1C", linewidth = 1) +
  labs(title = "Column Marginal Means",
       x = "Column", y = "Mean Yield") +
  theme_minimal(base_size = 11)

p3 + p4 + plot_layout(ncol = 2)
```

---

### Step 6 — Simple Mixed Model (No Spatial Component)

```r
# ── Baseline mixed model: genotype as random ──────────────────────────────
model_base <- lmer(
  Yield ~ (1 | ENTRY),
  data = field_book,
  REML = TRUE
)

# Variance components
vc_base    <- as.data.frame(VarCorr(model_base))
sigma2_g   <- vc_base[vc_base$grp == "ENTRY",    "vcov"]
sigma2_e   <- vc_base[vc_base$grp == "Residual", "vcov"]

cat("=== Baseline Mixed Model ===\n")
cat("σ²_g (genotype)  :", round(sigma2_g, 4), "\n")
cat("σ²_e (residual)  :", round(sigma2_e, 4), "\n")

# Heritability (approximate)
r_bar <- harmonic.mean <- 1 / mean(1 / table(field_book$ENTRY))
H2_base <- sigma2_g / (sigma2_g + sigma2_e / r_bar)
cat("H² (approximate) :", round(H2_base, 3), "\n")

# BLUPs — basic
blups_base <- ranef(model_base)$ENTRY
blups_base_df <- data.frame(
  ENTRY = rownames(blups_base),
  BLUP  = blups_base[["(Intercept)"]]
) |>
  arrange(desc(BLUP))
head(blups_base_df, 10)
```

---

### Step 7 — Spatial Model with SpATS (P-Splines)

SpATS fits a **2D P-spline surface** to model spatial trends continuously across
rows and columns, then separates genotype effects from the spatial trend.

```r
# ── SpATS spatial model ───────────────────────────────────────────────────
model_spats <- SpATS(
  response           = "Yield",
  genotype           = "ENTRY",
  genotype.as.random = TRUE,
  fixed              = NULL,
  spatial            = SAP(ROW, COL),
  data               = as.data.frame(field_book),
  control            = list(
    tolerance  = 1e-06,
    monitoring = 0
  )
)

summary(model_spats)
```

**Output:**

```
Variance components:
              Variance   Ratio
ENTRY         35.841    1.000
Residual       2.276    0.064

Effective dimensions:
  Intercept    ENTRY   f(ROW, COL)
   1.000     152.384      42.617

Heritability: 0.921
```

```r
# ── Extract variance components ───────────────────────────────────────────
vc_spats   <- model_spats$var.comp
sigma2_g_s <- vc_spats["ENTRY"]
sigma2_e_s <- vc_spats["Residual"]

cat("σ²_g (SpATS):", round(sigma2_g_s, 4), "\n")
cat("σ²_e (SpATS):", round(sigma2_e_s, 4), "\n")
cat("H²   (SpATS):", round(getHeritability(model_spats), 4), "\n")

# ── Model fit ─────────────────────────────────────────────────────────────
cat("AIC (SpATS)  :", round(model_spats$model$aic, 2), "\n")
```

---

### Step 8 — Spatial Diagnostics and Trend Plots

```r
# ── Plot spatial trend surface ────────────────────────────────────────────
plot(model_spats,
     main   = "SpATS Spatial Trend Surface",
     col    = viridis(100),
     spat.int.plot = TRUE)

# ── Residuals after spatial correction ───────────────────────────────────
resid_spats <- residuals(model_spats)
field_book$Resid_SpATS <- resid_spats

p_resid1 <- ggplot(field_book,
                   aes(x = COL, y = ROW, fill = Resid_SpATS)) +
  geom_tile(colour = NA) +
  scale_fill_gradient2(
    low      = "#d73027",
    mid      = "#ffffbf",
    high     = "#1a9850",
    midpoint = 0
  ) +
  scale_y_reverse() +
  labs(title = "Residuals After Spatial Correction",
       x = "Column", y = "Row",
       fill = "Residual") +
  theme_minimal(base_size = 12) +
  theme(panel.grid = element_blank())

p_resid2 <- ggplot(data.frame(resid = resid_spats),
                   aes(sample = resid)) +
  stat_qq(colour = "#2C7BB6", size = 2) +
  stat_qq_line(colour = "#E41A1C", linewidth = 1) +
  labs(title = "Q-Q Plot of Spatial Residuals",
       x = "Theoretical Quantiles",
       y = "Sample Quantiles") +
  theme_minimal(base_size = 12)

p_resid1 + p_resid2 + plot_layout(ncol = 2)

# ── Normality test ────────────────────────────────────────────────────────
shapiro.test(sample(resid_spats, min(50, length(resid_spats))))

# ── Autocorrelation check ─────────────────────────────────────────────────
# Row-direction autocorrelation of residuals
acf(resid_spats, main = "ACF of SpATS Residuals")
```

---

### Step 9 — AR1 × AR1 Model with sommer

```r
# ── AR1×AR1 via sommer ────────────────────────────────────────────────────
field_book$ROW_f <- factor(field_book$ROW)
field_book$COL_f <- factor(field_book$COL)

# AR1 structure in rows and columns
model_ar1 <- mmer(
  fixed  = Yield ~ 1,
  random = ~ vsr(ENTRY)
           + vsr(ROW_f, Gu = AR1(field_book$ROW_f, rho = 0.7))
           + vsr(COL_f, Gu = AR1(field_book$COL_f, rho = 0.6)),
  data   = field_book,
  verbose = FALSE
)

# Variance components
summary(model_ar1)$varcomp

# GBLUPs
gblups_ar1 <- randef(model_ar1)$`u:ENTRY`
cat("Top 10 genotypes (AR1×AR1):\n")
sort(gblups_ar1, decreasing = TRUE)[1:10]

# Heritability
vc_ar1     <- summary(model_ar1)$varcomp
sigma2_g_ar <- vc_ar1["u:ENTRY", "VarComp"]
sigma2_e_ar <- vc_ar1["units",   "VarComp"]
H2_ar1      <- sigma2_g_ar / (sigma2_g_ar + sigma2_e_ar / r_bar)
cat("\nH² (AR1×AR1):", round(H2_ar1, 3), "\n")
```

---

### Step 10 — Extract and Rank BLUPs

```r
# ── SpATS BLUPs ───────────────────────────────────────────────────────────
spats_preds <- predict(model_spats, which = "ENTRY")

blup_df <- data.frame(
  ENTRY  = spats_preds$ENTRY,
  BLUP   = spats_preds$predicted.values,
  SE     = spats_preds$standard.errors,
  PEV    = spats_preds$standard.errors^2,
  Acc    = sqrt(1 - spats_preds$standard.errors^2 /
                    (2 * sigma2_g_s))
) |>
  mutate(
    RepType = ifelse(ENTRY %in% prep_entries,
                     "p-rep (×2)", "Once (×1)"),
    BLUP_rank = rank(-BLUP)
  ) |>
  arrange(BLUP_rank)

cat("Top 15 genotypes by SpATS BLUP:\n")
print(head(blup_df[, c("ENTRY","BLUP","SE","Acc","RepType")], 15))

cat("\nMean accuracy — p-rep entries:",
    round(mean(blup_df$Acc[blup_df$RepType == "p-rep (×2)"]), 3), "\n")
cat("Mean accuracy — once entries :",
    round(mean(blup_df$Acc[blup_df$RepType == "Once (×1)"]),  3), "\n")
```

---

### Step 11 — Model Comparison (LRT)

```r
# ── Compare spatial vs non-spatial model ─────────────────────────────────
# Refit both with ML for LRT
model_null <- lmer(
  Yield ~ (1 | ENTRY),
  data  = field_book,
  REML  = FALSE
)

# Spatial model log-likelihood
ll_spats  <- model_spats$model$loglik
ll_null   <- logLik(model_null)[1]
LRT_stat  <- -2 * (ll_null - ll_spats)
LRT_pval  <- pchisq(LRT_stat, df = 2, lower.tail = FALSE)

cat("=== Likelihood Ratio Test: Spatial vs Non-spatial ===\n")
cat("LRT statistic :", round(LRT_stat, 3), "\n")
cat("df            : 2\n")
cat("p-value       :", format(LRT_pval, digits = 4), "\n")
cat(ifelse(LRT_pval < 0.05,
  "→ Spatial model significantly better — use SpATS.\n",
  "→ Spatial correction not needed — simple mixed model sufficient.\n"))
```

---

### Step 12 — Selection of Superior Genotypes

```r
# ── Selection intensity: top 10 % ────────────────────────────────────────
sel_frac   <- 0.10
n_select   <- ceiling(n_geno * sel_frac)
threshold  <- quantile(blup_df$BLUP, 1 - sel_frac)

selected   <- blup_df |>
  filter(BLUP >= threshold) |>
  arrange(desc(BLUP))

cat("Selection intensity (i)  : top", sel_frac * 100, "%\n")
cat("Threshold BLUP           :", round(threshold, 3), "\n")
cat("Genotypes selected       :", nrow(selected), "\n\n")

# ── Selection gain ────────────────────────────────────────────────────────
mu_all <- mean(blup_df$BLUP)
mu_sel <- mean(selected$BLUP)
sel_diff <- mu_sel - mu_all
cat(sprintf("Selection differential: %.3f q/ha\n", sel_diff))
cat(sprintf("Mean accuracy of selected: %.3f\n",
            mean(selected$Acc)))

# ── Selection gain with accuracy ─────────────────────────────────────────
# ΔG = i × σ_g × r_{g,ĝ}
i_val      <- mean(scale(selected$BLUP))    # selection intensity
sigma_g    <- sqrt(sigma2_g_s)
r_acc      <- mean(selected$Acc)
delta_G    <- i_val * sigma_g * r_acc
cat(sprintf("Expected ΔG = i × σ_g × r = %.3f × %.3f × %.3f = %.3f q/ha\n",
            i_val, sigma_g, r_acc, delta_G))

# ── Flag selected entries ─────────────────────────────────────────────────
blup_df <- blup_df |>
  mutate(Selected = ENTRY %in% selected$ENTRY)
```

---

### Step 13 — Publication-Quality Plots

#### BLUP ranking plot

```r
top_n_plot <- 40
plot_data  <- blup_df |>
  slice_head(n = top_n_plot)

ggplot(plot_data,
       aes(x = reorder(ENTRY, BLUP),
           y = BLUP,
           fill = RepType,
           alpha = Selected)) +
  geom_col(width = 0.75, colour = "grey20",
           linewidth = 0.3) +
  geom_errorbar(aes(ymin = BLUP - 1.96 * SE,
                    ymax = BLUP + 1.96 * SE),
                width = 0.3, linewidth = 0.6,
                colour = "grey30") +
  geom_hline(yintercept = threshold,
             linetype   = "dashed",
             colour     = "#E41A1C",
             linewidth  = 1) +
  scale_fill_manual(values = c("p-rep (×2)" = "#E41A1C",
                                "Once (×1)"  = "#4292C6")) +
  scale_alpha_manual(values = c(`TRUE` = 1.0, `FALSE` = 0.55)) +
  coord_flip() +
  labs(title    = paste0("Top ", top_n_plot,
                         " Genotypes — SpATS BLUPs"),
       subtitle = "Error bars = 95% CI | Dashed = 10% selection threshold",
       x        = NULL,
       y        = "BLUP (q/ha)",
       fill     = "Entry type",
       alpha    = "Selected") +
  theme_minimal(base_size = 11) +
  theme(plot.title = element_text(face = "bold"),
        legend.position = "bottom")
```

#### Accuracy distribution

```r
ggplot(blup_df,
       aes(x = Acc, fill = RepType)) +
  geom_histogram(bins = 25, alpha = 0.75,
                 position = "identity",
                 colour = "white") +
  scale_fill_manual(values = c("p-rep (×2)" = "#E41A1C",
                                "Once (×1)"  = "#4292C6")) +
  geom_vline(data = blup_df |>
               group_by(RepType) |>
               summarise(m = mean(Acc)),
             aes(xintercept = m, colour = RepType),
             linewidth = 1.2, linetype = "dashed") +
  scale_colour_manual(values = c("p-rep (×2)" = "#8B0000",
                                  "Once (×1)"  = "#003580")) +
  labs(title    = "BLUP Prediction Accuracy Distribution",
       subtitle = "p-rep entries have higher accuracy than once-replicated",
       x = "Accuracy", y = "Count",
       fill = "Entry type", colour = "Entry type") +
  theme_minimal(base_size = 13)
```

#### BLUP vs raw mean comparison

```r
raw_means <- field_book |>
  group_by(ENTRY) |>
  summarise(Raw_Mean = mean(Yield)) |>
  left_join(select(blup_df, ENTRY, BLUP, RepType),
            by = "ENTRY")

ggplot(raw_means,
       aes(x = Raw_Mean,
           y = BLUP + mean(field_book$Yield),
           colour = RepType)) +
  geom_abline(slope = 1, intercept = 0,
              linetype = "dashed", colour = "grey50") +
  geom_point(alpha = 0.7, size = 2.5) +
  scale_colour_manual(values = c("p-rep (×2)" = "#E41A1C",
                                  "Once (×1)"  = "#4292C6")) +
  labs(title    = "Raw Means vs BLUP-Adjusted Means",
       subtitle = "Shrinkage towards grand mean is visible for once-replicated entries",
       x = "Raw Mean (q/ha)",
       y = "BLUP + Grand Mean (q/ha)",
       colour = "Entry type") +
  theme_minimal(base_size = 13)
```

---

### Step 14 — Replicate Fraction Optimisation

How does the p-rep fraction affect heritability and accuracy?

```r
# ── Simulate different p-rep fractions ────────────────────────────────────
p_fractions <- seq(0.10, 0.50, by = 0.05)

sim_results <- lapply(p_fractions, function(p) {
  n_prep_i <- round(n_geno * p)
  r_bar_i  <- (n_prep_i * 2 + (n_geno - n_prep_i) * 1) / n_geno
  H2_i     <- sigma2_g_s / (sigma2_g_s + sigma2_e_s / r_bar_i)
  data.frame(
    p_fraction = p,
    n_prep     = n_prep_i,
    total_plots = n_geno + n_prep_i,
    r_bar      = round(r_bar_i, 3),
    H2         = round(H2_i, 3)
  )
})

opt_df <- bind_rows(sim_results)
print(opt_df)

ggplot(opt_df, aes(x = p_fraction)) +
  geom_line(aes(y = H2,
                colour = "Heritability (H²)"),
            linewidth = 1.2) +
  geom_point(aes(y = H2,
                 colour = "Heritability (H²)"),
             size = 3) +
  geom_line(aes(y = total_plots / (n_geno * 2),
                colour = "Relative plot cost"),
            linewidth = 1.2, linetype = "dashed") +
  scale_colour_manual(
    values = c("Heritability (H²)"   = "#2C7BB6",
               "Relative plot cost"  = "#E41A1C")
  ) +
  scale_x_continuous(labels = scales::percent) +
  labs(title    = "Trade-off: p-rep Fraction vs Heritability & Plot Cost",
       subtitle = "Choose p that maximises H² without excessive plot cost",
       x = "p-rep fraction (p)",
       y = "H² / Relative cost",
       colour = NULL) +
  theme_minimal(base_size = 13) +
  theme(legend.position = "bottom")
```

---

### Step 15 — Power Analysis

```r
library(pwr)

# Approximate power: treat as one-way ANOVA with n reps
# Median replication = 1.25 × for 25% p-rep
r_effective <- (n_geno + round(n_geno * p_rep)) / n_geno

# Effect size from variance components
f_eff <- sqrt(sigma2_g_s / sigma2_e_s)
cat("Cohen's f (from SpATS components):", round(f_eff, 3), "\n")

pwr.anova.test(k         = n_geno,
               n         = round(r_effective),
               f         = f_eff,
               sig.level = 0.05)

# Power by p-rep fraction
pw_vec <- sapply(p_fractions, function(p) {
  r_eff <- 1 + p
  pwr.anova.test(k = n_geno, n = round(r_eff),
                 f = f_eff, sig.level = 0.05)$power
})

ggplot(data.frame(p = p_fractions, power = pw_vec),
       aes(p, power)) +
  geom_line(colour = "#2C7BB6", linewidth = 1.2) +
  geom_point(size = 3, colour = "#2C7BB6") +
  geom_hline(yintercept = 0.80,
             linetype = "dashed",
             colour   = "#E41A1C",
             linewidth = 1) +
  scale_x_continuous(labels = scales::percent) +
  labs(title = "Power vs p-rep Fraction",
       x = "p-rep fraction", y = "Power") +
  theme_minimal(base_size = 13)
```

---

## 8. Non-Spatial Fallback

If SpATS fails to converge or $n$ is small, use a simple mixed model with row/column
as fixed effects:

```r
# ── Row + column fixed, genotype random ───────────────────────────────────
model_rowcol <- lmer(
  Yield ~ factor(ROW) + factor(COL) + (1 | ENTRY),
  data = field_book,
  REML = TRUE
)

# Variance components
print(VarCorr(model_rowcol))

# BLUPs
rc_blups <- ranef(model_rowcol)$ENTRY
rc_blup_df <- data.frame(
  ENTRY = rownames(rc_blups),
  BLUP  = rc_blups[["(Intercept)"]]
) |> arrange(desc(BLUP))

head(rc_blup_df, 10)
```

---

## 9. Complete Analysis Workflow

```
1. Define n (genotypes) and p (replication fraction)
   Target: N = n(1 + p) ≤ field capacity
          │
          ▼
2. Generate layout ── partially_replicated() [FielDHub]
   Ensure p-rep entries are spatially dispersed
          │
          ▼
3. Visualise layout ── tile map (p-rep vs once)
          │
          ▼
4. Collect field data
          │
          ▼
5. EDA ── distribution, spatial heatmap,
          row/column marginal means
          │
          ▼
6. Baseline mixed model ── lmer(y ~ (1|ENTRY))
   Approximate H², BLUPs (ignores spatial)
          │
          ▼
7. Spatial model ── SpATS(SAP(ROW, COL))
   H², spatial trend surface, residual diagnostics
          │
          ▼
8. AR1×AR1 model ── sommer::mmer() [optional]
   Compare with SpATS using AIC/LRT
          │
          ▼
9. LRT ── spatial vs non-spatial
   Keep spatial if p < 0.05
          │
          ▼
10. Extract BLUPs & accuracy ── predict(model_spats)
    Check: mean accuracy, PEV distribution
          │
          ▼
11. Select superior genotypes
    Threshold = grand mean + selection intensity
    Compute ΔG = i × σ_g × r_{g,ĝ}
          │
          ▼
12. Publication plots ── BLUP ranking,
    accuracy distribution, raw vs adjusted
```

---

## 10. Key Differences from Related Designs

| Feature | Augmented | Alpha Lattice | **p-rep** |
|---|---|---|---|
| Test entry replication | None (once) | Partial (all entries) | ~20–30 % |
| Error estimation | Checks only | Incomplete blocks | Spatial residuals |
| Spatial modelling | Basic | Limited | **Full AR1×AR1 / P-splines** |
| Genotype BLUPs | Adjusted means | BLUPs | **BLUPs with PEV** |
| Heritability | Approximate | From VC | **From SpATS** |
| Entries per trial | 50 – 1000 | 20 – 500 | **100 – 5000** |
| Accuracy | Low | Moderate | **High** |
| Best stage | Early screen | Mid-stage | **Stage 1 MET** |
| R packages | `agricolae` | `lme4` | `FielDHub` + `SpATS` |

---

## 11. Summary Table

| Parameter | Value (example) |
|---|---|
| Genotypes ($n$) | 200 |
| p-rep fraction ($p$) | 25 % |
| p-rep entries | 50 |
| Total plots ($N$) | 270 |
| Field dimensions | 15 × 18 |
| $\hat{\sigma}^2_g$ | 35.84 |
| $\hat{\sigma}^2_e$ | 2.28 |
| H² (SpATS) | 0.921 |
| Mean accuracy (p-rep) | 0.961 |
| Mean accuracy (once) | 0.943 |
| CV (%) | 2.7 |

---

## 12. References

- Cullis, B. R., Smith, A. B., & Coombes, N. E. (2006). On the design of early generation
  variety trials with correlated data. *Journal of Agricultural, Biological and
  Environmental Statistics*, 11(4), 381–393.
- Rodríguez-Álvarez, M. X., Boer, M. P., van Eeuwijk, F. A., & Eilers, P. H. C. (2018).
  Correcting for spatial heterogeneity in plant breeding experiments with P-splines.
  *Spatial Statistics*, 23, 52–71.
- Smith, A. B., Cullis, B. R., & Thompson, R. (2005). The analysis of crop cultivar
  breeding and evaluation trials: an overview of current mixed model approaches.
  *Journal of Agricultural Science*, 143(6), 449–462.
- Mramba, L., Wolfe, M., & Kramer, M. (2019). FielDHub: A Shiny App for Design of
  Experiments in Life Sciences. CRAN.
- Covarrubias-Pazaran, G. (2016). Genome-assisted prediction of quantitative traits
  using the R package sommer. *PLOS ONE*, 11(6).
- R Core Team (2026). *R: A Language and Environment for Statistical Computing*.

---]]></content><author><name>Dr. M. Shamshad</name><email>shamshad.rattan@gmail.com</email></author><category term="statistics" /><category term="R" /><category term="field-experiments" /><category term="p-rep" /><category term="partially-replicated" /><category term="plant-breeding" /><category term="spatial-analysis" /><category term="BLUPs" /><category term="SpATS" /><category term="FielDHub" /><category term="R" /><summary type="html"><![CDATA[The Partially Replicated (p-rep) design — formally developed by Cullis, Smith &amp; Coombes (2006) — is the modern standard for Stage 1 multi-environment plant breeding trials. It overcomes the key limitation of the augmented design (zero replication of test entries) by replicating a controlled fraction (typically 20–30 %) of test entries twice, while the remainder appear only once. This provides direct within-trial error estimation for all genotypes and enables powerful spatial modelling of field heterogeneity.]]></summary></entry><entry><title type="html">Augmented Design: Theory &amp;amp; Complete R Analysis</title><link href="https://shamshad.in/posts/2026/02/augmented-design-analysis/" rel="alternate" type="text/html" title="Augmented Design: Theory &amp;amp; Complete R Analysis" /><published>2026-02-24T00:00:00+05:30</published><updated>2026-05-10T14:53:38+05:30</updated><id>https://shamshad.in/posts/2026/02/augmented-design-analysis</id><content type="html" xml:base="https://shamshad.in/posts/2026/02/augmented-design-analysis/"><![CDATA[The **Augmented Design** — proposed by **Federer (1956)** — is a field experimental design specifically developed for **early-generation plant breeding** trials, where a large number of **new (unreplicated) test entries** are evaluated alongside a small set of **replicated check (standard) varieties**. It allows breeders to screen hundreds of
genotypes within a single trial without the cost of fully replicating every entry, while still enabling valid statistical inference through the checks.
<!--more-->

---

## 1. Concept and Rationale

In early breeding stages, thousands of new lines must be evaluated quickly and cheaply.
Full replication of every entry is impractical. The augmented design solves this by:

- **Check varieties** (standards/controls): appear in **every block** — their replication
  enables estimation of block effects and experimental error.
- **Test entries** (new genotypes): appear in **only one block** — they are evaluated
  relative to the checks in that block, correcting for spatial variation.

The checks act as a **calibration standard** across blocks, linking all test entries
to a common baseline.

---

## 2. Design Structure

| Entry type | Replication | Purpose |
|---|---|---|
| Check varieties ($c$) | $r$ times (once per block) | Estimate block effects and error |
| Test entries ($t$) | Once only | Screening new material |
| Total blocks | $r$ | — |
| Plots per block | $c + n_i$ | $c$ checks + $n_i$ test entries in block $i$ |
| Total plots | $rc + t$ | — |

**Requirements:**
- At least **2 check varieties** (3–5 recommended for reliable error estimation)
- At least **3 blocks** (more blocks → better spatial control)
- All checks must appear in **every** block

---

## 3. Linear Model

$$y_{ijk} = \mu + \gamma_i + \beta_j + \varepsilon_{ijk}$$

where the entry effect $\gamma_i$ covers **both** checks and test entries:

$$\gamma_i = \begin{cases} \tau_i & \text{if check variety } i \\ \delta_i & \text{if test entry } i \end{cases}$$

| Symbol | Meaning |
|---|---|
| $\mu$ | Grand mean |
| $\gamma_i$ | Effect of entry $i$ (check or test) |
| $\beta_j$ | Effect of block $j$ |
| $\varepsilon_{ijk}$ | Error; $\varepsilon \sim \mathcal{N}(0, \sigma^2)$ |

---

## 4. Hypotheses

### Treatment (entry) effect

$$H_0: \gamma_1 = \gamma_2 = \cdots = \gamma_{c+t} = 0$$

$$H_1: \gamma_i \neq 0 \quad \text{for at least one entry } i$$

### Block effect

$$H_0: \beta_1 = \beta_2 = \cdots = \beta_r = 0$$

$$H_1: \beta_j \neq 0 \quad \text{for at least one } j$$

### Check vs test entries

$$H_0: \bar{\mu}_{\text{check}} = \bar{\mu}_{\text{test}}$$

$$H_1: \bar{\mu}_{\text{check}} \neq \bar{\mu}_{\text{test}}$$

---

## 5. ANOVA Table

The ANOVA is partitioned into **checks** and **test entries** components:

| Source | df | SS | MS | F |
|---|---|---|---|---|
| Blocks (unadj.) | $r - 1$ | $SS_B$ | $MS_B$ | $MS_B / MS_E$ |
| Entries (adj.) | $c + t - 1$ | $SS_{Ent}$ | $MS_{Ent}$ | $MS_{Ent} / MS_E$ |
| — Checks (adj.) | $c - 1$ | $SS_C$ | $MS_C$ | $MS_C / MS_E$ |
| — Test entries (adj.) | $t - 1$ | $SS_T$ | $MS_T$ | $MS_T / MS_E$ |
| — Checks vs Tests | $1$ | $SS_{CvT}$ | $MS_{CvT}$ | $MS_{CvT} / MS_E$ |
| Error | $(c-1)(r-1)$ | $SS_E$ | $MS_E$ | — |
| Total | $rc + t - 1$ | $SS_{Tot}$ | — | — |

**Error degrees of freedom:**

$$df_E = (c - 1)(r - 1)$$

> This means error df depends **only** on the number of checks and blocks —
> adding more test entries does **not** increase error df.

---

## 6. Adjusted Means

Since test entries appear only once, their raw means are biased by block effects.
**Adjusted means** correct for the block in which each test entry appeared:

$$\hat{\mu}_i^{\text{adj}} = y_{ij} - \hat{\beta}_j + \bar{\hat{\beta}}$$

where $\hat{\beta}_j$ is the estimated block effect and $\bar{\hat{\beta}}$ is the mean
block effect. In practice, this is obtained directly from the model as:

$$\hat{\mu}_i^{\text{adj}} = \hat{\mu} + \hat{\gamma}_i$$

---

## 7. Coefficient of Variation

$$CV = \frac{\sqrt{MS_E}}{\bar{y}_{\text{checks}}} \times 100$$

Note: CV for augmented designs is typically computed on **check means only**,
since checks are replicated.

---

## 8. Full R Analysis

### Step 1 — Packages

```r
pkgs <- c("agricolae", "lme4", "lmerTest", "emmeans",
          "ggplot2", "dplyr", "tidyr", "car",
          "multcomp", "multcompView", "effectsize",
          "tibble", "ggrepel")
install.packages(setdiff(pkgs, rownames(installed.packages())))

library(agricolae)
library(lme4)
library(lmerTest)
library(emmeans)
library(ggplot2)
library(dplyr)
library(tidyr)
library(car)
library(multcomp)
library(multcompView)
library(effectsize)
library(tibble)
library(ggrepel)
```

---

### Step 2 — Generate Augmented Design Layout

```r
# ── Design: 3 checks, 30 test entries, 3 blocks ──────────────────────────
set.seed(42)
n_checks  <- 3
n_test    <- 30
n_blocks  <- 3

checks <- paste0("Check", 1:n_checks)
tests  <- paste0("G",     sprintf("%02d", 1:n_test))

aug_design <- design.augmented(
  trt    = tests,
  checks = checks,
  r      = n_blocks,
  seed   = 42
)

field_book <- aug_design$book
cat("Augmented Design Summary\n")
cat("  Check varieties :", n_checks, "\n")
cat("  Test entries    :", n_test,   "\n")
cat("  Blocks          :", n_blocks, "\n")
cat("  Error df        :", (n_checks - 1) * (n_blocks - 1), "\n")
cat("  Total plots     :", nrow(field_book), "\n\n")

# First few rows
head(field_book, 15)
```

**Output:**

```
Augmented Design Summary
  Check varieties : 3
  Test entries    : 30
  Blocks          : 3
  Error df        : 4
  Total plots     : 39

   plots block      trt
1    101     1   Check1
2    102     1   Check2
3    103     1   Check3
4    104     1      G01
5    105     1      G02
...
```

---

### Step 3 — Field Layout Visualisation

```r
# ── Assign row/column for display ─────────────────────────────────────────
plots_per_block <- field_book |>
  group_by(block) |>
  summarise(n = n()) |>
  pull(n)

field_book <- field_book |>
  mutate(
    Block    = factor(block),
    Type     = ifelse(grepl("Check", trt), "Check", "Test"),
    Col      = ave(seq_len(nrow(field_book)),
                   block,
                   FUN = seq_along),
    Row      = as.integer(Block)
  )

ggplot(field_book,
       aes(x = Col, y = Row,
           fill = Type, label = trt)) +
  geom_tile(colour = "white", linewidth = 1.5, alpha = 0.85) +
  geom_text(size = 2.8, fontface = "bold") +
  scale_fill_manual(values = c(Check = "#E41A1C",
                                Test  = "#377EB8")) +
  scale_y_reverse(breaks  = 1:n_blocks,
                  labels  = paste("Block", 1:n_blocks)) +
  labs(title    = "Augmented Design Field Layout",
       subtitle = "Red = Check varieties (replicated) | Blue = Test entries (unreplicated)",
       x = "Plot position within block",
       y = NULL,
       fill = "Entry type") +
  theme_minimal(base_size = 12) +
  theme(panel.grid = element_blank())
```

---

### Step 4 — Simulate Yield Data

```r
# ── True effects ──────────────────────────────────────────────────────────
set.seed(7)

# Check variety effects
check_eff <- setNames(c(0, 5, 3), checks)

# Test entry effects (spanning a range of breeding value)
test_eff  <- setNames(
  rnorm(n_test, mean = 2, sd = 6),
  tests
)

# Block effects (fertility gradient)
block_eff <- c(0, -3, 4)

all_eff <- c(check_eff, test_eff)

field_book <- field_book |>
  mutate(
    Yield = 50
      + all_eff[trt]
      + block_eff[as.integer(Block)]
      + rnorm(n(), 0, 2.0)
  )

cat("Grand mean (all entries)  :", round(mean(field_book$Yield), 3), "\n")
cat("Mean yield — Checks       :",
    round(mean(field_book$Yield[field_book$Type == "Check"]), 3), "\n")
cat("Mean yield — Test entries :",
    round(mean(field_book$Yield[field_book$Type == "Test"]),  3), "\n")
```

---

### Step 5 — Exploratory Data Analysis

```r
# ── Check variety summary (replicated) ───────────────────────────────────
check_summary <- field_book |>
  filter(Type == "Check") |>
  group_by(trt) |>
  summarise(
    n    = n(),
    Mean = round(mean(Yield), 3),
    SD   = round(sd(Yield),   3),
    SE   = round(sd(Yield) / sqrt(n()), 3)
  )
print(check_summary)

# ── Block summary ─────────────────────────────────────────────────────────
blk_summary <- field_book |>
  group_by(Block) |>
  summarise(
    n_check = sum(Type == "Check"),
    n_test  = sum(Type == "Test"),
    Mean    = round(mean(Yield), 3),
    SD      = round(sd(Yield),   3)
  )
print(blk_summary)

# ── Yield distribution: checks vs tests ──────────────────────────────────
ggplot(field_book, aes(x = Yield, fill = Type)) +
  geom_histogram(bins = 15, alpha = 0.7,
                 position = "identity",
                 colour = "white") +
  scale_fill_manual(values = c(Check = "#E41A1C",
                                Test  = "#377EB8")) +
  geom_vline(data = field_book |>
               group_by(Type) |>
               summarise(m = mean(Yield)),
             aes(xintercept = m, colour = Type),
             linewidth = 1.2, linetype = "dashed") +
  scale_colour_manual(values = c(Check = "#8B0000",
                                  Test  = "#003580")) +
  labs(title    = "Yield Distribution — Checks vs Test Entries",
       subtitle = "Dashed lines = group means",
       x = "Yield (q/ha)", y = "Count",
       fill = "Type", colour = "Type") +
  theme_minimal(base_size = 13)
```

---

### Step 6 — ANOVA (Fixed Effects)

```r
# ── Standard augmented ANOVA via agricolae ────────────────────────────────
model_aug <- audpc(aug_design, field_book$Yield)

# Alternative: manual aov (more flexible)
model_aov  <- aov(Yield ~ trt + Block, data = field_book)
anova_full <- summary(model_aov)
print(anova_full)

# ── Decompose entries into checks, tests, checks-vs-tests ─────────────────
field_book <- field_book |>
  mutate(EntryType = ifelse(grepl("Check", trt),
                            "Check", "Test"))

# Check means model
model_checks <- aov(
  Yield ~ trt + Block,
  data = filter(field_book, Type == "Check")
)
anova_checks <- summary(model_checks)

# ── MS_E from checks only ─────────────────────────────────────────────────
MS_E   <- anova_checks[[1]]["Residuals", "Mean Sq"]
df_E   <- anova_checks[[1]]["Residuals", "Df"]
cat("\nMS Error (from checks) :", round(MS_E, 3), "\n")
cat("Error df               :", df_E, "\n")

# ── Grand mean and CV (check-based) ──────────────────────────────────────
grand_mean_chk <- mean(field_book$Yield[field_book$Type == "Check"])
CV  <- sqrt(MS_E) / grand_mean_chk * 100
cat("Grand mean (checks)    :", round(grand_mean_chk, 3), "\n")
cat("CV (%)                 :", round(CV, 2), "\n")
```

**Output:**

```
            Df Sum Sq Mean Sq F value   Pr(>F)
trt         32 2184.3   68.26   17.43  < 2e-16 ***
Block        2  196.4   98.20   25.07  3.2e-05 ***
Residuals    4   15.7    3.91

MS Error (from checks) : 3.912
Error df               : 4
Grand mean (checks)    : 53.847
CV (%)                 : 3.68
```

> **Note:** With only 4 error df, estimates are imprecise. Increase checks or blocks to
> improve reliability. Aim for $df_E \geq 12$ in practice.

---

### Step 7 — Adjusted Means for All Entries

```r
# ── Block effect estimates ────────────────────────────────────────────────
block_estimates <- coef(model_aov)[grepl("Block", names(coef(model_aov)))]
block_effects_est <- c(0, block_estimates)   # Block 1 = reference

# Compute adjusted means manually
adj_means <- field_book |>
  mutate(
    blk_num   = as.integer(Block),
    blk_adj   = block_effects_est[blk_num],
    mean_blk  = mean(block_effects_est),
    Yield_adj = Yield - blk_adj + mean_blk
  ) |>
  group_by(trt, Type) |>
  summarise(
    Raw_Mean = round(mean(Yield),     3),
    Adj_Mean = round(mean(Yield_adj), 3),
    .groups  = "drop"
  ) |>
  arrange(desc(Adj_Mean))

print(adj_means, n = 15)
```

---

### Step 8 — emmeans Adjusted Means

```r
# ── Estimated marginal means (adjusts for block) ──────────────────────────
emm_all <- emmeans(model_aov, ~ trt)
emm_df  <- as.data.frame(emm_all) |>
  rename(Entry = trt, Adj_Mean = emmean) |>
  mutate(Type = ifelse(grepl("Check", Entry), "Check", "Test")) |>
  arrange(desc(Adj_Mean))

cat("Top 10 entries by adjusted mean:\n")
print(head(emm_df, 10))

# ── Contrast: checks vs test entries ─────────────────────────────────────
field_book$TypeF <- factor(field_book$Type)
model_type <- aov(Yield ~ TypeF + Block, data = field_book)
emm_type   <- emmeans(model_type, ~ TypeF)
pairs(emm_type)
```

---

### Step 9 — Post-Hoc Comparisons

#### 9a. LSD — Test entries vs each check

```r
# ── LSD between all entries ───────────────────────────────────────────────
lsd_aug <- LSD.test(model_aov, "trt",
                     p.adj   = "bonferroni",
                     console = FALSE)

lsd_groups <- lsd_aug$groups |>
  rownames_to_column("Entry") |>
  mutate(Type = ifelse(grepl("Check", Entry), "Check", "Test")) |>
  arrange(desc(Yield))

head(lsd_groups, 15)
```

#### 9b. Dunnett — All test entries vs best check

```r
# ── Dunnett: compare everything to Check1 ────────────────────────────────
library(multcomp)
dunnett_aug <- glht(model_aov,
                    linfct = mcp(trt = "Dunnett"),
                    base   = which(levels(factor(field_book$trt)) == "Check1"))
summary(dunnett_aug)
```

#### 9c. Tukey HSD among checks only

```r
tukey_chk <- TukeyHSD(model_checks, "trt")
print(tukey_chk)
plot(tukey_chk, las = 1, col = "#E41A1C",
     main = "Tukey HSD — Checks Only")
```

---

### Step 10 — Mixed Model with BLUPs

Treating blocks as random allows BLUPs for all entries — especially useful when
blocks are considered a random sample of environments:

```r
# ── Mixed model: block as random ──────────────────────────────────────────
model_mixed <- lmer(
  Yield ~ trt + (1 | Block),
  data = field_book,
  REML = TRUE
)

# Fixed effects table
anova(model_mixed, ddf = "Kenward-Roger")

# Variance components
print(VarCorr(model_mixed))

# BLUPs for all entries
emm_mixed <- emmeans(model_mixed, ~ trt)
blup_df   <- as.data.frame(emm_mixed) |>
  rename(Entry = trt, BLUP = emmean) |>
  mutate(Type = ifelse(grepl("Check", Entry), "Check", "Test")) |>
  arrange(desc(BLUP))

cat("\nTop 10 entries (BLUP-adjusted):\n")
print(head(blup_df, 10))
```

---

### Step 11 — Selection of Superior Test Entries

```r
# ── Selection threshold: mean of best check ───────────────────────────────
best_check_mean <- emm_df |>
  filter(Type == "Check") |>
  pull(Adj_Mean) |>
  max()

# Select test entries that exceed the best check
superior <- emm_df |>
  filter(Type == "Test", Adj_Mean >= best_check_mean)

cat("Best check mean      :", round(best_check_mean, 3), "q/ha\n")
cat("Superior test entries:", nrow(superior), "\n\n")
print(superior)

# ── Alternatively: select top 10% of test entries ─────────────────────────
top10_thresh <- quantile(
  emm_df$Adj_Mean[emm_df$Type == "Test"],
  probs = 0.90
)

top10 <- emm_df |>
  filter(Type == "Test", Adj_Mean >= top10_thresh)

cat("\nTop 10% test entries (n =", nrow(top10), "):\n")
print(top10)

# ── Selection gain ────────────────────────────────────────────────────────
mean_all_test <- mean(emm_df$Adj_Mean[emm_df$Type == "Test"])
mean_selected <- mean(top10$Adj_Mean)
sel_gain      <- mean_selected - mean_all_test

cat(sprintf("\nSelection gain: %.3f q/ha (%.1f%% above mean)\n",
            sel_gain, sel_gain / mean_all_test * 100))
```

---

### Step 12 — Publication-Quality Plots

#### Ranked entry plot

```r
ggplot(emm_df,
       aes(x = reorder(Entry, Adj_Mean),
           y = Adj_Mean,
           fill = Type)) +
  geom_col(alpha = 0.85, width = 0.75,
           colour = "grey20", linewidth = 0.3) +
  geom_errorbar(aes(ymin = lower.CL, ymax = upper.CL),
                width = 0.3, linewidth = 0.6,
                colour = "grey30") +
  geom_hline(yintercept = best_check_mean,
             linetype = "dashed",
             colour   = "#E41A1C",
             linewidth = 1) +
  annotate("text",
           x     = 3,
           y     = best_check_mean + 1.5,
           label = paste("Best check =",
                         round(best_check_mean, 1)),
           colour = "#E41A1C", size = 3.5) +
  scale_fill_manual(values = c(Check = "#E41A1C",
                                Test  = "#4292C6")) +
  coord_flip() +
  labs(title    = "Adjusted Entry Means — Augmented Design",
       subtitle = "Red dashed = best check threshold | Error bars = 95% CI",
       x = NULL,
       y = "Adjusted Mean Yield (q/ha)",
       fill = "Entry Type") +
  theme_minimal(base_size = 11) +
  theme(plot.title = element_text(face = "bold"))
```

#### Raw vs adjusted scatter

```r
ggplot(adj_means,
       aes(x = Raw_Mean, y = Adj_Mean,
           colour = Type, label = trt)) +
  geom_abline(slope = 1, intercept = 0,
              linetype = "dashed", colour = "grey50") +
  geom_point(size = 3, alpha = 0.8) +
  ggrepel::geom_text_repel(
    data   = filter(adj_means, Type == "Check"),
    size   = 3.5, fontface = "bold",
    max.overlaps = 10
  ) +
  scale_colour_manual(values = c(Check = "#E41A1C",
                                  Test  = "#4292C6")) +
  labs(title    = "Raw Means vs Block-Adjusted Means",
       subtitle = "Points above diagonal → upward adjustment for poor block",
       x = "Raw Mean (q/ha)",
       y = "Adjusted Mean (q/ha)",
       colour = "Entry Type") +
  theme_minimal(base_size = 13)
```

#### Check consistency across blocks

```r
check_data <- filter(field_book, Type == "Check")

ggplot(check_data,
       aes(x = Block, y = Yield,
           group = trt, colour = trt)) +
  geom_line(linewidth = 1.2, alpha = 0.9) +
  geom_point(size = 4) +
  geom_text(aes(label = round(Yield, 1)),
            vjust = -1, size = 3.2) +
  scale_colour_manual(values = c("#E41A1C", "#FF7F00", "#984EA3")) +
  labs(title    = "Check Variety Performance Across Blocks",
       subtitle = "Consistent ranking → reliable block effect estimation",
       x = "Block", y = "Yield (q/ha)",
       colour = "Check") +
  theme_minimal(base_size = 13)
```

---

### Step 13 — Assumptions Diagnostics

```r
# ── Residuals (based on checks model) ────────────────────────────────────
par(mfrow = c(2, 2))
plot(model_checks, which = 1:4)
par(mfrow = c(1, 1))

# Shapiro-Wilk on check residuals
shapiro.test(residuals(model_checks))

# Levene's test (checks only)
leveneTest(Yield ~ trt, data = filter(field_book, Type == "Check"))

# ── Q-Q plot ──────────────────────────────────────────────────────────────
ggplot(data.frame(resid = residuals(model_checks)),
       aes(sample = resid)) +
  stat_qq(colour = "#2C7BB6", size = 2.5) +
  stat_qq_line(colour = "#E41A1C", linewidth = 1) +
  labs(title = "Normal Q-Q Plot — Check Residuals",
       x = "Theoretical Quantiles",
       y = "Sample Quantiles") +
  theme_minimal(base_size = 13)
```

---

### Step 14 — Increasing Error df

A critical limitation of augmented designs is the small $df_E = (c-1)(r-1)$.
The table below shows how to improve it:

```r
# ── df_E as a function of checks and blocks ───────────────────────────────
df_table <- expand.grid(
  Checks = 2:6,
  Blocks = 2:8
) |>
  mutate(df_E = (Checks - 1) * (Blocks - 1))

ggplot(df_table,
       aes(x = Blocks, y = df_E,
           colour = factor(Checks),
           group  = factor(Checks))) +
  geom_line(linewidth = 1.1) +
  geom_point(size = 2.5) +
  geom_hline(yintercept = 12,
             linetype = "dashed",
             colour   = "#E41A1C") +
  annotate("text", x = 7.5, y = 13.5,
           label = "df_E = 12 (minimum recommended)",
           colour = "#E41A1C", size = 3.5) +
  scale_colour_brewer(palette = "Set1") +
  labs(title    = "Error df as Function of Checks and Blocks",
       subtitle = "Aim for df_E ≥ 12 for reliable F-tests",
       x = "Number of Blocks",
       y = "Error df = (c−1)(r−1)",
       colour = "Checks (c)") +
  theme_minimal(base_size = 13)
```

---

### Step 15 — Spatial Adjustment (Row-Column Model)

When test entries are confounded with spatial position within blocks, fit a
row-column spatial model using `lme4`:

```r
# ── Assign spatial coordinates ────────────────────────────────────────────
field_book <- field_book |>
  mutate(
    Row_pos = ((as.integer(Block) - 1) * ceiling(n_test / n_blocks + n_checks)) +
               as.integer(factor(trt)),
    Col_pos = 1L
  )

# Spatial model with SpATS (if available)
if (requireNamespace("SpATS", quietly = TRUE)) {
  library(SpATS)
  spatial_aug <- SpATS(
    response           = "Yield",
    genotype           = "trt",
    genotype.as.random = FALSE,
    fixed              = ~ Block,
    spatial            = SAP(Row_pos, Col_pos),
    data               = field_book,
    control            = list(tolerance = 1e-04)
  )
  summary(spatial_aug)
  spatial_preds <- predict(spatial_aug, which = "trt") |>
    arrange(desc(predicted.values)) |>
    head(10)
  print(spatial_preds)
}
```

---

## 9. Complete Analysis Workflow

```
1. Define checks (c ≥ 3) and blocks (r ≥ 3)
          │
          ▼
2. Set target df_E = (c−1)(r−1) ≥ 12
          │
          ▼
3. Generate layout ── design.augmented()
          │
          ▼
4. Visualise layout ── tile map (checks vs tests)
          │
          ▼
5. Collect field data
          │
          ▼
6. EDA ── check means, block summary, distributions
          │
          ▼
7. ANOVA ── aov(y ~ trt + Block)
          │   MS_E from checks, CV, F-tests
          ▼
8. Adjusted means ── emmeans(), manual block correction
          │
          ▼
9. Post-hoc ── LSD, Dunnett vs best check
          │
          ▼
10. Assumptions ── Shapiro, Levene (checks model)
          │
          ▼
11. Mixed model ── lmer(y ~ trt + (1|Block)), BLUPs
          │
          ▼
12. Select superior entries ── threshold = best check mean
          │
          ▼
13. Publication plots ── ranked means, raw vs adj, check profiles
```

---

## 10. Design Variants

| Variant | Description |
|---|---|
| Augmented RCBD | Blocks = complete for checks + partial for tests (standard) |
| Augmented CRD | No blocks — only check replication; use only when field is uniform |
| Augmented Row-Column | Adds column blocking for two-directional control |
| p-rep (partially replicated) | Replicate a fraction of test entries for better error estimation |

---

## 11. Comparison with Related Designs

| Feature | RCBD | Augmented | Alpha Lattice | p-rep |
|---|---|---|---|---|
| Test entry replication | Complete | None (once) | Partial | ~20–30 % |
| Check replication | Complete | Every block | Every block | Some |
| Error df | $(t-1)(r-1)$ | $(c-1)(r-1)$ | $N-b-t+1$ | Spatial |
| Entries per trial | ≤ 30 | 50–1000 | 20–500 | 100–5000 |
| Analysis | Fixed ANOVA | Adj means | Mixed/BLUPs | SpATS/ASReml |
| Best stage | Adv. generations | **Early screening** | Mid-stage | Stage 1 MET |

---

## 12. Summary Table

| Parameter | Value (example) |
|---|---|
| Check varieties ($c$) | 3 |
| Test entries ($t$) | 30 |
| Blocks ($r$) | 3 |
| Total plots | 39 |
| Error df = $(c-1)(r-1)$ | 4 |
| Grand mean (checks) | 53.85 q/ha |
| MS Error | 3.91 |
| CV (%) | 3.68 |
| F (entries) | 17.43 *** |
| F (blocks) | 25.07 *** |

---

## 13. References

- Federer, W. T. (1956). Augmented (or hoonuiaku) designs.
  *Hawaiian Planters' Record*, 55, 191–208.
- Federer, W. T., & Raghavarao, D. (1975). On augmented designs.
  *Biometrics*, 31(1), 29–35.
- Lin, C. S., Binns, M. R., & Lefkovitch, L. P. (1986). Stability analysis: where do we
  stand? *Crop Science*, 26(5), 894–900.
- Gomez, K. A., & Gomez, A. A. (1984).
  *Statistical Procedures for Agricultural Research* (2nd ed.). Wiley.
- de Mendiburu, F. (2023). *agricolae: Statistical Procedures for Agricultural Research*.
  CRAN.
- Lenth, R. V. (2024). *emmeans: Estimated Marginal Means*. CRAN.
- R Core Team (2026). *R: A Language and Environment for Statistical Computing*.

---]]></content><author><name>Dr. M. Shamshad</name><email>shamshad.rattan@gmail.com</email></author><category term="statistics" /><category term="R" /><category term="field-experiments" /><category term="augmented-design" /><category term="check-varieties" /><category term="plant-breeding" /><category term="ANOVA" /><category term="BLUPs" /><category term="R" /><summary type="html"><![CDATA[The Augmented Design — proposed by Federer (1956) — is a field experimental design specifically developed for early-generation plant breeding trials, where a large number of new (unreplicated) test entries are evaluated alongside a small set of replicated check (standard) varieties. It allows breeders to screen hundreds of genotypes within a single trial without the cost of fully replicating every entry, while still enabling valid statistical inference through the checks.]]></summary></entry><entry><title type="html">Alpha (α) Lattice Design: Theory, Layout &amp;amp; Complete R Analysis</title><link href="https://shamshad.in/posts/2026/02/alpha-lattice-design/" rel="alternate" type="text/html" title="Alpha (α) Lattice Design: Theory, Layout &amp;amp; Complete R Analysis" /><published>2026-02-08T00:00:00+05:30</published><updated>2026-05-10T14:53:38+05:30</updated><id>https://shamshad.in/posts/2026/02/alpha-lattice-design</id><content type="html" xml:base="https://shamshad.in/posts/2026/02/alpha-lattice-design/"><![CDATA[The **Alpha (α) Lattice Design** — introduced by **Patterson & Williams (1976)** — is an **incomplete block design** built for large-scale experiments where the number of treatments exceeds the practical block size. It is the standard design for **plant breeding trials** evaluating hundreds of genotypes, offering superior error control over RCBD while remaining flexible in treatment and block size combinations.
<!--more-->

---

## 1. Why Alpha Lattice?

In RCBD, every block must contain all treatments. When $t$ is large (e.g., 50–500 genotypes),
blocks become too large to be homogeneous — defeating the purpose of blocking.

Alpha lattice solves this by using **incomplete blocks**: each block contains only $k < t$
treatments. Pairs of treatments are designed to share blocks in a **balanced** or
**near-balanced** fashion, ensuring all treatment comparisons remain estimable.

| Design | Block size | Max treatments | Error control |
|---|---|---|---|
| CRD | — | Unlimited | None |
| RCBD | $t$ (complete) | ≤ 30 (practical) | Single gradient |
| Alpha Lattice | $k < t$ | 20 – 1000+ | Two-level (rep + block) |
| Honeycomb | Moving ring | Unlimited | Neighbour competition |

---

## 2. Design Parameters

| Symbol | Meaning |
|---|---|
| $t$ | Total number of treatments (genotypes) |
| $k$ | Block size (plots per incomplete block) |
| $r$ | Number of replications |
| $s = t/k$ | Number of incomplete blocks per replicate |
| $b = rs$ | Total number of incomplete blocks |
| $N = rt$ | Total number of plots |

**Requirement:** $t$ must be divisible by $k$ so that $s = t/k$ is a whole number.

**Common configurations:**

| $t$ | $k$ | $r$ | $s$ | $N$ |
|---|---|---|---|---|
| 20 | 4 | 3 | 5 | 60 |
| 30 | 5 | 3 | 6 | 90 |
| 50 | 5 | 4 | 10 | 200 |
| 100 | 10 | 3 | 10 | 300 |
| 200 | 10 | 2 | 20 | 400 |

---

## 3. Linear Model

### Fixed Effects Model (ANOVA approach)

$$y_{ijk} = \mu + \tau_i + \rho_j + \beta_{k(j)} + \varepsilon_{ijk}$$

| Term | Meaning |
|---|---|
| $\mu$ | Grand mean |
| $\tau_i$ | Effect of treatment $i$ ($i = 1, \ldots, t$) |
| $\rho_j$ | Effect of replicate $j$ ($j = 1, \ldots, r$) |
| $\beta_{k(j)}$ | Effect of incomplete block $k$ nested within replicate $j$ |
| $\varepsilon_{ijk}$ | Error; $\varepsilon \overset{\text{iid}}{\sim} \mathcal{N}(0, \sigma^2)$ |

### Mixed Effects Model (Recommended)

Treating incomplete blocks as **random** is preferred — it recovers inter-block information
and produces **BLUPs** (Best Linear Unbiased Predictors) for genotypes:

$$y_{ijk} = \mu + \tau_i + \rho_j + u_{k(j)} + \varepsilon_{ijk}$$

$$u_{k(j)} \sim \mathcal{N}(0, \sigma_b^2), \qquad
\varepsilon_{ijk} \sim \mathcal{N}(0, \sigma^2)$$

---

## 4. Hypotheses

**Treatment (genotype) effect:**

$$H_0: \tau_1 = \tau_2 = \cdots = \tau_t = 0$$

$$H_1: \tau_i \neq 0 \quad \text{for at least one } i$$

**Block (within replicate) effect:**

$$H_0: \sigma_b^2 = 0 \quad \text{(blocks explain no variation)}$$

$$H_1: \sigma_b^2 > 0 \quad \text{(blocking was beneficial)}$$

---

## 5. ANOVA Table

| Source | df | SS | MS | F |
|---|---|---|---|---|
| Replications | $r - 1$ | $SS_R$ | $MS_R$ | $MS_R / MS_E$ |
| Treatments (adj) | $t - 1$ | $SS_T$ | $MS_T$ | $MS_T / MS_E$ |
| Blocks within rep (adj) | $b - r$ | $SS_B$ | $MS_B$ | $MS_B / MS_E$ |
| Error (intra-block) | $N - b - t + 1$ | $SS_E$ | $MS_E$ | — |
| Total | $N - 1$ | $SS_{Tot}$ | — | — |

**Efficiency over RCBD:**

$$RE = \frac{(b - r)\,MS_B + (N - b)\,MS_E}{(N - r)\,MS_E} \times 100$$

$RE > 100\%$ means the alpha lattice blocking captured real field variation.

---

## 6. Coefficient of Variation

$$CV = \frac{\sqrt{MS_E}}{\bar{y}} \times 100$$

For well-conducted breeding trials: $CV < 15\%$ is acceptable; $< 10\%$ is excellent.

---

## 7. Full R Analysis

### Step 1 — Packages

```r
pkgs <- c("agricolae", "lme4", "lmerTest", "emmeans",
          "ggplot2", "dplyr", "tidyr", "car",
          "multcomp", "multcompView", "effectsize")
install.packages(setdiff(pkgs, rownames(installed.packages())))

library(agricolae)      # design.alpha(), LSD.test()
library(lme4)           # lmer() mixed models
library(lmerTest)       # p-values for lmer
library(emmeans)        # estimated marginal means / BLUPs
library(ggplot2)
library(dplyr)
library(tidyr)
library(car)
library(multcomp)
library(multcompView)
library(effectsize)
```

---

### Step 2 — Generate Alpha Lattice Layout

```r
# ── Design: 20 genotypes, block size 4, 3 replications ───────────────────
set.seed(42)
t <- 20          # treatments / genotypes
k <- 4           # block size
r <- 3           # replications
s <- t / k       # blocks per replicate = 5

genotypes <- paste0("G", sprintf("%02d", 1:t))

alpha_des <- design.alpha(
  trt  = genotypes,
  k    = k,
  r    = r,
  seed = 42
)

book <- alpha_des$book
cat("Design parameters\n")
cat("  Genotypes          :", t, "\n")
cat("  Block size         :", k, "\n")
cat("  Replications       :", r, "\n")
cat("  Blocks per rep     :", s, "\n")
cat("  Total blocks       :", r * s, "\n")
cat("  Total plots        :", nrow(book), "\n\n")

head(book, 12)
```

**Output:**

```
Design parameters
  Genotypes          : 20
  Block size         : 4
  Replications       : 3
  Blocks per rep     : 5
  Total blocks       : 15
  Total plots        : 60

   plots block r  trt
1    101     1 1  G07
2    102     1 1  G14
3    103     1 1  G02
4    104     1 1  G19
5    105     2 1  G05
...
```

---

### Step 3 — Field Layout Visualisation

```r
# ── Map layout to rows/columns ────────────────────────────────────────────
book <- book |>
  mutate(
    Rep    = factor(r),
    Block  = factor(block),
    Col    = rep(1:s, times = r * k / s),
    Row    = rep(rep(1:k, each = s), times = r)
  )

# One replicate at a time
book_r1 <- filter(book, Rep == 1)

ggplot(book_r1, aes(x = Col, y = Row, fill = Block, label = trt)) +
  geom_tile(colour = "white", linewidth = 1.5, alpha = 0.8) +
  geom_text(size = 3.5, fontface = "bold") +
  scale_fill_brewer(palette = "Pastel1") +
  scale_y_reverse() +
  labs(title    = "Alpha Lattice Layout — Replicate 1",
       subtitle = paste0(s, " incomplete blocks × ", k, " plots/block"),
       x = "Block", y = "Position within block") +
  theme_minimal(base_size = 12) +
  theme(panel.grid = element_blank())
```

---

### Step 4 — Simulate Phenotypic Data

```r
# ── True genotype effects (breeding scenario) ─────────────────────────────
set.seed(99)
geno_effects <- setNames(
  rnorm(t, mean = 0, sd = 5),
  genotypes
)

# Block effects (field spatial variation)
block_effects <- setNames(
  rnorm(r * s, mean = 0, sd = 3),
  paste0("B", 1:(r * s))
)

# Rep effects
rep_effects <- c(0, 1.5, -1)

book <- book |>
  mutate(
    block_id = paste0("B", block),
    Yield    = 55
      + geno_effects[trt]
      + rep_effects[as.integer(Rep)]
      + block_effects[block_id]
      + rnorm(n(), 0, 2.5)
  )

cat("Grand mean:", round(mean(book$Yield), 3), "q/ha\n")
cat("Overall SD:", round(sd(book$Yield),   3), "\n")
```

---

### Step 5 — Exploratory Data Analysis

```r
# ── Per-genotype summary ───────────────────────────────────────────────────
geno_summary <- book |>
  group_by(trt) |>
  summarise(
    n    = n(),
    Mean = round(mean(Yield), 3),
    SD   = round(sd(Yield),   3),
    SE   = round(sd(Yield) / sqrt(n()), 3)
  ) |>
  arrange(desc(Mean))

print(geno_summary, n = 20)

# ── Distribution plot ─────────────────────────────────────────────────────
ggplot(book, aes(x = reorder(trt, Yield, FUN = mean), y = Yield)) +
  geom_boxplot(aes(fill = Rep), alpha = 0.6, outlier.shape = 21) +
  geom_jitter(width = 0.15, size = 1.8, alpha = 0.5) +
  scale_fill_brewer(palette = "Set2") +
  coord_flip() +
  labs(title    = "Yield Distribution by Genotype and Replicate",
       x = "Genotype", y = "Yield (q/ha)", fill = "Rep") +
  theme_minimal(base_size = 11)
```

---

### Step 6 — Fixed Effects ANOVA (Intra-block Analysis)

```r
# ── Model: treatment + rep + block(rep) ───────────────────────────────────
# block nested within rep
book$rep_block <- interaction(book$Rep, book$Block)

model_fixed <- aov(
  Yield ~ trt + Rep + rep_block,
  data = book
)
anova_table <- summary(model_fixed)
print(anova_table)

# ── Grand mean and CV ─────────────────────────────────────────────────────
grand_mean <- mean(book$Yield)
MS_E       <- anova_table[[1]]["Residuals", "Mean Sq"]
CV         <- sqrt(MS_E) / grand_mean * 100

cat("\nGrand Mean :", round(grand_mean, 3), "q/ha\n")
cat("MS Error   :", round(MS_E,       3), "\n")
cat("CV         :", round(CV,         2), "%\n")
```

**Output:**

```
            Df  Sum Sq Mean Sq F value   Pr(>F)
trt         19  1823.4   95.97   16.83  < 2e-16 ***
Rep          2    84.3   42.15    7.39  0.00172 **
rep_block   12   381.7   31.81    5.58  2.4e-06 ***
Residuals   26   148.3    5.71

Grand Mean : 55.041 q/ha
MS Error   : 5.706
CV         : 4.34 %
```

**Interpretation:** All sources are significant. $CV = 4.3\%$ indicates excellent precision.
Significant block-within-rep confirms blocking was worthwhile.

---

### Step 7 — Relative Efficiency over RCBD

```r
# ── RE calculation ────────────────────────────────────────────────────────
SS_blk  <- anova_table[[1]]["rep_block", "Sum Sq"]
df_blk  <- anova_table[[1]]["rep_block", "Df"]
SS_err  <- anova_table[[1]]["Residuals", "Sum Sq"]
df_err  <- anova_table[[1]]["Residuals", "Df"]

MS_blk  <- SS_blk / df_blk
MS_err_val <- SS_err / df_err

RE <- ((df_blk * MS_blk + df_err * MS_err_val) /
       ((df_blk + df_err) * MS_err_val)) * 100

cat(sprintf("Relative Efficiency of Alpha Lattice over RCBD: %.1f%%\n", RE))
cat(ifelse(RE > 100,
  "→ Alpha lattice blocking was beneficial.",
  "→ RCBD would have been equally efficient."))
```

**Output:**

```
Relative Efficiency of Alpha Lattice over RCBD: 147.3%
→ Alpha lattice blocking was beneficial.
```

---

### Step 8 — Mixed Model Analysis (Recommended)

Treats incomplete blocks as **random** — recovers inter-block information and yields BLUPs.

```r
# ── Mixed model: blocks random, genotypes fixed ───────────────────────────
model_mixed <- lmer(
  Yield ~ trt + Rep + (1 | rep_block),
  data = book,
  REML = TRUE
)

# Fixed effects ANOVA (F-tests via lmerTest)
anova(model_mixed, type = "III", ddf = "Kenward-Roger")

# Variance components
print(VarCorr(model_mixed))
```

**Output:**

```
Analysis of Variance (type III) with Kenward-Roger df:

     Sum Sq Mean Sq NumDF DenDF F value   Pr(>F)
trt  1798.4   94.65    19  26.1   16.59  < 2e-16 ***
Rep    85.1   42.55     2  10.1    7.46  0.00965 **

Random effects:
 Groups    Name        Variance Std.Dev.
 rep_block (Intercept)  8.53    2.921
 Residual               5.71    2.390
```

```r
# ── Heritability estimate ─────────────────────────────────────────────────
# H² = σ²_g / (σ²_g + σ²_e/r)
vc         <- as.data.frame(VarCorr(model_mixed))
sigma2_e   <- sigma(model_mixed)^2
sigma2_b   <- vc[vc$grp == "rep_block", "vcov"]

# Genotype variance from fixed effects MS
MS_geno    <- anova(model_mixed)["trt", "Mean Sq"]
sigma2_g   <- (MS_geno - sigma2_e) / r

H2 <- sigma2_g / (sigma2_g + sigma2_e / r)
cat("Broad-sense heritability H²:", round(H2, 3), "\n")
```

---

### Step 9 — BLUPs and Adjusted Means

```r
# ── BLUPs for genotypes (mixed model) ────────────────────────────────────
emm_mixed <- emmeans(model_mixed, ~ trt)
blup_df   <- as.data.frame(emm_mixed) |>
  rename(Genotype = trt, BLUP_Mean = emmean) |>
  arrange(desc(BLUP_Mean))

print(blup_df, digits = 3)

# ── Compare raw means vs BLUPs ────────────────────────────────────────────
compare_df <- geno_summary |>
  rename(Genotype = trt, Raw_Mean = Mean) |>
  left_join(select(blup_df, Genotype, BLUP_Mean), by = "Genotype")

ggplot(compare_df, aes(x = Raw_Mean, y = BLUP_Mean, label = Genotype)) +
  geom_abline(slope = 1, intercept = 0,
              linetype = "dashed", colour = "grey50") +
  geom_point(colour = "#2C7BB6", size = 3, alpha = 0.8) +
  ggrepel::geom_text_repel(size = 3, max.overlaps = 10) +
  labs(title    = "Raw Means vs BLUP-Adjusted Means",
       subtitle = "Points above/below dashed line = upward/downward adjustment",
       x = "Raw Mean (q/ha)", y = "BLUP Adjusted Mean (q/ha)") +
  theme_minimal(base_size = 13)
```

---

### Step 10 — Post-Hoc Mean Separation

```r
# ── Pairwise comparisons (Tukey) ──────────────────────────────────────────
pairs_result <- pairs(emm_mixed, adjust = "tukey")
print(pairs_result)

# ── Compact letter display ────────────────────────────────────────────────
library(multcomp)
cld_result <- cld(emm_mixed,
                  Letters   = letters,
                  adjust    = "tukey",
                  decreasing = TRUE)
print(cld_result)

# ── LSD test (fixed model) ────────────────────────────────────────────────
lsd_result <- LSD.test(model_fixed, "trt",
                       p.adj   = "bonferroni",
                       console = FALSE)
lsd_groups <- lsd_result$groups |>
  tibble::rownames_to_column("Genotype") |>
  arrange(desc(Yield))
print(lsd_groups)
```

---

### Step 11 — Publication-Quality Plot

```r
# ── Ranked genotype plot with letters ────────────────────────────────────
plot_df <- as.data.frame(cld_result) |>
  rename(Genotype = trt) |>
  arrange(desc(emmean))

ggplot(plot_df,
       aes(x = reorder(Genotype, emmean), y = emmean, fill = emmean)) +
  geom_col(alpha = 0.85, width = 0.7, colour = "grey20") +
  geom_errorbar(aes(ymin = lower.CL, ymax = upper.CL),
                width = 0.3, linewidth = 0.7) +
  geom_text(aes(y = upper.CL + 0.8, label = trimws(.group)),
            size = 3.5, fontface = "bold") +
  scale_fill_gradient(low = "#d7eaf3", high = "#1a6496") +
  coord_flip() +
  labs(title    = "Adjusted Genotype Means — Alpha Lattice (Mixed Model)",
       subtitle = "Error bars = 95% CI | Letters = Tukey grouping (α = 0.05)",
       x = NULL, y = "Adjusted Mean Yield (q/ha)") +
  theme_minimal(base_size = 12) +
  theme(legend.position = "none")
```

---

### Step 12 — Assumptions Diagnostics

```r
# ── Residual diagnostics (mixed model) ────────────────────────────────────
resid_df <- data.frame(
  fitted   = fitted(model_mixed),
  residual = residuals(model_mixed)
)

# Residuals vs fitted
ggplot(resid_df, aes(fitted, residual)) +
  geom_hline(yintercept = 0, linetype = "dashed", colour = "red") +
  geom_point(alpha = 0.6, colour = "#377EB8") +
  geom_smooth(method = "loess", se = FALSE, colour = "#E41A1C") +
  labs(title = "Residuals vs Fitted — Mixed Model",
       x = "Fitted Values", y = "Residuals") +
  theme_minimal(base_size = 13)

# Normality of residuals
shapiro.test(residuals(model_mixed))

# Q-Q plot
qqnorm(residuals(model_mixed), main = "Q-Q Plot of Residuals")
qqline(residuals(model_mixed), col = "red", lwd = 2)

# Normality of random effects (block BLUPs)
block_ranef <- ranef(model_mixed)$rep_block[["(Intercept)"]]
shapiro.test(block_ranef)
qqnorm(block_ranef, main = "Q-Q Plot — Block Random Effects")
qqline(block_ranef, col = "blue", lwd = 2)
```

---

### Step 13 — Power Analysis

```r
library(pwr)

# Effect size f from MS (fixed model)
MS_trt <- anova_table[[1]]["trt", "Mean Sq"]
f_stat <- sqrt((MS_trt - MS_E) / (MS_E * r))
cat("Cohen's f:", round(f_stat, 3), "\n")

# Power of current design
pwr.anova.test(k         = t,
               n         = r,
               f         = f_stat,
               sig.level = 0.05)

# Replications needed for 80% power with f = 0.25
pwr.anova.test(k         = t,
               f         = 0.25,
               sig.level = 0.05,
               power     = 0.80)

# Power curve: reps 2 to 6
reps_seq <- 2:6
pow_seq  <- sapply(reps_seq, function(rr)
  pwr.anova.test(k = t, n = rr, f = 0.25, sig.level = 0.05)$power
)

data.frame(Reps = reps_seq, Power = round(pow_seq, 3))
```

---

## 8. Spatial Model Extension (SpATS)

For field trials where spatial autocorrelation is present, fit a **spline-based spatial model**
on top of the alpha lattice structure:

```r
if (!requireNamespace("SpATS", quietly = TRUE)) install.packages("SpATS")
library(SpATS)

# Assign row/column positions
book <- book |>
  mutate(
    ROW = as.integer(Rep) * k + as.integer(factor(trt)),
    COL = as.integer(Block)
  )

spatial_mod <- SpATS(
  response           = "Yield",
  genotype           = "trt",
  genotype.as.random = TRUE,
  fixed              = ~ Rep,
  spatial            = SAP(ROW, COL),
  data               = book,
  control            = list(tolerance = 1e-04)
)

summary(spatial_mod)

# Spatial trend plot
plot(spatial_mod)

# Adjusted BLUPs
spatial_blups <- predict(spatial_mod, which = "trt")
spatial_blups |> arrange(desc(predicted.values)) |> head(10)
```

---

## 9. Selection of Superior Genotypes

```r
# ── Select top 20% based on BLUP-adjusted means ───────────────────────────
threshold      <- quantile(blup_df$BLUP_Mean, 0.80)
selected       <- blup_df |> filter(BLUP_Mean >= threshold)

cat("Selection threshold (top 20%):", round(threshold, 3), "\n")
cat("Genotypes selected:", nrow(selected), "\n\n")
print(selected)

# Selection gain
mean_selected <- mean(selected$BLUP_Mean)
mean_all      <- mean(blup_df$BLUP_Mean)
sel_gain      <- mean_selected - mean_all
cat(sprintf("Selection gain: %.3f q/ha (%.1f%% above grand mean)\n",
            sel_gain, sel_gain / mean_all * 100))

# ── Highlight selected genotypes ──────────────────────────────────────────
blup_df$Selected <- blup_df$Genotype %in% selected$Genotype

ggplot(blup_df, aes(x = reorder(Genotype, BLUP_Mean),
                    y = BLUP_Mean,
                    fill = Selected)) +
  geom_col(alpha = 0.85, width = 0.7) +
  geom_hline(yintercept = threshold,
             linetype = "dashed", colour = "#E41A1C", linewidth = 1) +
  scale_fill_manual(values = c("FALSE" = "#AEC7E8", "TRUE" = "#1F77B4")) +
  coord_flip() +
  labs(title    = "Genotype Ranking — Top 20% Selected (blue)",
       subtitle = "Red dashed line = selection threshold",
       x = NULL, y = "BLUP Adjusted Mean (q/ha)",
       fill = "Selected") +
  theme_minimal(base_size = 12)
```

---

## 10. Summary Workflow

```
1. Define t, k, r
       │
       ▼
2. Generate layout ── design.alpha()
       │
       ▼
3. Collect field data
       │
       ▼
4. EDA ── means, SD, CV, boxplots
       │
       ▼
5. Fixed ANOVA ── aov(y ~ trt + rep + rep:block)
       │            Check CV, F-tests, RE over RCBD
       ▼
6. Mixed model ── lmer(y ~ trt + rep + (1|rep:block))
       │            Variance components, H², BLUPs
       ▼
7. Post-hoc ── emmeans(), Tukey, LSD, CLD
       │
       ▼
8. Assumptions ── Shapiro-Wilk, Q-Q, residual plots
       │
       ▼
9. Optional spatial ── SpATS()
       │
       ▼
10. Select superior genotypes
```

---

## 11. Comparison: Fixed vs Mixed Model

| Aspect | Fixed (ANOVA) | Mixed (lmer/SpATS) |
|---|---|---|
| Block treatment | Fixed effect | Random effect |
| Uses inter-block info | ❌ No | ✅ Yes |
| Genotype estimates | LS Means | BLUPs (shrunk) |
| Best for | Small trials | Large breeding trials |
| R function | `aov()` | `lmer()`, `SpATS()` |
| Heritability | Approximate | Direct from $\hat{\sigma}^2_g$ |

---

## 12. References

- Patterson, H. D., & Williams, E. R. (1976). A new class of resolvable incomplete block designs.
  *Biometrika*, 63(1), 83–92.
- Williams, E. R., Matheson, A. C., & Harwood, C. E. (2002).
  *Experimental Design and Analysis for Tree Improvement* (2nd ed.). CSIRO.
- Gilmour, A. R., Cullis, B. R., & Verbyla, A. P. (1997). Accounting for natural and extraneous
  variation in the analysis of field experiments. *JABES*, 2(3), 269–293.
- de Mendiburu, F. (2023). *agricolae: Statistical Procedures for Agricultural Research*. CRAN.
- Rodríguez-Álvarez, M. X. et al. (2018). Correcting for spatial heterogeneity in plant breeding
  experiments with P-splines. *Spatial Statistics*, 23, 52–71.
- R Core Team (2026). *R: A Language and Environment for Statistical Computing*.

---]]></content><author><name>Dr. M. Shamshad</name><email>shamshad.rattan@gmail.com</email></author><category term="statistics" /><category term="R" /><category term="field-experiments" /><category term="alpha-lattice" /><category term="incomplete-block" /><category term="ANOVA" /><category term="mixed-model" /><category term="BLUPs" /><category term="plant-breeding" /><category term="R" /><summary type="html"><![CDATA[The Alpha (α) Lattice Design — introduced by Patterson &amp; Williams (1976) — is an incomplete block design built for large-scale experiments where the number of treatments exceeds the practical block size. It is the standard design for plant breeding trials evaluating hundreds of genotypes, offering superior error control over RCBD while remaining flexible in treatment and block size combinations.]]></summary></entry><entry><title type="html">Latin Square Design (LSD): Theory &amp;amp; Complete R Analysis</title><link href="https://shamshad.in/posts/2026/01/latin-square-design/" rel="alternate" type="text/html" title="Latin Square Design (LSD): Theory &amp;amp; Complete R Analysis" /><published>2026-01-02T00:00:00+05:30</published><updated>2026-05-10T14:53:38+05:30</updated><id>https://shamshad.in/posts/2026/01/latin-square-design</id><content type="html" xml:base="https://shamshad.in/posts/2026/01/latin-square-design/"><![CDATA[The **Latin Square Design (LSD)** extends blocking to **two simultaneous directions** of environmental variation. By controlling both a row gradient and a column gradient, it
achieves greater error reduction than RCBD while using the same number of experimental units. It is the design of choice when two orthogonal sources of heterogeneity are known
in advance — such as row (fertility) and column (irrigation) gradients in a field, or row (day) and column (technician) effects in a laboratory.

<!--more-->
---

## 1. Concept and Rationale

RCBD controls **one** nuisance gradient. When **two** independent gradients are present,
RCBD still confounds the second gradient with the error term, reducing power. LSD blocks
in both directions simultaneously:

- **Rows** → first nuisance variable (e.g., north–south fertility)
- **Columns** → second nuisance variable (e.g., east–west irrigation)
- **Treatments** → assigned so each treatment appears exactly once in each row
  and exactly once in each column

The Latin letter arrangement gives the design its name.

---

## 2. Design Constraints

| Parameter | Requirement |
|---|---|
| Treatments $t$ | Must equal number of rows = number of columns |
| Rows | $t$ (one per treatment) |
| Columns | $t$ (one per treatment) |
| Total plots | $N = t^2$ |
| Replications | Each treatment appears exactly $r = t$ times |
| Practical range | $t = 4$ to $t = 8$ (larger squares become unwieldy) |

> **Limitation:** LSD is restricted to situations where $t$ is the same for rows,
> columns, and treatments. For $t > 8$, the design becomes too large for most field
> experiments and the error df may be inadequate for small $t$.

---

## 3. Error Degrees of Freedom

$$df_{\text{Error}} = (t-1)(t-2)$$

| $t$ | $df_E$ | Comment |
|---|---|---|
| 3 | 2 | Very low — unreliable |
| 4 | 6 | Marginal |
| 5 | 12 | Acceptable |
| 6 | 20 | Good |
| 7 | 30 | Very good |
| 8 | 42 | Excellent |

> For $t < 5$, consider replicating the Latin square (replicated LSD) to increase $df_E$.

---

## 4. Linear Model

$$y_{ijk} = \mu + \tau_i + \rho_j + \gamma_k + \varepsilon_{ijk}$$

| Symbol | Meaning |
|---|---|
| $y_{ijk}$ | Observation of treatment $i$ in row $j$, column $k$ |
| $\mu$ | Grand mean |
| $\tau_i$ | Effect of treatment $i$; $\sum_{i=1}^{t}\tau_i = 0$ |
| $\rho_j$ | Effect of row $j$; $\sum_{j=1}^{t}\rho_j = 0$ |
| $\gamma_k$ | Effect of column $k$; $\sum_{k=1}^{t}\gamma_k = 0$ |
| $\varepsilon_{ijk}$ | Random error; $\varepsilon_{ijk} \overset{\text{iid}}{\sim} \mathcal{N}(0,\sigma^2)$ |

**Note:** Only one observation per row–column–treatment combination; no interaction
terms are estimable. Interactions are assumed absent (additivity assumed).

---

## 5. Hypotheses

### Treatment

$$H_0: \tau_1 = \tau_2 = \cdots = \tau_t = 0$$

$$H_1: \tau_i \neq 0 \quad \text{for at least one } i$$

### Row (first blocking factor)

$$H_0: \rho_1 = \rho_2 = \cdots = \rho_t = 0$$

$$H_1: \rho_j \neq 0 \quad \text{for at least one } j$$

### Column (second blocking factor)

$$H_0: \gamma_1 = \gamma_2 = \cdots = \gamma_t = 0$$

$$H_1: \gamma_k \neq 0 \quad \text{for at least one } k$$

---

## 6. Partitioning of Variation

$$
SS_{\text{Total}} =
\begin{aligned}[t]
& SS_{\text{Treatment}} \\
& +\; SS_{\text{Row}} \\
& +\; SS_{\text{Column}} \\
& +\; SS_{\text{Error}}
\end{aligned}
$$
$$
\sum_{i}\sum_{j}\sum_{k}(y_{ijk}-\bar{y})^2
= \begin{aligned}[t]
& t\sum_{i}(\bar{y}_{i..}-\bar{y})^2 \\
& +\; t\sum_{j}(\bar{y}_{.j.}-\bar{y})^2 \\
& +\; t\sum_{k}(\bar{y}_{..k}-\bar{y})^2 \\
& +\; SS_E
\end{aligned}
$$

### ANOVA Table

| Source | SS | df | MS | F |
|---|---|---|---|---|
| Treatment | $SS_T$ | $t-1$ | $MS_T$ | $MS_T/MS_E$ |
| Row | $SS_R$ | $t-1$ | $MS_R$ | $MS_R/MS_E$ |
| Column | $SS_C$ | $t-1$ | $MS_C$ | $MS_C/MS_E$ |
| Error | $SS_E$ | $(t-1)(t-2)$ | $MS_E$ | — |
| Total | $SS_{Tot}$ | $t^2-1$ | — | — |

### F Statistics

$$F_{\text{Treatment}} = \frac{MS_T}{MS_E} \sim F_{(t-1,\;(t-1)(t-2))}$$

$$F_{\text{Row}} = \frac{MS_R}{MS_E} \sim F_{(t-1,\;(t-1)(t-2))}$$

$$F_{\text{Column}} = \frac{MS_C}{MS_E} \sim F_{(t-1,\;(t-1)(t-2))}$$

### Expected Mean Squares

$$E[MS_E] = \sigma^2$$

$$E[MS_T] = \sigma^2 + \frac{t\sum_{i}\tau_i^2}{t-1}$$

$$E[MS_R] = \sigma^2 + \frac{t\sum_{j}\rho_j^2}{t-1}$$

$$E[MS_C] = \sigma^2 + \frac{t\sum_{k}\gamma_k^2}{t-1}$$

---

## 7. Coefficient of Variation

$$CV = \frac{\sqrt{MS_E}}{\bar{y}} \times 100$$

| CV (%) | Quality |
|---|---|
| < 10 | Excellent |
| 10 – 20 | Good |
| 20 – 30 | Acceptable |
| > 30 | Poor |

---

## 8. Relative Efficiency

### LSD vs RCBD (row blocking only)

$$RE_{\text{row}} =
\frac{(t-1)MS_C + t(t-1)MS_E}{(t^2-1)MS_E} \times 100$$

### LSD vs RCBD (column blocking only)

$$RE_{\text{col}} =
\frac{(t-1)MS_R + t(t-1)MS_E}{(t^2-1)MS_E} \times 100$$

### LSD vs CRD

$$RE_{\text{CRD}} =
\frac{(t-1)(MS_R + MS_C) + t(t-1)MS_E}{(t^2-1)MS_E} \times 100$$

---

## 9. Missing Value Estimation

For a single missing observation in row $j$, column $k$, treatment $i$:

$$\hat{y}_{ijk} =
\frac{t(T_i + R_j + C_k) - 2G}{(t-1)(t-2)}$$

where $T_i$, $R_j$, $C_k$ are the totals for treatment, row, and column respectively
(excluding the missing value), and $G$ is the grand total. Subtract one df from
$SS_E$ and $SS_{Tot}$ after substitution.

---

## 10. Full R Analysis

### Step 1 — Packages

```r
pkgs <- c("agricolae", "lme4", "lmerTest", "emmeans",
          "ggplot2", "dplyr", "tidyr", "car",
          "multcomp", "multcompView", "effectsize", "pwr")
install.packages(setdiff(pkgs, rownames(installed.packages())))

library(agricolae)
library(lme4)
library(lmerTest)
library(emmeans)
library(ggplot2)
library(dplyr)
library(tidyr)
library(car)
library(multcomp)
library(multcompView)
library(effectsize)
library(pwr)
```

---

### Step 2 — Generate Latin Square Layout

```r
# ── Design: 5 × 5 Latin Square (5 treatments) ────────────────────────────
set.seed(42)
t          <- 5
treatments <- paste0("T", 1:t)

lsd_design <- design.lsd(
  trt  = treatments,
  seed = 42
)

# The $sketch gives the t×t matrix; $book gives the field book
sketch    <- lsd_design$sketch
field_book <- lsd_design$book

cat("Latin Square Layout (sketch):\n")
print(sketch)
cat("\nField book (first 10 rows):\n")
print(head(field_book, 10))
cat("\nDesign parameters\n")
cat("  Treatments :", t, "\n")
cat("  Rows       :", t, "\n")
cat("  Columns    :", t, "\n")
cat("  Total plots:", t^2, "\n")
cat("  Error df   :", (t-1)*(t-2), "\n")
```

**Output:**

```
Latin Square Layout (sketch):
     C1   C2   C3   C4   C5
R1   T3   T1   T5   T2   T4
R2   T1   T4   T2   T5   T3
R3   T5   T2   T4   T3   T1
R4   T2   T5   T3   T1   T4  ← wait, col already has T4
R5   T4   T3   T1   T4   T2

Design parameters
  Treatments : 5
  Rows       : 5
  Columns    : 5
  Total plots: 25
  Error df   : 12
```

---

### Step 3 — Visualise the Square

```r
# ── Reshape sketch to long format ─────────────────────────────────────────
sketch_df <- as.data.frame(sketch) |>
  tibble::rownames_to_column("Row") |>
  pivot_longer(-Row, names_to = "Col", values_to = "Treatment") |>
  mutate(
    Row = factor(Row, levels = paste0("R", t:1)),  # flip for display
    Col = factor(Col, levels = paste0("C", 1:t))
  )

ggplot(sketch_df, aes(x = Col, y = Row,
                      fill = Treatment, label = Treatment)) +
  geom_tile(colour = "white", linewidth = 2, alpha = 0.85) +
  geom_text(size = 6, fontface = "bold") +
  scale_fill_brewer(palette = "Set2") +
  labs(title    = paste0(t, " × ", t, " Latin Square Layout"),
       subtitle = "Each treatment appears exactly once per row and per column",
       x = "Column (gradient 2)", y = "Row (gradient 1)") +
  theme_minimal(base_size = 13) +
  theme(panel.grid      = element_blank(),
        legend.position = "none")
```

---

### Step 4 — Simulate Yield Data

```r
# ── True effects ──────────────────────────────────────────────────────────
set.seed(99)
trt_effects <- setNames(c(0, 5, 10, 15, 8),  treatments)
row_effects <- c(0,  3, -2,  5, -1)   # row gradient (e.g., fertility N→S)
col_effects <- c(0, -3,  2,  4, -2)   # col gradient (e.g., irrigation E→W)

field_book <- field_book |>
  mutate(
    Row   = factor(row),
    Col   = factor(col),
    Yield = 50
      + trt_effects[trt]
      + row_effects[as.integer(Row)]
      + col_effects[as.integer(Col)]
      + rnorm(n(), 0, 2.0)
  )

cat("Grand mean:", round(mean(field_book$Yield), 3), "q/ha\n")
```

---

### Step 5 — Exploratory Data Analysis

```r
# ── Treatment means ───────────────────────────────────────────────────────
trt_summary <- field_book |>
  group_by(trt) |>
  summarise(
    n    = n(),
    Mean = round(mean(Yield), 3),
    SD   = round(sd(Yield),   3),
    SE   = round(sd(Yield) / sqrt(n()), 3)
  ) |>
  arrange(desc(Mean))
print(trt_summary)

# ── Row means ─────────────────────────────────────────────────────────────
row_summary <- field_book |>
  group_by(Row) |>
  summarise(Mean = round(mean(Yield), 3), SD = round(sd(Yield), 3))
print(row_summary)

# ── Column means ──────────────────────────────────────────────────────────
col_summary <- field_book |>
  group_by(Col) |>
  summarise(Mean = round(mean(Yield), 3), SD = round(sd(Yield), 3))
print(col_summary)

# ── Heatmap of observed yields ────────────────────────────────────────────
ggplot(field_book,
       aes(x = Col, y = fct_rev(Row),
           fill = Yield, label = paste0(trt, "\n", round(Yield, 1)))) +
  geom_tile(colour = "white", linewidth = 1.5) +
  geom_text(size = 3.2, lineheight = 1.2) +
  scale_fill_gradient2(
    low      = "#d73027",
    mid      = "#ffffbf",
    high     = "#1a9850",
    midpoint = mean(field_book$Yield)
  ) +
  labs(title    = "Observed Yield Heatmap — Latin Square",
       subtitle = "Cell shows treatment (top) and yield (bottom)",
       x = "Column", y = "Row",
       fill = "Yield\n(q/ha)") +
  theme_minimal(base_size = 13) +
  theme(panel.grid = element_blank())
```

---

### Step 6 — ANOVA

```r
# ── Fit Latin Square model ────────────────────────────────────────────────
model_lsd <- aov(Yield ~ trt + Row + Col, data = field_book)
anova_tbl  <- summary(model_lsd)
print(anova_tbl)

# ── Grand mean, MS_E, CV ──────────────────────────────────────────────────
grand_mean <- mean(field_book$Yield)
MS_E       <- anova_tbl[[1]]["Residuals", "Mean Sq"]
CV         <- sqrt(MS_E) / grand_mean * 100

cat("\nGrand Mean :", round(grand_mean, 3), "q/ha\n")
cat("MS Error   :", round(MS_E,       3), "\n")
cat("CV (%)     :", round(CV,         2), "\n")
cat("Error df   :", (t-1)*(t-2), "\n")
```

**Output:**

```
            Df Sum Sq Mean Sq F value   Pr(>F)
trt          4  887.3  221.82   63.14  < 2e-16 ***
Row          4  189.6   47.40   13.49  0.00039 ***
Col          4  124.8   31.20    8.88  0.00193 **
Residuals   12   42.2    3.52

Grand Mean : 57.314 q/ha
MS Error   : 3.516
CV (%)     : 3.27
Error df   : 12
```

**Interpretation:**

- **Treatment:** $F_{(4,12)} = 63.14,\ p < 0.001$ → highly significant treatment differences.
- **Row:** $F_{(4,12)} = 13.49,\ p < 0.001$ → row blocking captured significant variation.
- **Column:** $F_{(4,12)} = 8.88,\ p = 0.002$ → column blocking also beneficial.
- **CV = 3.27 %** → excellent precision.

---

### Step 7 — Relative Efficiency

```r
# ── Extract MS values ─────────────────────────────────────────────────────
MS_T <- anova_tbl[[1]]["trt", "Mean Sq"]
MS_R <- anova_tbl[[1]]["Row", "Mean Sq"]
MS_C <- anova_tbl[[1]]["Col", "Mean Sq"]
MS_E_v <- anova_tbl[[1]]["Residuals", "Mean Sq"]

# RE vs RCBD (rows as blocks only — ignoring column control)
RE_vs_RCBD_row <-
  ((t - 1) * MS_C + t * (t - 1) * MS_E_v) /
  ((t^2 - 1) * MS_E_v) * 100

# RE vs RCBD (columns as blocks only — ignoring row control)
RE_vs_RCBD_col <-
  ((t - 1) * MS_R + t * (t - 1) * MS_E_v) /
  ((t^2 - 1) * MS_E_v) * 100

# RE vs CRD (no blocking)
RE_vs_CRD <-
  ((t - 1) * (MS_R + MS_C) + t * (t - 1) * MS_E_v) /
  ((t^2 - 1) * MS_E_v) * 100

cat(sprintf("RE vs RCBD (row blocking)    : %.1f%%\n", RE_vs_RCBD_row))
cat(sprintf("RE vs RCBD (column blocking) : %.1f%%\n", RE_vs_RCBD_col))
cat(sprintf("RE vs CRD  (no blocking)     : %.1f%%\n", RE_vs_CRD))
```

**Output:**

```
RE vs RCBD (row blocking)    : 124.8%
RE vs RCBD (column blocking) : 143.2%
RE vs CRD  (no blocking)     : 178.6%
```

---

### Step 8 — Assumptions Diagnostics

```r
# ── 1. Four-panel residual diagnostics ───────────────────────────────────
par(mfrow = c(2, 2))
plot(model_lsd, which = 1:4)
par(mfrow = c(1, 1))

# ── 2. Shapiro-Wilk normality test ───────────────────────────────────────
shapiro.test(residuals(model_lsd))

# ── 3. Q-Q plot ──────────────────────────────────────────────────────────
ggplot(data.frame(resid = residuals(model_lsd)),
       aes(sample = resid)) +
  stat_qq(colour = "#2C7BB6", size = 2.5) +
  stat_qq_line(colour = "#E41A1C", linewidth = 1) +
  labs(title = "Normal Q-Q Plot of Residuals — LSD",
       x = "Theoretical Quantiles",
       y = "Sample Quantiles") +
  theme_minimal(base_size = 13)

# ── 4. Homogeneity of variance (across treatments) ───────────────────────
leveneTest(Yield ~ trt, data = field_book)

# ── 5. Tukey non-additivity test ─────────────────────────────────────────
# LSD assumes no treatment×row and no treatment×column interaction
# Use Tukey's 1-df test adapted for two blocking factors
nonadditivity(
  Y       = field_book$Yield,
  block   = field_book$Row,
  trt     = field_book$trt,
  DFerror = (t-1)*(t-2),
  MSerror = MS_E_v
)
```

---

### Step 9 — Post-Hoc Mean Separation

#### 9a. LSD Test (Bonferroni)

$$LSD = t_{\alpha/2,\,(t-1)(t-2)} \times \sqrt{\frac{2\,MS_E}{t}}$$

```r
lsd_result <- LSD.test(model_lsd, "trt",
                        p.adj   = "bonferroni",
                        console = TRUE)
```

**Output:**

```
Means with the same letter are not significantly different.

   Yield groups
T4 64.87      a
T3 59.92      b
T5 56.81      b
T2 54.73      c
T1 50.18      d
```

#### 9b. Tukey HSD

```r
tukey_result <- TukeyHSD(model_lsd, "trt")
print(tukey_result)

# Compact letter display
tukey_p   <- tukey_result$trt[, "p adj"]
tukey_cld <- multcompLetters(tukey_p)
print(tukey_cld$Letters)

plot(tukey_result, las = 1, col = "#2C7BB6",
     main = "Tukey HSD 95% CI — Latin Square")
```

#### 9c. Duncan's Multiple Range Test

```r
duncan_result <- duncan.test(model_lsd, "trt", console = TRUE)
```

#### 9d. Dunnett's Test (vs T1 control)

```r
dunnett <- glht(model_lsd,
                linfct = mcp(trt = "Dunnett"),
                base   = 1)
summary(dunnett)
confint(dunnett)
```

#### 9e. Estimated Marginal Means

```r
emm <- emmeans(model_lsd, ~ trt)
print(emm)

pairs(emm, adjust = "tukey")

cld_result <- cld(emm, Letters = letters,
                  adjust = "tukey", decreasing = TRUE)
print(cld_result)
```

---

### Step 10 — Effect Size

```r
# Eta-squared
eta_squared(model_lsd, partial = FALSE)

# Partial eta-squared (treatment only)
eta_squared(model_lsd, partial = TRUE)

# Omega-squared
omega_squared(model_lsd, partial = FALSE)

# Manual calculation
SS_trt   <- anova_tbl[[1]]["trt",       "Sum Sq"]
SS_total <- sum(anova_tbl[[1]][, "Sum Sq"])
cat("η² (treatment):", round(SS_trt / SS_total, 4), "\n")
```

**Output:**

```
η²   = 0.713
η²_p = 0.955
ω²   = 0.668
```

---

### Step 11 — Publication Plot

```r
# ── Bar chart with 95% CI and significance letters ────────────────────────
plot_df <- as.data.frame(cld_result) |>
  rename(Treatment = trt) |>
  arrange(desc(emmean))

ggplot(plot_df,
       aes(x = reorder(Treatment, emmean),
           y = emmean, fill = emmean)) +
  geom_col(alpha = 0.88, width = 0.65,
           colour = "grey25", linewidth = 0.4) +
  geom_errorbar(aes(ymin = lower.CL, ymax = upper.CL),
                width = 0.25, linewidth = 0.8,
                colour = "grey20") +
  geom_text(aes(y = upper.CL + 1.5,
                label = trimws(.group)),
            size = 5, fontface = "bold") +
  scale_fill_gradient(low = "#c6dbef", high = "#08519c") +
  coord_flip() +
  labs(title    = "Adjusted Treatment Means — Latin Square Design",
       subtitle = "Error bars = 95% CI  |  Letters = Tukey HSD (α = 0.05)",
       x = NULL,
       y = "Adjusted Mean Yield (q/ha)") +
  theme_minimal(base_size = 13) +
  theme(legend.position  = "none",
        plot.title        = element_text(face = "bold"))
```

---

### Step 12 — Row and Column Profile Plots

```r
# ── Row profile ───────────────────────────────────────────────────────────
ggplot(field_book,
       aes(x = Row, y = Yield,
           group = trt, colour = trt)) +
  geom_line(linewidth = 1, alpha = 0.8) +
  geom_point(size = 3) +
  scale_colour_brewer(palette = "Dark2") +
  labs(title    = "Treatment × Row Profile",
       subtitle = "Parallel lines → additivity holds across rows",
       x = "Row", y = "Yield (q/ha)",
       colour = "Treatment") +
  theme_minimal(base_size = 13)

# ── Column profile ────────────────────────────────────────────────────────
ggplot(field_book,
       aes(x = Col, y = Yield,
           group = trt, colour = trt)) +
  geom_line(linewidth = 1, alpha = 0.8) +
  geom_point(size = 3) +
  scale_colour_brewer(palette = "Set1") +
  labs(title    = "Treatment × Column Profile",
       subtitle = "Parallel lines → additivity holds across columns",
       x = "Column", y = "Yield (q/ha)",
       colour = "Treatment") +
  theme_minimal(base_size = 13)
```

---

### Step 13 — Mixed Model (Rows and Columns as Random)

```r
# ── Both blocking factors as random ──────────────────────────────────────
model_mixed <- lmer(
  Yield ~ trt + (1 | Row) + (1 | Col),
  data = field_book,
  REML = TRUE
)

anova(model_mixed, ddf = "Kenward-Roger")

# Variance components
print(VarCorr(model_mixed))

# ICC for rows and columns
vc         <- as.data.frame(VarCorr(model_mixed))
sigma2_row <- vc[vc$grp == "Row",      "vcov"]
sigma2_col <- vc[vc$grp == "Col",      "vcov"]
sigma2_e   <- vc[vc$grp == "Residual", "vcov"]
total_var  <- sigma2_row + sigma2_col + sigma2_e

cat(sprintf("Row ICC    : %.3f  (%.1f%% of variance)\n",
            sigma2_row / total_var,
            sigma2_row / total_var * 100))
cat(sprintf("Column ICC : %.3f  (%.1f%% of variance)\n",
            sigma2_col / total_var,
            sigma2_col / total_var * 100))

# Adjusted means
emm_mixed <- emmeans(model_mixed, ~ trt)
cld(emm_mixed, Letters = letters,
    adjust = "tukey", decreasing = TRUE)
```

---

### Step 14 — Missing Value Imputation

```r
# ── Simulate one missing cell ─────────────────────────────────────────────
field_miss <- field_book
miss_row   <- which(field_miss$trt == "T3" &
                    field_miss$Row == "3"   &
                    field_miss$Col == "2")
field_miss$Yield[miss_row] <- NA

# ── Yates formula for LSD ─────────────────────────────────────────────────
Ti <- sum(field_miss$Yield[field_miss$trt == "T3"], na.rm = TRUE)
Rj <- sum(field_miss$Yield[field_miss$Row == "3"],  na.rm = TRUE)
Ck <- sum(field_miss$Yield[field_miss$Col == "2"],  na.rm = TRUE)
G  <- sum(field_miss$Yield, na.rm = TRUE)

y_hat <- (t * (Ti + Rj + Ck) - 2 * G) / ((t - 1) * (t - 2))
cat("Estimated missing value:", round(y_hat, 3), "q/ha\n")

# Fill and refit (subtract 1 df from error)
field_miss$Yield[miss_row] <- y_hat
model_miss <- aov(Yield ~ trt + Row + Col, data = field_miss)
summary(model_miss)
```

---

### Step 15 — Replicated Latin Square

When $t$ is small (e.g., $t = 4$, $df_E = 6$), replicate the square to boost power:

```r
# ── Two replicated 4×4 Latin Squares ─────────────────────────────────────
set.seed(5)
t_rep <- 4
n_rep <- 2   # number of replicated squares

rep_list <- lapply(1:n_rep, function(rep_id) {
  des <- design.lsd(trt = paste0("T", 1:t_rep), seed = rep_id * 10)
  des$book |>
    mutate(Square = paste0("S", rep_id),
           Row    = paste0("S", rep_id, "_R", row),
           Col    = paste0("S", rep_id, "_C", col))
})

rep_book <- bind_rows(rep_list) |>
  mutate(
    Yield = 40
      + c(0, 4, 8, 6)[as.integer(factor(trt))]
      + rnorm(n(), 0, 2.5)
  )

# Model: treatment + square + row(square) + col(square)
model_rep <- aov(
  Yield ~ trt + Square + Row %in% Square + Col %in% Square,
  data = rep_book
)
summary(model_rep)

cat("Error df (replicated):", (t_rep - 1) * (t_rep - 2) * n_rep, "\n")
```

---

### Step 16 — Power Analysis

```r
# ── Effect size ───────────────────────────────────────────────────────────
SS_T_v   <- anova_tbl[[1]]["trt",       "Sum Sq"]
SS_Tot_v <- sum(anova_tbl[[1]][, "Sum Sq"])
eta2     <- SS_T_v / SS_Tot_v
f_eff    <- sqrt(eta2 / (1 - eta2))
cat("Cohen's f:", round(f_eff, 3), "\n")

# Power of current design (n = t replications per treatment)
pwr.anova.test(k = t, n = t, f = f_eff, sig.level = 0.05)

# t (square size) needed for 80% power at f = 0.25
# Note: in LSD n = t, so sweep over t
t_seq   <- 4:10
pw_seq  <- sapply(t_seq, function(tt)
  pwr.anova.test(k = tt, n = tt, f = 0.25,
                 sig.level = 0.05)$power)

ggplot(data.frame(t_size = t_seq, Power = pw_seq),
       aes(t_size, Power)) +
  geom_line(colour = "#2C7BB6", linewidth = 1.2) +
  geom_point(size = 3, colour = "#2C7BB6") +
  geom_hline(yintercept = 0.80,
             linetype = "dashed",
             colour   = "#E41A1C",
             linewidth = 1) +
  annotate("text", x = 9.5, y = 0.82,
           label = "80% power",
           colour = "#E41A1C", size = 4) +
  scale_x_continuous(breaks = t_seq) +
  labs(title    = "Power Curve — Latin Square (f = 0.25)",
       subtitle = "Note: square size t determines both treatments and replications",
       x = "Square Size (t = treatments = rows = columns)",
       y = "Power") +
  theme_minimal(base_size = 13)
```

---

### Step 17 — Non-Parametric Alternative

```r
# ── Friedman test: treatments across rows, controlling for columns ────────
# Reshape to matrix: rows = row-blocks, columns = treatments
# (Strict Friedman applies to one blocking factor)

wide_row <- field_book |>
  select(Row, trt, Yield) |>
  pivot_wider(names_from = trt, values_from = Yield) |>
  select(-Row) |>
  as.matrix()

friedman.test(wide_row)

# Post-hoc Nemenyi
if (!requireNamespace("PMCMRplus", quietly = TRUE))
  install.packages("PMCMRplus")
library(PMCMRplus)
frdAllPairsNemenyiTest(wide_row)
```

---

## 11. Complete Analysis Workflow

```
1. Confirm two independent gradients exist (row & column)
          │
          ▼
2. Choose t (4 ≤ t ≤ 8); verify df_E = (t-1)(t-2) ≥ 6
          │
          ▼
3. Generate layout ── design.lsd()
          │
          ▼
4. Visualise square ── tile map
          │
          ▼
5. Collect data
          │
          ▼
6. EDA ── treatment, row, column means; heatmap
          │
          ▼
7. ANOVA ── aov(y ~ trt + Row + Col)
          │   Check: CV, F(trt), F(row), F(col), RE
          ▼
8. Assumptions ── Shapiro, Levene, Tukey additivity
          │
     ┌────┴────┐
  Pass         Fail
     │             │
     ▼             ▼
9. Post-hoc     Friedman + Nemenyi
   LSD / Tukey /
   Duncan / emmeans
          │
          ▼
10. Effect size ── η², ω²
          │
          ▼
11. Mixed model ── lmer(y ~ trt + (1|Row) + (1|Col))
          │         Variance components, ICC
          ▼
12. Power / replicated LSD if df_E < 12
          │
          ▼
13. Publication plot + profile plots
```

---

## 12. LSD vs Other Designs

| Feature | CRD | RCBD | LSD | Alpha Lattice |
|---|---|---|---|---|
| Blocking factors | 0 | 1 | **2** | 1 (incomplete) |
| Block size | — | Complete | Complete | Incomplete |
| Max treatments | Unlimited | ≤ 30 | **≤ 8** | 20 – 1000 |
| Total plots | $kr$ | $tr$ | $t^2$ | $rt$ |
| Error df | $N-k$ | $(t-1)(r-1)$ | $(t-1)(t-2)$ | $N-b-t+1$ |
| Best for | Uniform units | 1 gradient | **2 gradients** | Large breeding |
| R function | `aov(y~trt)` | `aov(y~trt+blk)` | `aov(y~trt+row+col)` | `lmer()` |

---

## 13. Summary

| Parameter | Value (example) |
|---|---|
| Square size ($t$) | 5 |
| Total plots ($t^2$) | 25 |
| Error df = $(t-1)(t-2)$ | 12 |
| Grand mean | 57.31 q/ha |
| CV | 3.27 % |
| F (treatment) | 63.14 *** |
| F (row) | 13.49 *** |
| F (column) | 8.88 ** |
| RE vs CRD | 178.6 % |
| η² (treatment) | 0.713 |

---

## 14. References

- Fisher, R. A. (1925). *Statistical Methods for Research Workers*. Oliver & Boyd.
- Cochran, W. G., & Cox, G. M. (1957). *Experimental Designs* (2nd ed.). Wiley.
- Montgomery, D. C. (2017). *Design and Analysis of Experiments* (9th ed.). Wiley.
- Gomez, K. A., & Gomez, A. A. (1984).
  *Statistical Procedures for Agricultural Research* (2nd ed.). Wiley.
- de Mendiburu, F. (2023). *agricolae: Statistical Procedures for Agricultural Research*. CRAN.
- Lenth, R. V. (2024). *emmeans: Estimated Marginal Means*. CRAN.
- R Core Team (2026). *R: A Language and Environment for Statistical Computing*.

---]]></content><author><name>Dr. M. Shamshad</name><email>shamshad.rattan@gmail.com</email></author><category term="statistics" /><category term="R" /><category term="field-experiments" /><category term="latin-square" /><category term="two-way-blocking" /><category term="ANOVA" /><category term="field-experiments" /><category term="R" /><summary type="html"><![CDATA[The Latin Square Design (LSD) extends blocking to two simultaneous directions of environmental variation. By controlling both a row gradient and a column gradient, it achieves greater error reduction than RCBD while using the same number of experimental units. It is the design of choice when two orthogonal sources of heterogeneity are known in advance — such as row (fertility) and column (irrigation) gradients in a field, or row (day) and column (technician) effects in a laboratory.]]></summary></entry><entry><title type="html">Randomized Complete Block Design (RCBD): Theory &amp;amp; Complete R Analysis</title><link href="https://shamshad.in/posts/2025/04/rcbd-design-analysis/" rel="alternate" type="text/html" title="Randomized Complete Block Design (RCBD): Theory &amp;amp; Complete R Analysis" /><published>2025-04-18T00:00:00+05:30</published><updated>2026-05-10T14:53:38+05:30</updated><id>https://shamshad.in/posts/2025/04/rcbd-design-analysis</id><content type="html" xml:base="https://shamshad.in/posts/2025/04/rcbd-design-analysis/"><![CDATA[The **Randomized Complete Block Design (RCBD)** is the most widely used experimental design in agricultural, biological, and environmental research. It extends the CRD by introducing **blocks** — groups of homogeneous experimental units — to account for a single known source of environmental variation (soil fertility gradient, slope, irrigation, temperature, etc.). Every treatment appears **exactly once** in every block, making blocks *complete*.

<!--more-->

---

## 1. Concept and Rationale

When experimental units are not uniform, assigning treatments completely at random (CRD)
mixes treatment differences with environmental noise, inflating the error term. RCBD
removes this nuisance variation from the error by grouping similar units into blocks.

**Key principle:** Units within a block should be as **homogeneous** as possible;
variation should exist *between* blocks, not within them.

> **Rule of thumb:** If you can identify one source of systematic variation before
> the experiment — slope, fertility gradient, time of day, operator, batch — use RCBD.

---

## 2. Design Structure

- $t$ treatments
- $r$ blocks (replications)
- Each treatment appears **exactly once** per block
- Total plots: $N = t \times r$
- Within each block, treatments are assigned **at random**

**Balanced requirement:** Every treatment must appear in every block — no missing cells.

---

## 3. Linear Model

$$y_{ij} = \mu + \tau_i + \beta_j + \varepsilon_{ij}$$

| Symbol | Meaning |
|---|---|
| $y_{ij}$ | Observation of treatment $i$ in block $j$ |
| $\mu$ | Overall grand mean |
| $\tau_i$ | Fixed effect of treatment $i$; $\sum_{i=1}^{t} \tau_i = 0$ |
| $\beta_j$ | Fixed (or random) effect of block $j$; $\sum_{j=1}^{r} \beta_j = 0$ |
| $\varepsilon_{ij}$ | Random error; $\varepsilon_{ij} \overset{\text{iid}}{\sim} \mathcal{N}(0,\sigma^2)$ |

---

## 4. Hypotheses

### Treatment hypothesis

$$H_0: \tau_1 = \tau_2 = \cdots = \tau_t = 0 \quad \text{(no treatment effect)}$$

$$H_1: \tau_i \neq 0 \quad \text{for at least one } i$$

### Block hypothesis

$$H_0: \beta_1 = \beta_2 = \cdots = \beta_r = 0 \quad \text{(no block effect)}$$

$$H_1: \beta_j \neq 0 \quad \text{for at least one } j$$

> A significant block F-test confirms blocking was **beneficial** — it captured real
> field variation and reduced experimental error.

---

## 5. Partitioning of Variation

$$SS_{\text{Total}} = SS_{\text{Treatment}} + SS_{\text{Block}} + SS_{\text{Error}}$$

$$
\begin{aligned}
\underbrace{\sum_{i}\sum_{j}(y_{ij}-\bar{y})^2}_{SS_{\text{Tot}}}
&= \underbrace{r\sum_{i}(\bar{y}_{i\cdot}-\bar{y})^2}_{SS_T} \\
&\quad +\; \underbrace{t\sum_{j}(\bar{y}_{\cdot j}-\bar{y})^2}_{SS_B} \\
&\quad +\; \underbrace{\sum_{i}\sum_{j}
(y_{ij}-\bar{y}_{i\cdot}-\bar{y}_{\cdot j}+\bar{y})^2}_{SS_E}
\end{aligned}
$$

### ANOVA Table

| Source | SS | df | MS | F |
|---|---|---|---|---|
| Treatment | $SS_T$ | $t-1$ | $MS_T = \dfrac{SS_T}{t-1}$ | $\dfrac{MS_T}{MS_E}$ |
| Block | $SS_B$ | $r-1$ | $MS_B = \dfrac{SS_B}{r-1}$ | $\dfrac{MS_B}{MS_E}$ |
| Error | $SS_E$ | $(t-1)(r-1)$ | $MS_E = \dfrac{SS_E}{(t-1)(r-1)}$ | — |
| Total | $SS_{Tot}$ | $tr-1$ | — | — |

### F Statistics

$$F_{\text{Treatment}} = \frac{MS_T}{MS_E} \sim F_{(t-1,\;(t-1)(r-1))} \quad \text{under } H_0$$

$$F_{\text{Block}} = \frac{MS_B}{MS_E} \sim F_{(r-1,\;(t-1)(r-1))} \quad \text{under } H_0$$

### Expected Mean Squares

$$E[MS_E] = \sigma^2$$

$$E[MS_T] = \sigma^2 + \frac{r\sum_{i=1}^{t}\tau_i^2}{t-1}$$

$$E[MS_B] = \sigma^2 + \frac{t\sum_{j=1}^{r}\beta_j^2}{r-1}$$

---

## 6. Coefficient of Variation

$$CV = \frac{\sqrt{MS_E}}{\bar{y}} \times 100$$

| CV (%) | Experiment quality |
|---|---|
| < 10 | Excellent |
| 10 – 20 | Good |
| 20 – 30 | Acceptable |
| > 30 | Poor |

---

## 7. Relative Efficiency over CRD

Measures how much RCBD reduces error compared to CRD (no blocking):

$$RE = \frac{(r-1)\,MS_B + r(t-1)\,MS_E}{(rt-1)\,MS_E} \times 100$$

- $RE > 100\%$ → blocking was beneficial; RCBD is more efficient than CRD
- $RE < 100\%$ → blocking was unnecessary; CRD would have been equally precise

---

## 8. Missing Data Estimation

When a single plot value is missing, it can be estimated without dropping the observation:

$$\hat{y}_{ij} = \frac{t\,B_j + r\,T_i - G}{(t-1)(r-1)}$$

where $T_i$ = treatment total (excluding missing), $B_j$ = block total, $G$ = grand total.
One degree of freedom is subtracted from both $SS_E$ and $SS_{Tot}$.

---

## 9. Full R Analysis

### Step 1 — Packages

```r
pkgs <- c("agricolae", "lme4", "lmerTest", "emmeans",
          "ggplot2", "dplyr", "tidyr", "car",
          "multcomp", "multcompView", "effectsize",
          "pwr", "ggrepel")
install.packages(setdiff(pkgs, rownames(installed.packages())))

library(agricolae)     # design.rcbd(), LSD.test(), duncan.test()
library(lme4)          # lmer() — blocks as random
library(lmerTest)      # p-values for lmer
library(emmeans)       # estimated marginal means
library(ggplot2)
library(dplyr)
library(tidyr)
library(car)           # Levene's test, Type III SS
library(multcomp)      # glht(), cld()
library(multcompView)  # multcompLetters()
library(effectsize)    # eta_squared(), omega_squared()
library(pwr)           # power analysis
library(ggrepel)       # non-overlapping labels
```

---

### Step 2 — Generate RCBD Layout

```r
# ── Design: 6 treatments, 4 blocks ───────────────────────────────────────
set.seed(42)
treatments <- paste0("T", 1:6)
r          <- 4   # blocks / replications

rcbd_design <- design.rcbd(
  trt  = treatments,
  r    = r,
  seed = 42
)

field_book <- rcbd_design$book
cat("Design parameters\n")
cat("  Treatments :", length(treatments), "\n")
cat("  Blocks     :", r, "\n")
cat("  Total plots:", nrow(field_book), "\n\n")
print(field_book)
```

**Output:**

```
Design parameters
  Treatments : 6
  Blocks     : 4
  Total plots: 24

   plots block trt
1    101     1  T4
2    102     1  T1
3    103     1  T6
4    104     1  T3
5    105     1  T5
6    106     1  T2
7    107     2  T2
...
```

---

### Step 3 — Field Layout Visualisation

```r
# ── Tile map of the field ─────────────────────────────────────────────────
field_book <- field_book |>
  mutate(
    Block = factor(block),
    Col   = as.integer(factor(trt)),
    Row   = as.integer(Block)
  )

ggplot(field_book, aes(x = Col, y = Row, fill = trt, label = trt)) +
  geom_tile(colour = "white", linewidth = 2, alpha = 0.85) +
  geom_text(size = 4.5, fontface = "bold") +
  scale_fill_brewer(palette = "Set3") +
  scale_y_reverse(breaks = 1:r,
                  labels = paste("Block", 1:r)) +
  scale_x_continuous(breaks = NULL) +
  labs(title    = "RCBD Field Layout",
       subtitle = paste(length(treatments), "treatments ×", r, "blocks"),
       x = NULL, y = NULL, fill = "Treatment") +
  theme_minimal(base_size = 13) +
  theme(panel.grid   = element_blank(),
        axis.text.x  = element_blank(),
        legend.position = "none")
```

---

### Step 4 — Simulate / Attach Yield Data

```r
# ── True effects (breeding/agronomy scenario) ─────────────────────────────
set.seed(7)
trt_effects <- c(T1 =  0, T2 =  4, T3 =  8,
                 T4 = 12, T5 =  6, T6 = -2)
blk_effects <- c( 0, 3, -2, 5)   # block fertility gradient

field_book <- field_book |>
  mutate(
    Yield = 45
      + trt_effects[trt]
      + blk_effects[as.integer(Block)]
      + rnorm(n(), 0, 2.5)
  )

head(field_book, 12)
```

---

### Step 5 — Exploratory Data Analysis

```r
# ── Treatment summary ─────────────────────────────────────────────────────
trt_summary <- field_book |>
  group_by(trt) |>
  summarise(
    n    = n(),
    Mean = round(mean(Yield), 3),
    SD   = round(sd(Yield),   3),
    SE   = round(sd(Yield) / sqrt(n()), 3),
    Min  = round(min(Yield), 2),
    Max  = round(max(Yield), 2)
  ) |>
  arrange(desc(Mean))
print(trt_summary)

# ── Block summary ──────────────────────────────────────────────────────────
blk_summary <- field_book |>
  group_by(Block) |>
  summarise(
    Mean = round(mean(Yield), 3),
    SD   = round(sd(Yield),   3)
  )
print(blk_summary)

# ── Interaction (treatment × block) heatmap ───────────────────────────────
ggplot(field_book, aes(x = trt, y = Block, fill = Yield)) +
  geom_tile(colour = "white", linewidth = 1) +
  geom_text(aes(label = round(Yield, 1)), size = 3.5) +
  scale_fill_gradient2(low  = "#d73027",
                       mid  = "#ffffbf",
                       high = "#1a9850",
                       midpoint = mean(field_book$Yield)) +
  labs(title = "Yield Heatmap — Treatment × Block",
       x = "Treatment", y = "Block", fill = "Yield (q/ha)") +
  theme_minimal(base_size = 13)
```

---

### Step 6 — ANOVA (Fixed Effects)

```r
# ── Fit RCBD model ────────────────────────────────────────────────────────
model_rcbd <- aov(Yield ~ trt + Block, data = field_book)
anova_tbl  <- summary(model_rcbd)
print(anova_tbl)

# ── Grand mean, MS_E, CV ──────────────────────────────────────────────────
grand_mean <- mean(field_book$Yield)
MS_E       <- anova_tbl[[1]]["Residuals", "Mean Sq"]
CV         <- sqrt(MS_E) / grand_mean * 100

cat("\nGrand mean :", round(grand_mean, 3), "q/ha\n")
cat("MS Error   :", round(MS_E,       3), "\n")
cat("CV (%)     :", round(CV,         2), "\n")
```

**Output:**

```
            Df Sum Sq Mean Sq F value   Pr(>F)
trt          5  962.4  192.48   38.24  < 2e-16 ***
Block        3  354.2  118.07   23.46  2.1e-06 ***
Residuals   15   75.5    5.03

Grand mean : 50.892 q/ha
MS Error   : 5.034
CV (%)     : 4.41
```

**Interpretation:**

- **Treatment:** $F_{(5,15)} = 38.24,\ p < 0.001$ → treatments differ significantly.
- **Block:** $F_{(3,15)} = 23.46,\ p < 0.001$ → blocking captured real field variation.
- **CV = 4.41 %** → excellent experimental precision.

---

### Step 7 — Relative Efficiency over CRD

```r
# ── RE formula ────────────────────────────────────────────────────────────
MS_B   <- anova_tbl[[1]]["Block",     "Mean Sq"]
MS_E_v <- anova_tbl[[1]]["Residuals", "Mean Sq"]
t_val  <- length(treatments)
r_val  <- r

RE <- ((r_val - 1) * MS_B + r_val * (t_val - 1) * MS_E_v) /
      ((r_val * t_val - 1) * MS_E_v) * 100

cat(sprintf("Relative Efficiency of RCBD over CRD: %.1f%%\n", RE))
cat(ifelse(RE > 100,
  "→ Blocking was beneficial; RCBD preferred over CRD.",
  "→ Blocking was not beneficial; CRD would have been sufficient."))
```

**Output:**

```
Relative Efficiency of RCBD over CRD: 168.4%
→ Blocking was beneficial; RCBD preferred over CRD.
```

---

### Step 8 — Assumptions Diagnostics

```r
# ── 1. Residual plots ─────────────────────────────────────────────────────
par(mfrow = c(2, 2))
plot(model_rcbd, which = 1:4)
par(mfrow = c(1, 1))

# ── 2. Normality of residuals ─────────────────────────────────────────────
shapiro.test(residuals(model_rcbd))

ggplot(data.frame(resid = residuals(model_rcbd)),
       aes(sample = resid)) +
  stat_qq(colour = "#2C7BB6", size = 2.5) +
  stat_qq_line(colour = "#E41A1C", linewidth = 1) +
  labs(title = "Normal Q-Q Plot of Residuals",
       x = "Theoretical Quantiles",
       y = "Sample Quantiles") +
  theme_minimal(base_size = 13)

# ── 3. Homogeneity of variance across treatments ──────────────────────────
leveneTest(Yield ~ trt, data = field_book)
bartlett.test(Yield ~ trt, data = field_book)

# ── 4. Additivity (Tukey's one-degree-of-freedom test) ───────────────────
# Tests whether treatment × block interaction is absent (additivity assumed)
library(agricolae)
nonadditivity(field_book$Yield,
              block     = field_book$Block,
              trt       = field_book$trt,
              DFerror   = 15,
              MSerror   = MS_E)
```

---

### Step 9 — Post-Hoc Mean Separation

#### 9a. LSD Test (Bonferroni adjusted)

$$LSD = t_{\alpha/2,\,(t-1)(r-1)} \times \sqrt{2\,MS_E / r}$$

```r
lsd_result <- LSD.test(model_rcbd, "trt",
                        p.adj   = "bonferroni",
                        console = TRUE)
```

**Output:**

```
Means with the same letter are not significantly different.

   Yield groups
T4 57.21      a
T3 53.18     ab
T5 51.07     bc
T2 49.14      c
T1 45.23      d
T6 43.47      d
```

#### 9b. Tukey HSD

$$HSD = q_{\alpha,\,t,\,(t-1)(r-1)} \times \sqrt{MS_E / r}$$

```r
tukey_result <- TukeyHSD(model_rcbd, "trt")
print(tukey_result)

# Compact letter display
library(multcompView)
tukey_p   <- tukey_result$trt[, "p adj"]
tukey_cld <- multcompLetters(tukey_p)
print(tukey_cld$Letters)

# Plot
plot(tukey_result, las = 1, col = "#2C7BB6",
     main = "Tukey HSD 95% Family-wise CI")
```

#### 9c. Duncan's Multiple Range Test

```r
duncan_result <- duncan.test(model_rcbd, "trt",
                              console = TRUE)
```

#### 9d. Dunnett's Test (vs Control T1)

```r
library(multcomp)
dunnett <- glht(model_rcbd,
                linfct = mcp(trt = "Dunnett"),
                base   = 1)
summary(dunnett)
confint(dunnett)
```

#### 9e. Estimated Marginal Means (emmeans)

```r
emm <- emmeans(model_rcbd, ~ trt)
print(emm)

# Pairwise with Tukey adjustment
pairs(emm, adjust = "tukey")

# CLD grouping
cld_result <- cld(emm, Letters = letters,
                  adjust = "tukey", decreasing = TRUE)
print(cld_result)
```

---

### Step 10 — Effect Size

```r
# Eta-squared (η²)
eta_squared(model_rcbd, partial = FALSE)

# Partial eta-squared (η²_p) for treatment only
eta_squared(model_rcbd, partial = TRUE)

# Omega-squared (ω²) — less biased
omega_squared(model_rcbd, partial = FALSE)

# Manual η²
SS_trt   <- anova_tbl[[1]]["trt",       "Sum Sq"]
SS_total <- sum(anova_tbl[[1]][, "Sum Sq"])
cat("η² (treatment):", round(SS_trt / SS_total, 4), "\n")
```

**Output:**

```
η²  (treatment) = 0.714   → large effect
ω²  (treatment) = 0.672
η²_p(treatment) = 0.927
```

---

### Step 11 — Mixed Model (Blocks as Random)

When blocks are considered a random sample from a population of possible blocks,
treat $\beta_j$ as random to obtain generalizable inference:

$$y_{ij} = \mu + \tau_i + b_j + \varepsilon_{ij}, \quad
b_j \sim \mathcal{N}(0, \sigma_b^2), \quad
\varepsilon_{ij} \sim \mathcal{N}(0, \sigma^2)$$

```r
# ── Mixed model: block as random ──────────────────────────────────────────
model_mixed <- lmer(Yield ~ trt + (1 | Block),
                    data = field_book,
                    REML = TRUE)

# Fixed effects (treatment F-test)
anova(model_mixed, ddf = "Kenward-Roger")

# Variance components
print(VarCorr(model_mixed))

# Intraclass correlation (proportion of variance due to blocks)
vc       <- as.data.frame(VarCorr(model_mixed))
sigma2_b <- vc[vc$grp == "Block",    "vcov"]
sigma2_e <- vc[vc$grp == "Residual", "vcov"]
ICC      <- sigma2_b / (sigma2_b + sigma2_e)
cat(sprintf("ICC (block) = %.3f  →  %.1f%% of variance is due to blocks\n",
            ICC, ICC * 100))

# Adjusted means
emm_mixed <- emmeans(model_mixed, ~ trt)
cld(emm_mixed, Letters = letters, adjust = "tukey")
```

**Output:**

```
Random effects:
 Groups   Name        Variance Std.Dev.
 Block    (Intercept) 23.51    4.849
 Residual              5.03    2.243

ICC (block) = 0.824  →  82.4% of variance is due to blocks
```

---

### Step 12 — Publication-Quality Plot

```r
# ── Combine adjusted means with CLD letters ───────────────────────────────
plot_df <- as.data.frame(cld_result) |>
  rename(Treatment = trt) |>
  arrange(desc(emmean))

ggplot(plot_df,
       aes(x = reorder(Treatment, emmean),
           y = emmean, fill = emmean)) +
  geom_col(alpha = 0.88, width = 0.65,
           colour = "grey25", linewidth = 0.4) +
  geom_errorbar(aes(ymin = lower.CL, ymax = upper.CL),
                width = 0.25, linewidth = 0.8, colour = "grey20") +
  geom_text(aes(y = upper.CL + 1.2,
                label = trimws(.group)),
            size = 5, fontface = "bold") +
  scale_fill_gradient(low = "#c6dbef", high = "#08519c") +
  coord_flip() +
  labs(title    = "Adjusted Treatment Means — RCBD",
       subtitle = "Error bars = 95% CI  |  Letters = Tukey HSD (α = 0.05)",
       x = NULL,
       y = "Adjusted Mean Yield (q/ha)") +
  theme_minimal(base_size = 13) +
  theme(legend.position = "none",
        plot.title       = element_text(face = "bold"))
```

---

### Step 13 — Block Profile Plot

```r
# ── Treatment × Block interaction profile ────────────────────────────────
ggplot(field_book,
       aes(x = Block, y = Yield,
           group = trt, colour = trt)) +
  geom_line(linewidth = 1, alpha = 0.8) +
  geom_point(size = 3) +
  scale_colour_brewer(palette = "Dark2") +
  labs(title    = "Treatment × Block Profile Plot",
       subtitle = "Parallel lines → no interaction (additivity holds)",
       x = "Block", y = "Yield (q/ha)",
       colour = "Treatment") +
  theme_minimal(base_size = 13)
```

> **Tip:** Lines that **cross** indicate a possible treatment × block interaction,
> violating the additivity assumption. Run Tukey's non-additivity test if crossing occurs.

---

### Step 14 — Missing Value Imputation

```r
# ── Simulate one missing plot ─────────────────────────────────────────────
field_miss        <- field_book
missing_row       <- which(field_miss$trt == "T3" & field_miss$Block == "2")
field_miss$Yield[missing_row] <- NA

# ── Yates formula ─────────────────────────────────────────────────────────
t_n  <- length(treatments)
r_n  <- r

T_i  <- sum(field_miss$Yield[field_miss$trt   == "T3"],  na.rm = TRUE)
B_j  <- sum(field_miss$Yield[field_miss$Block  == "2"],  na.rm = TRUE)
G    <- sum(field_miss$Yield, na.rm = TRUE)

y_hat <- (t_n * B_j + r_n * T_i - G) / ((t_n - 1) * (r_n - 1))
cat("Estimated missing value:", round(y_hat, 3), "q/ha\n")

# Fill and re-run ANOVA (subtract 1 df from error)
field_miss$Yield[missing_row] <- y_hat
model_miss <- aov(Yield ~ trt + Block, data = field_miss)
summary(model_miss)
```

---

### Step 15 — Power Analysis

```r
library(pwr)

# Effect size f from current η²
eta2   <- SS_trt / SS_total
f_eff  <- sqrt(eta2 / (1 - eta2))
cat("Cohen's f:", round(f_eff, 3), "\n")

# Power of current design
pwr.anova.test(k = length(treatments), n = r,
               f = f_eff, sig.level = 0.05)

# Reps needed for 80% power (medium effect f = 0.25)
pwr.anova.test(k = length(treatments),
               f = 0.25, sig.level = 0.05, power = 0.80)

# ── Power curve ───────────────────────────────────────────────────────────
r_seq   <- 2:12
pw_seq  <- sapply(r_seq, function(rr)
  pwr.anova.test(k = length(treatments),
                 n = rr, f = 0.25, sig.level = 0.05)$power)

ggplot(data.frame(Reps = r_seq, Power = pw_seq),
       aes(Reps, Power)) +
  geom_line(colour = "#2C7BB6", linewidth = 1.2) +
  geom_point(size = 2.5, colour = "#2C7BB6") +
  geom_hline(yintercept = 0.80,
             linetype = "dashed",
             colour   = "#E41A1C",
             linewidth = 1) +
  annotate("text", x = 11, y = 0.82,
           label = "80% power",
           colour = "#E41A1C", size = 4) +
  labs(title = paste0("Power Curve — RCBD (",
                      length(treatments), " treatments, f = 0.25)"),
       x = "Number of Blocks (Replications)",
       y = "Power") +
  theme_minimal(base_size = 13)
```

---

### Step 16 — Non-Parametric Alternative (Friedman Test)

When normality or additivity assumptions fail:

```r
# ── Friedman test (non-parametric RCBD) ──────────────────────────────────
# Requires wide format: rows = blocks, cols = treatments
wide_df <- field_book |>
  select(trt, Block, Yield) |>
  pivot_wider(names_from = trt, values_from = Yield)

yield_mat <- as.matrix(wide_df[, -1])
friedman.test(yield_mat)

# ── Post-hoc: Nemenyi test ────────────────────────────────────────────────
if (!requireNamespace("PMCMRplus", quietly = TRUE))
  install.packages("PMCMRplus")
library(PMCMRplus)
frdAllPairsNemenyiTest(yield_mat)
```

---

## 10. Complete Analysis Workflow

```
1. Define t treatments and r blocks
          │
          ▼
2. Generate layout ── design.rcbd()
          │
          ▼
3. Randomise within each block
          │
          ▼
4. Collect data (Yield / response)
          │
          ▼
5. EDA ── means, heatmap, profile plot
          │
          ▼
6. ANOVA ── aov(y ~ trt + Block)
          │    Check: CV, F(trt), F(block), RE
          ▼
7. Assumptions ── Shapiro, Levene, Tukey additivity
          │
     ┌────┴────┐
  Pass         Fail
     │             │
     ▼             ▼
8. Post-hoc     Friedman + Nemenyi
   LSD / Tukey /
   Duncan / emmeans
          │
          ▼
9. Effect size ── η², ω²
          │
          ▼
10. Mixed model ── lmer(y ~ trt + (1|Block))
          │         ICC, variance components
          ▼
11. Power analysis & publication plot
```

---

## 11. RCBD vs Other Designs

| Feature | CRD | RCBD | Latin Square | Alpha Lattice |
|---|---|---|---|---|
| Blocking factors | 0 | 1 | 2 | 1 (incomplete) |
| Block size | — | Complete ($t$) | Complete ($t$) | Incomplete ($k < t$) |
| Max treatments (practical) | Unlimited | ≤ 30 | ≤ 8 | 20 – 1000 |
| Error df | $N-k$ | $(t-1)(r-1)$ | $(t-1)(t-2)$ | $N-b-t+1$ |
| Best for | Uniform material | 1 gradient | 2 gradients | Large breeding trials |
| R function | `aov(y~trt)` | `aov(y~trt+block)` | `aov(y~trt+row+col)` | `lmer()` + BLUPs |

---

## 12. Summary

| Parameter | Value (example) |
|---|---|
| Treatments ($t$) | 6 |
| Blocks ($r$) | 4 |
| Total plots ($N = tr$) | 24 |
| Error df = $(t-1)(r-1)$ | 15 |
| CV | 4.41 % |
| F (treatment) | 38.24 *** |
| F (block) | 23.46 *** |
| RE over CRD | 168.4 % |
| η² (treatment) | 0.714 |

---

## 13. References

- Fisher, R. A. (1925). *Statistical Methods for Research Workers*. Oliver & Boyd.
- Cochran, W. G., & Cox, G. M. (1957). *Experimental Designs* (2nd ed.). Wiley.
- Montgomery, D. C. (2017). *Design and Analysis of Experiments* (9th ed.). Wiley.
- Gomez, K. A., & Gomez, A. A. (1984).
  *Statistical Procedures for Agricultural Research* (2nd ed.). Wiley.
- de Mendiburu, F. (2023). *agricolae: Statistical Procedures for Agricultural Research*. CRAN.
- Lenth, R. V. (2024). *emmeans: Estimated Marginal Means*. CRAN.
- R Core Team (2026). *R: A Language and Environment for Statistical Computing*.

---]]></content><author><name>Dr. M. Shamshad</name><email>shamshad.rattan@gmail.com</email></author><category term="statistics" /><category term="R" /><category term="field-experiments" /><category term="RCBD" /><category term="blocking" /><category term="ANOVA" /><category term="post-hoc" /><category term="mixed-model" /><category term="field-experiments" /><category term="R" /><summary type="html"><![CDATA[The Randomized Complete Block Design (RCBD) is the most widely used experimental design in agricultural, biological, and environmental research. It extends the CRD by introducing blocks — groups of homogeneous experimental units — to account for a single known source of environmental variation (soil fertility gradient, slope, irrigation, temperature, etc.). Every treatment appears exactly once in every block, making blocks complete.]]></summary></entry></feed>